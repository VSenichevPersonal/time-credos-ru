---
phase: 01-time-mvp
plan: "01-02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/time/src/objects/tt-department.object.ts
  - apps/time/src/objects/tt-employee.object.ts
  - apps/time/src/objects/tt-project.object.ts
  - apps/time/src/objects/tt-stage.object.ts
  - apps/time/src/objects/tt-activity.object.ts
  - apps/time/src/objects/tt-time-entry.object.ts
  - apps/time/src/objects/tt-billing-link.object.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, DATA-07, DATA-08, L10N-01]
must_haves:
  truths:
    - "7 объектов tt* определены через defineObject с русскими labels"
    - "Связи RELATION двусторонние (Project→Department, TimeEntry→Project/Employee/Activity/Stage)"
    - "TimeEntry.hours — NUMBER (decimal); category/status/group — SELECT с русскими опциями из SSOT"
  artifacts:
    - "apps/time/src/objects/tt-time-entry.object.ts с FieldType.RELATION и NUMBER"
    - "apps/time/src/objects/tt-project.object.ts с category, plannedHours, approvalRequired"
  key_links:
    - "tt-*.object.ts ← src/shared/constants.ts (опции SELECT)"
    - "tt-project → стандартный Company через STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS"
---

<objective>
Определить модель данных как 7 объектов defineObject с префиксом tt: Department, Employee, Project, Stage, Activity, TimeEntry, BillingLink. Поля, связи (RELATION), индексы. Все labels — русские, опции SELECT — из SSOT. Это ядро данных, на нём строятся NAV, SEED, GRID, CAPACITY, APPROVAL.
</objective>

<context>
@research/twenty-sdk/fresh/data/objects.md
@research/twenty-sdk/fresh/data/relations.md
@research/twenty-sdk/fresh/data/overview.md
@docs/data-model/DATA_MODEL_SYNTHESIS.md
@docs/standards/DEV_STANDARDS.md
@apps/time/src/shared/constants.ts
@apps/time/src/shared/types.ts
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Справочники ttDepartment, ttActivity, ttEmployee</name>
    <files>apps/time/src/objects/tt-department.object.ts, apps/time/src/objects/tt-activity.object.ts, apps/time/src/objects/tt-employee.object.ts</files>
    <action>ttDepartment: code (SELECT: OV/OIB/OPIB/TC/OPR), name (рус.), approvalRequired (BOOLEAN), capacityFactor (NUMBER, default 0.8), headcount (NUMBER). ttActivity: name (рус.), group (SELECT из ActivityGroup, рус. ярлыки), department (RELATION→ttDepartment, nullable=global), billableByDefault (BOOLEAN). ttEmployee: firstName/lastName/middleName (TEXT), email (TEXT), department (RELATION→ttDepartment), position (TEXT), active (BOOLEAN), workspaceMemberRef (TEXT — id пользователя workspace). Стабильные universalIdentifier-константы.</action>
    <verify>grep -l "defineObject" apps/time/src/objects/tt-department.object.ts apps/time/src/objects/tt-activity.object.ts apps/time/src/objects/tt-employee.object.ts</verify>
    <done>3 справочника определены с русскими labels</done>
  </task>
  <task type="auto">
    <name>Task 2: ttProject + ttStage</name>
    <files>apps/time/src/objects/tt-project.object.ts, apps/time/src/objects/tt-stage.object.ts</files>
    <action>ttProject: code, name, company (RELATION→стандартный Company через STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS), department (RELATION→ttDepartment), manager (RELATION→ttEmployee), category (SELECT WorkCategory рус.), billable (BOOLEAN), status (SELECT), startDate/endDate (DATE_TIME), plannedHours (NUMBER), approvalRequired (BOOLEAN nullable), serviceRef (TEXT nullable — задел под каталог услуг). ttStage: project (RELATION→ttProject), code, name, status, startDate/endDate, plannedHours.</action>
    <verify>grep -E "category|plannedHours|Company" apps/time/src/objects/tt-project.object.ts</verify>
    <done>Project (развитая сущность) и Stage определены</done>
  </task>
  <task type="auto">
    <name>Task 3: ttTimeEntry + ttBillingLink + индексы</name>
    <files>apps/time/src/objects/tt-time-entry.object.ts, apps/time/src/objects/tt-billing-link.object.ts</files>
    <action>ttTimeEntry: date (DATE_TIME), hours (NUMBER, decimal), description (TEXT), employee (RELATION→ttEmployee), project (RELATION→ttProject), stage (RELATION→ttStage nullable), activity (RELATION→ttActivity nullable), billable (BOOLEAN), status (SELECT EntryStatus рус.). ttBillingLink: project (RELATION→ttProject), externalSystem (TEXT='1С'), docType (SELECT Order/Payment/Act рус.), externalId/number (TEXT), date (DATE_TIME), amount (NUMBER). Индексы: ttProject.code uniq; составной по ttTimeEntry (employee+date) если поддерживается. Двусторонние RELATION (обратные поля) по правилам relations.md.</action>
    <verify>grep -E "RELATION|NUMBER" apps/time/src/objects/tt-time-entry.object.ts</verify>
    <done>TimeEntry (атом) и BillingLink (M:N задел) определены, связи двусторонние</done>
  </task>
</tasks>

<verification>
- [ ] 7 файлов tt-*.object.ts с defineObject
- [ ] Все labels русские, опции SELECT из constants.ts
- [ ] RELATION двусторонние; Company — стандартный объект
- [ ] hours = NUMBER decimal
- [ ] yarn twenty dev --once --dry-run показывает создание 7 объектов без ошибок
</verification>

<success_criteria>7 объектов tt* валидны, dry-run проходит, нейминг и локализация по стандартам</success_criteria>
