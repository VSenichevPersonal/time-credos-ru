---
phase: 01-time-mvp
plan: "01-07"
type: execute
wave: 5
depends_on: ["01-02", "01-05"]
files_modified:
  - apps/time/src/front/capacity-board.front.tsx
  - apps/time/src/front/hooks/use-capacity.ts
  - apps/time/src/logic/capacity-data.logic.ts
autonomous: true
requirements: [CAPACITY-01, CAPACITY-02]
must_haves:
  truths:
    - "Доска: отделы × недели/месяцы вперёд, загрузка % с цветом"
    - "Загрузка = plannedHours проектов по неделям / ёмкость отдела"
  artifacts:
    - "apps/time/src/front/capacity-board.front.tsx"
    - "apps/time/src/logic/capacity-data.logic.ts (/s/ route расчёта)"
---

<objective>
Доска планирования загрузки (Excel-уровень): для каждого отдела — загрузка по неделям/месяцам вперёд, чтобы продажи видели, когда отдел освободится. Расчёт на бэке (/s/-route), отрисовка во front.
</objective>

<context>
@research/twenty-sdk/fresh/layout/front-components.md
@research/twenty-sdk/fresh/logic/logic-functions.md
@docs/data-model/CAPACITY_PLANNING.md
@apps/time/src/objects/tt-project.object.ts
@apps/time/src/objects/tt-department.object.ts
</context>

<tasks>
  <task type="auto">
    <name>Task 1: /s/-route расчёта загрузки</name>
    <files>apps/time/src/logic/capacity-data.logic.ts</files>
    <action>defineLogicFunction httpRouteTriggerSettings ('/capacity', isAuthRequired:true): для каждого ttDepartment и недели горизонта — ёмкость = headcount × норма(40) × capacityFactor; загрузка = сумма plannedHours проектов отдела, раскиданная по неделям периода (startDate..endDate); вернуть матрицу отдел×неделя {capacity, planned, percent}. CoreApiClient.</action>
    <verify>grep "capacity" apps/time/src/logic/capacity-data.logic.ts</verify>
    <done>Route возвращает матрицу загрузки</done>
  </task>
  <task type="auto">
    <name>Task 2: Front-доска</name>
    <files>apps/time/src/front/capacity-board.front.tsx, apps/time/src/front/hooks/use-capacity.ts</files>
    <action>defineFrontComponent: таблица отделы(строки) × недели/месяцы(колонки), ячейка = загрузка % с цветом (зелёный/жёлтый/красный). Клик по отделу → проекты, формирующие загрузку. Горизонт настраиваемый (3-6 мес). Русские заголовки. Привязать к пункту «Планирование».</action>
    <verify>grep -E "defineFrontComponent|capacity" apps/time/src/front/capacity-board.front.tsx</verify>
    <done>Доска показывает загрузку по отделам</done>
  </task>
</tasks>

<verification>
- [ ] Матрица отделы×недели, загрузка % с цветом
- [ ] Расчёт ёмкость/план корректен
- [ ] Доступна из пункта «Планирование»
</verification>

<success_criteria>Доска планирования показывает загрузку отделов вперёд</success_criteria>
