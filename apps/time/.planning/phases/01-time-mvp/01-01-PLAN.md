---
phase: 01-time-mvp
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/time/application-config.ts
  - apps/time/package.json
  - apps/time/src/shared/constants.ts
  - apps/time/src/shared/types.ts
  - apps/time/README.md
autonomous: false
requirements: [SETUP-01, SETUP-02, SETUP-03, SETUP-04]
must_haves:
  truths:
    - "Скаффолд SDK-app существует в apps/time, собирается"
    - "defineApplication задаёт displayName «Трудозатраты» и engines.twenty"
    - "constants.ts содержит русские ярлыки категорий/статусов/групп"
  artifacts:
    - "apps/time/application-config.ts с defineApplication"
    - "apps/time/src/shared/constants.ts (SSOT)"
  key_links:
    - "constants.ts ← types.ts (string-literal union)"
---

<objective>
Создать каркас SDK-приложения time в apps/time: скаффолд через create-twenty-app, манифест defineApplication, SSOT-константы (категории/статусы/группы — русские ярлыки) и типы. Подготовить локальную дев-среду (Twenty 2.x в Docker). Это фундамент для всех остальных планов.
</objective>

<context>
@research/twenty-sdk/fresh/getting-started/scaffolding.md
@research/twenty-sdk/fresh/getting-started/project-structure.md
@research/twenty-sdk/fresh/config/application.md
@research/twenty-sdk/fresh/getting-started/local-server.md
@docs/standards/DEV_STANDARDS.md
@docs/data-model/DATA_MODEL_SYNTHESIS.md
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Скаффолд приложения</name>
    <files>apps/time/</files>
    <action>Выполнить `npx create-twenty-app@latest` с целевой папкой apps/time (или скаффолд во временную и перенести содержимое в apps/time, сохранив наш README.md). Node 24+, Yarn 4 (corepack enable). Убедиться, что структура содержит application-config.ts, дефолтную роль, CI-workflow, integration-тесты.</action>
    <verify>ls apps/time/application-config.ts && cat apps/time/package.json | grep -i twenty</verify>
    <done>Скаффолд на месте, package.json ссылается на twenty-sdk</done>
  </task>
  <task type="auto">
    <name>Task 2: defineApplication + engines</name>
    <files>apps/time/application-config.ts, apps/time/package.json</files>
    <action>Настроить defineApplication: стабильный universalIdentifier (UUID-константа), displayName «Трудозатраты», description по-русски. В package.json добавить engines.twenty с semver-range целевой версии 2.x (например ">=2.1.0"). Версию приложения держать с дисциплиной (start 0.1.0).</action>
    <verify>grep -E "displayName|universalIdentifier" apps/time/application-config.ts; grep -A2 '"engines"' apps/time/package.json</verify>
    <done>Манифест валиден, engines.twenty задан</done>
  </task>
  <task type="auto">
    <name>Task 3: SSOT constants + types</name>
    <files>apps/time/src/shared/constants.ts, apps/time/src/shared/types.ts</files>
    <action>Создать SSOT: types.ts — string-literal union для WorkCategory (Client|Presale|Pilot|Internal|Infrastructure|Training), EntryStatus (Draft|Submitted|Approved|Rejected), ActivityGroup (production|projectManagement|presale|meetings|training|internal), BillingDocType (Order|Payment|Act), DepartmentCode (OV|OIB|OPIB|TC|OPR). constants.ts — маппинг код→русский ярлык для всех (по §2b/§7a синтеза) + список 5 отделов. Без хардкода строк в других файлах.</action>
    <verify>grep -c "Черновик\|На клиента\|Пресейл\|Самообучение" apps/time/src/shared/constants.ts</verify>
    <done>constants.ts содержит все русские ярлыки; types.ts — union-типы</done>
  </task>
  <task type="auto">
    <name>Task 4: Документировать дев-среду</name>
    <files>apps/time/README.md</files>
    <action>Дополнить apps/time/README.md: команды локальной разработки (локальный Twenty 2.x в Docker, `yarn twenty dev`, `yarn twenty dev --once --dry-run`), требования (Node 24, Yarn 4, Docker), ссылки на research/twenty-sdk/.</action>
    <verify>grep "yarn twenty dev" apps/time/README.md</verify>
    <done>README описывает запуск дев-среды</done>
  </task>
  <task type="checkpoint:human-verify" gate="blocking">
    <what-built>Каркас SDK-app time: скаффолд + манифест + SSOT</what-built>
    <how-to-verify>Проверить apps/time/application-config.ts (displayName «Трудозатраты»), constants.ts (русские ярлыки), запуск `yarn twenty dev --once --dry-run` без ошибок на локальном Twenty</how-to-verify>
    <resume-signal>Напишите "approved" или опишите проблему</resume-signal>
  </task>
</tasks>

<verification>
- [ ] apps/time/application-config.ts с defineApplication («Трудозатраты»)
- [ ] package.json: twenty-sdk + engines.twenty
- [ ] constants.ts/types.ts — SSOT, русские ярлыки
- [ ] dev --once --dry-run проходит
</verification>

<success_criteria>Скаффолд готов, манифест и SSOT на месте, дев-среда документирована, dry-run без ошибок</success_criteria>
