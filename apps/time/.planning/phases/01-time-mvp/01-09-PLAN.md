---
phase: 01-time-mvp
plan: "01-09"
type: execute
wave: 6
depends_on: ["01-03","01-04","01-05","01-06","01-07","01-08"]
files_modified:
  - apps/time/package.json
  - apps/time/.planning/dev-reports/
autonomous: false
requirements: [INSTALL-01, L10N-01, L10N-02]
must_haves:
  truths:
    - "Аудит локализации пройден: ноль англоязычных строк в UI/справочниках"
    - "App собирается, публикуется приватно и устанавливается в dev-workspace"
  artifacts:
    - "apps/time/.planning/dev-reports/001-*-install-poc.md"
---

<objective>
Финал MVP: аудит локализации (всё русское), сборка и установка приложения в dev-workspace (PoC), dev-report. Проверка end-to-end на реальном Twenty 2.x.
</objective>

<context>
@research/twenty-sdk/fresh/operations/cli.md
@research/twenty-sdk/fresh/operations/publishing.md
@research/twenty-sdk/fresh/operations/sync-and-recovery.md
@docs/standards/DEV_STANDARDS.md
@docs/data-model/DATA_MODEL_SYNTHESIS.md
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Аудит локализации</name>
    <files>apps/time/src</files>
    <action>Пройти по всем labels объектов/полей/опций SELECT, надписям front-компонентов, пунктам навигации. Убедиться: все пользовательские строки русские; коды enum латиницей только как идентификаторы. Исправить найденное. Проверить русскую локаль дат/дней недели в сетке и таймере.</action>
    <verify>grep -rIn -E "\b(Name|Status|Project|Submit|Approve|Save|Cancel)\b" apps/time/src/front apps/time/src/navigation | grep -iv "//\|import\|type\|const\|function" | head</verify>
    <done>Ноль англоязычных строк в UI</done>
  </task>
  <task type="auto">
    <name>Task 2: Сборка + публикация + установка</name>
    <files>apps/time/package.json</files>
    <action>Поднять локальный/целевой Twenty 2.x. `yarn twenty remote:add --url <server> --as dev`; `yarn twenty dev:build`; `yarn twenty app:publish --private`; `yarn twenty app:install`. Проверить engines.twenty совместимость. Версия package.json корректна. Если форк ещё пред-GA — установка на локальный Docker Twenty 2.x (дев), прод-форк по ходу (upstream-sync).</action>
    <verify>echo "ручная проверка: app виден в workspace, раздел «Трудозатраты» в сайдбаре"</verify>
    <done>App установлен, раздел в сайдбаре, сид-данные видны</done>
  </task>
  <task type="auto">
    <name>Task 3: Dev-report</name>
    <files>apps/time/.planning/dev-reports/001-2026-XX-XX-install-poc.md</files>
    <action>Написать dev-report по образцу CRM: цель, что построено (7 объектов, сетка, таймер, планирование, согласование, сид), файлы, ограничения, открытые v2-пункты. По-русски.</action>
    <verify>ls apps/time/.planning/dev-reports/</verify>
    <done>Dev-report создан</done>
  </task>
  <task type="checkpoint:human-verify" gate="blocking">
    <what-built>MVP модуля time, установленный в workspace</what-built>
    <how-to-verify>В workspace: раздел «Трудозатраты», недельная сетка (ввод/итоги), таймер, доска планирования (загрузка отделов), согласование вкл/выкл, сид-данные за янв-июнь 2026 (реальные ОВ + мок). Всё на русском.</how-to-verify>
    <resume-signal>Напишите "approved" или опишите проблемы</resume-signal>
  </task>
</tasks>

<verification>
- [ ] Локализация: ноль англ. строк в UI
- [ ] App собран, опубликован, установлен
- [ ] Раздел и все экраны работают на реальном Twenty
- [ ] Dev-report написан
</verification>

<success_criteria>MVP установлен и работает в workspace; всё на русском; dev-report готов</success_criteria>
