---
phase: 01-time-mvp
plan: "01-05"
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - apps/time/src/front/weekly-grid.front.tsx
  - apps/time/src/front/components/grid-row.tsx
  - apps/time/src/front/components/hour-cell.tsx
  - apps/time/src/front/hooks/use-week.ts
  - apps/time/src/logic/time-entry-crud.logic.ts
autonomous: true
requirements: [GRID-01, GRID-02, GRID-03, GRID-04, L10N-02]
must_haves:
  truths:
    - "Недельная сетка рендерится: строки проект/вид работ × дни недели, итоги, план/факт"
    - "Часы вводятся инлайн (decimal 0.5), сохраняются через /s/-route"
    - "Навигация по неделям и переключение сотрудника (для руководителя)"
  artifacts:
    - "apps/time/src/front/weekly-grid.front.tsx (defineFrontComponent)"
    - "apps/time/src/logic/time-entry-crud.logic.ts (httpRouteTriggerSettings /s/)"
  key_links:
    - "weekly-grid → time-entry-crud (/s/ route) через RestApiClient"
---

<objective>
Главный экран — недельная сетка ввода трудозатрат. Front-компонент (песочница) рендерит сетку проект/вид работ × дни недели с итогами и планом; сохранение записей — через /s/-route logic-функцию (front не имеет прямого доступа к БД). Русская локаль.
</objective>

<context>
@research/twenty-sdk/fresh/layout/front-components.md
@research/twenty-sdk/fresh/logic/logic-functions.md
@docs/data-model/DATA_MODEL_SYNTHESIS.md
@docs/data-model/CAPACITY_PLANNING.md
@apps/time/src/objects/tt-time-entry.object.ts
@apps/time/src/shared/constants.ts
</context>

<tasks>
  <task type="auto">
    <name>Task 1: /s/-route CRUD трудозатрат</name>
    <files>apps/time/src/logic/time-entry-crud.logic.ts</files>
    <action>defineLogicFunction с httpRouteTriggerSettings (path '/time-entry', isAuthRequired:true): операции list (по employee+неделя), upsert (date,hours,project,activity,stage,description), delete. Использовать CoreApiClient. userWorkspaceId → определить сотрудника (ttEmployee.workspaceMemberRef). Валидация часов (0..24, шаг 0.5).</action>
    <verify>grep "httpRouteTriggerSettings" apps/time/src/logic/time-entry-crud.logic.ts</verify>
    <done>Route-функция CRUD записей</done>
  </task>
  <task type="auto">
    <name>Task 2: Front недельная сетка</name>
    <files>apps/time/src/front/weekly-grid.front.tsx, apps/time/src/front/components/grid-row.tsx, apps/time/src/front/components/hour-cell.tsx, apps/time/src/front/hooks/use-week.ts</files>
    <action>defineFrontComponent: сетка строки (проект+вид работ) × 7 дней. HourCell — инлайн-редактирование decimal (шаг 0.5). Итоги по дню/строке/неделе, строка плана. use-week — навигация недель, русская локаль (дни недели, формат даты). Данные через RestApiClient на /s/time-entry. Дизайн-токены Twenty. Соблюсти лимиты размера (компоненты <150 строк — выносить в подкомпоненты/хуки).</action>
    <verify>grep -E "defineFrontComponent|RestApiClient" apps/time/src/front/weekly-grid.front.tsx</verify>
    <done>Сетка рендерится, ввод сохраняется</done>
  </task>
  <task type="auto">
    <name>Task 3: Переключение сотрудника + nav-пункт</name>
    <files>apps/time/src/front/weekly-grid.front.tsx, apps/time/src/navigation/</files>
    <action>Для руководителя — селектор сотрудника (фильтр по правам). Привязать компонент к пункту «Мой таймшит» (navigation/page-layout). Проверить, что сетка открывается из сайдбара.</action>
    <verify>grep -i "employee" apps/time/src/front/weekly-grid.front.tsx</verify>
    <done>Сетка доступна из навигации, есть переключение сотрудника</done>
  </task>
  <task type="checkpoint:human-verify" gate="blocking">
    <what-built>Недельная сетка ввода трудозатрат</what-built>
    <how-to-verify>Открыть «Мой таймшит» в сайдбаре, ввести часы в ячейку, проверить сохранение (перезагрузка) и итоги; надписи русские</how-to-verify>
    <resume-signal>Напишите "approved" или опишите проблему</resume-signal>
  </task>
</tasks>

<verification>
- [ ] Сетка проект/вид работ × дни, итоги, план
- [ ] Ввод decimal 0.5, сохранение через /s/-route
- [ ] Навигация недель, переключение сотрудника
- [ ] Русская локаль
</verification>

<success_criteria>Недельная сетка работает: ввод, сохранение, итоги; на русском</success_criteria>
