---
phase: 01-time-mvp
plan: "01-03"
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - apps/time/src/views/*.view.ts
  - apps/time/src/navigation/*.navigation.ts
  - apps/time/src/roles/tt-default.role.ts
  - apps/time/src/roles/tt-manager.role.ts
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]
must_haves:
  truths:
    - "В сайдбаре появляется папка «Трудозатраты» с пунктами"
    - "Каждый видимый объект tt* имеет дефолтный view (иначе невидим)"
    - "Есть одна defineApplicationRole (сотрудник) и defineRole (руководитель)"
  artifacts:
    - "apps/time/src/navigation/tt-folder.navigation.ts (FOLDER «Трудозатраты»)"
    - "apps/time/src/roles/tt-default.role.ts (defineApplicationRole)"
  key_links:
    - "navigation VIEW-пункты ← view.universalIdentifier"
---

<objective>
Сделать модуль видимым и управляемым по доступу: раздел-папка «Трудозатраты» в сайдбаре, дефолтные view для объектов (без view+nav объект невидим — критичное правило SDK), роли (сотрудник по умолчанию + руководитель/админ).
</objective>

<context>
@research/twenty-sdk/fresh/layout/navigation-menu-items.md
@research/twenty-sdk/fresh/layout/views.md
@research/twenty-sdk/fresh/config/roles.md
@docs/data-model/DATA_MODEL_SYNTHESIS.md
@apps/time/src/objects
</context>

<tasks>
  <task type="auto">
    <name>Task 1: Дефолтные views</name>
    <files>apps/time/src/views/</files>
    <action>defineView (ViewKey.INDEX) для видимых объектов: ttProject, ttTimeEntry, ttActivity, ttEmployee, ttDepartment. Колонки (fields) с русскими заголовками, position, isVisible. Операнды фильтров — по типу поля (иначе reject на sync).</action>
    <verify>ls apps/time/src/views/*.view.ts | wc -l</verify>
    <done>Views для всех видимых объектов</done>
  </task>
  <task type="auto">
    <name>Task 2: Навигация — папка «Трудозатраты»</name>
    <files>apps/time/src/navigation/</files>
    <action>defineNavigationMenuItem: FOLDER «Трудозатраты» (icon IconClock) + дочерние пункты типа VIEW: «Мой таймшит», «Проекты», «Согласование», «Планирование», «Справочники» (на справочники Activity/Department). Вложенность через folderUniversalIdentifier. position задать.</action>
    <verify>grep -E "FOLDER|Трудозатраты|folderUniversalIdentifier" apps/time/src/navigation/*.ts</verify>
    <done>Папка с пунктами в сайдбаре</done>
  </task>
  <task type="auto">
    <name>Task 3: Роли</name>
    <files>apps/time/src/roles/</files>
    <action>defineApplicationRole — дефолтная роль «Сотрудник» (read/write своих TimeEntry, read справочников/проектов). defineRole — «Руководитель» (read/write всех записей отдела, approve, управление проектами). objectPermissions/fieldPermissions по объектам tt*. Ровно одна defineApplicationRole.</action>
    <verify>grep -c "defineApplicationRole" apps/time/src/roles/*.ts</verify>
    <done>Дефолтная + руководительская роли</done>
  </task>
</tasks>

<verification>
- [ ] Папка «Трудозатраты» с пунктами в сайдбаре
- [ ] Каждый видимый объект имеет view
- [ ] Ровно одна defineApplicationRole + доп. defineRole
- [ ] dry-run без ошибок
</verification>

<success_criteria>Модуль виден в сайдбаре как раздел, объекты доступны, роли заданы</success_criteria>
