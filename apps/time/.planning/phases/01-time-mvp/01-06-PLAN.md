---
phase: 01-time-mvp
plan: "01-06"
type: execute
wave: 4
depends_on: ["01-02"]
files_modified:
  - apps/time/src/front/timer.front.tsx
  - apps/time/src/front/hooks/use-timer.ts
  - apps/time/src/logic/timer-stop.logic.ts
autonomous: true
requirements: [TIMER-01]
must_haves:
  truths:
    - "Таймер start/stop с выбором проекта и вида работ"
    - "По стопу создаётся ttTimeEntry через /s/-route"
  artifacts:
    - "apps/time/src/front/timer.front.tsx (defineFrontComponent)"
    - "apps/time/src/logic/timer-stop.logic.ts (/s/ route)"
---

<objective>
Таймер (Kimai-стиль): запуск/остановка с привязкой к проекту и виду работ; по остановке создаёт запись трудозатрат (часы из длительности, Decimal) через /s/-route.
</objective>

<context>
@research/twenty-sdk/fresh/layout/front-components.md
@research/twenty-sdk/fresh/logic/logic-functions.md
@apps/time/src/objects/tt-time-entry.object.ts
@apps/time/src/shared/constants.ts
</context>

<tasks>
  <task type="auto">
    <name>Task 1: /s/-route создания записи по таймеру</name>
    <files>apps/time/src/logic/timer-stop.logic.ts</files>
    <action>defineLogicFunction httpRouteTriggerSettings ('/timer-stop', isAuthRequired:true): принимает project, activity, durationSeconds, description → создаёт ttTimeEntry (hours = round(duration/3600, 0.25)) на текущую дату для сотрудника по userWorkspaceId. CoreApiClient.</action>
    <verify>grep "timer-stop\|httpRouteTriggerSettings" apps/time/src/logic/timer-stop.logic.ts</verify>
    <done>Route создаёт запись из длительности</done>
  </task>
  <task type="auto">
    <name>Task 2: Front-таймер</name>
    <files>apps/time/src/front/timer.front.tsx, apps/time/src/front/hooks/use-timer.ts</files>
    <action>defineFrontComponent: выбор проекта/вида работ, кнопки Старт/Стоп, отсчёт. use-timer — состояние и тик (без Date.now-ограничений песочницы — через performance/счётчик). По Стоп — POST /s/timer-stop. Русские надписи. Компактный (плавающий/в хедере раздела).</action>
    <verify>grep -E "defineFrontComponent|Старт|Стоп" apps/time/src/front/timer.front.tsx</verify>
    <done>Таймер работает, по стопу создаёт запись</done>
  </task>
</tasks>

<verification>
- [ ] Старт/стоп с проектом и видом работ
- [ ] По стопу создаётся ttTimeEntry (decimal часы)
- [ ] Русские надписи
</verification>

<success_criteria>Таймер создаёт корректную запись трудозатрат</success_criteria>
