# Находки аналитика — сводка для arch (durable)

**Автор:** аналитик AITEAM
**Обновлено:** 2026-06-21
**Назначение:** Единое durable-меню находок (правило arch №3 — не терять в SIGNALS LIFO). 5 слоёв анализа. arch берёт на доску `BACKLOG_BOARD.md` по приоритету. Каждый ID разворачивается в `requirements/REQ-*`.
**База:** реальный код `apps/time/src/front-components/`, `research/timetta-kimai-timesheet-views.md`, `research/timetta/`, `research/kimai/`, GAP-аудиты, STATUS/ROADMAP.

> Это меню, не требование. Сверять с референсами перед реализацией (правило 8). Статус триажа arch — в колонке «Решение».

---

## СЛОЙ 1 — Фичи/RICE (что разблокирует / дёшево / руководителю / прод)

### Блок A — разблокирует много, один корень (research current-user)
| ID | Что | Эффект | Решение arch |
|---|---|---|---|
| A1 | Research: доступ к текущему юзеру в front-component (хук/контекст Twenty) | КОРЕНЬ A2/A3/REQ-0014/CISO-005 | 🔴 ВЗЯТ (запущен) |
| A2 | isManager в таймшит из роли (сейчас `weekly-grid.tsx:25` хардкод `false`) | оживляет approval-workflow (мёртв в UI) + фильтр сотрудника | один заход по итогу A1 |
| A3 | REQ-0014 личный кабинет («Мои часы»+«Мои периоды») | 42 чел «слепые» | один заход по итогу A1 |

### Блок B — дёшевый фронт-полиш (база консистентности)
| ID | Что | Усилие | Решение arch |
|---|---|---|---|
| B1 | Слить 3 токена (grid/capacity/reports) → `shared/tokens` | ~0.5дн | 🔴 ВЗЯТ |
| B2 | `PeriodNav` один компонент (дубль: `grid/period-nav.tsx` vs инлайн `reports-dashboard.tsx:15`) | ~0.5дн | 🔴 ВЗЯТ |
| B3 | Дубль строки в режимы День/Проект (сейчас только Неделя) | ~0.5дн | 🟡 доска |
| B4 | Чистка мёртвого пропа `lastWorkTypeByProject` (`project-view.tsx:23`) | мелочь | 🔴 ВЗЯТ |

### Блок C — ценность руководителю (есть данные, нет UI)
| ID | Что | Источник | Решение arch |
|---|---|---|---|
| C1 | Экспорт CSV отчётов (F-F, нигде нет кнопки) | Kimai, заказчик | 🟡 доска |
| C2 | Массовое согласование (по отделу/всё) — всплывёт после A2 | Timetta | 🟡 доска |
| C3 | Себестоимость проекта Σ(часы×ставка) — базовее P&L | Timetta | 🟢 бэклог |
| C4 | Отчёт «Проекты×Месяцы» + тренд утилизации | Kimai | 🟢 бэклог |

### Блок D — качество данных / прод-готовность
| ID | Что | Когда критично | Решение arch |
|---|---|---|---|
| D1 | Детект аномалий ввода (16ч/день, выходной, дубли) | отчёты врут на мусоре | 🟢 бэклог |
| D2 | Архивация проектов/видов работ (селектор пухнет) | уже на 42 проектах | 🟢 бэклог |
| D3 | Audit log изменений согласованных записей | 152-ФЗ прод-гейт | 🟢 бэклог |
| D4 | HR-события датой (перевод/увольнение → ёмкость по дате) | прод | 🟢 бэклог |

---

## СЛОЙ 2 — UX-код-консистентность (противоречия в поведении)

| № | Находка | Файл | Решение arch |
|---|---|---|---|
| 1 | **isManager наоборот** — таймшит хардкод `false` (approval мёртв в UI), capacity показывает «Планировать» всем. Тот же корень что REQ-0014/A1 | `weekly-grid.tsx:25`, `approval-bar.tsx:120`, `use-capacity.ts:83` | 🔴 = A1/A2 |
| 2 | **Три системы токенов** (дрейф цветов/радиусов между экранами) | `grid/tokens.ts` + `capacity/cap-tokens.ts` + `reports/report-tokens.ts` | 🔴 = B1 |
| 3 | **PeriodNav переизобретён** инлайн в отчётах | `reports-dashboard.tsx:15` vs `grid/period-nav.tsx` | 🔴 = B2 |

Прочее: дубль строки только в Неделя (B3); нет «удалить строку»; экспорт нигде (C1); мёртвый проп (B4).

---

## СЛОЙ 3 — тех-здоровье/устойчивость

### Надёжность (сломается/повторится)
| ID | Что | Почему | Решение arch | Security-связка |
|---|---|---|---|---|
| T1 | Error-boundary на front-components (`shared/`) | P1-крэш (OLAP undefined.map) уронил весь виджет. Повторится | 🔴 ВЗЯТ | — |
| T2 | Норма — один источник (сервер) | `grid/format.ts` дублирует серверную норму → сетка≠дашборд | 🔴 ВЗЯТ | — |
| T3 | Оптимистичная блокировка периода (rowVersion) | 2 таба → тихая перезапись правок | 🟡 доска | **CISO-011** (одним спринтом) |
| T4 | Debounce/батч автосейва | шторм upsert при tab-навигации | 🟡 доска | — |
| T5 | Degraded-режим при частичном отказе REST | падение 1 fetch = весь экран мёртв | 🟡 доска | — |

### Архитектура/перф/a11y/тест
| ID | Что | Решение arch | Security-связка |
|---|---|---|---|
| T6 | `shared/ui` + слияние Segmented(2)/токенов(3)/PeriodNav(2) | 🔴 = B1/B2 | — |
| T7 | Виртуализация capacity-доски + мемо инлайн-стилей | 🟢 бэклог | — |
| T8 | Курсор-пагинация клиентских fetch reports/capacity | 🟢 бэклог | — |
| T9 | Цвет — единственный сигнал загрузки (дальтоники) → иконка/паттерн | 🟡 доска | — |
| T10 | Ноль e2e/UI-флоу (1034 unit — расчёты). P1 проскочил так | 🟢 трек QA | **CISO-006/007/008 guards только unit — integ до прода** |
| T11 | Нет телеметрии заполняемости | 🟢 бэклог | — |

> CISO-связка: A1 research явно проверить `userWorkspaceId` (нужен для server-actor CISO-005).

---

## СЛОЙ 4 — бизнес-ценность + орг-риски (как у референсов → внедрять ритмом)

### Блок E — система собирает часы, но не отвечает на вопросы бизнеса
| ID | Вопрос | Как у референсов | У нас |
|---|---|---|---|
| E1 | «Сколько зарабатываем на проекте?» | Timetta ставки×часы; Kimai rate-каскад+budget | часы есть, ставок НЕТ. **🟢 БЭКЛОГ — решение заказчика 2026-06-21: ставки пока НЕ вводим, отложено до RBAC/решения по ставкам → REQ-0002 P&L** |
| E2 | «Окупается ли пресейл?» | Timetta opportunity→booking | категории есть, результата сделки нет |
| E3 | «Кого взять на проект?» | Timetta ресурсный план + скиллы | «свободно ч» есть, связки навык↔человек нет |
| E4 | «Где утекает время?» | Kimai budget/timeBudget+алерт | нет benchmark норма vs факт по типу работ |

### Блок F — орг-риски внедрения (иначе данные-мусор → аналитика на песке)
| ID | Риск | Митигация (референсы) |
|---|---|---|
| F1 | Нет «зачем мне это» сотруднику (только контроль сверху) | Kimai/Timetta личный дашборд + переработки как основание. REQ-0014 частично |
| F2 | Учёт как наказание → рисуют 8/8/8 → мерим фикцию | культурная рамка + 152-ФЗ основание (CISO) |
| F3 | Двойной ввод (уже ведут в Директум5/задачах) → саботаж | референсы тянут из задач → ИМПОРТ из Директум5 = фактор принятия |

### Блок G — специфика Кредо-С (ИБ-интегратор)
| ID | Что | Зачем |
|---|---|---|
| G1 | Мост часы→акты выполненных работ (этапы оплаты договора) | ИБ-оплата по этапам. BillingLink есть, моста нет |
| G2 | Учёт аттестованных спецов на лицензируемых работах (ФСТЭК/ФСБ) | отчётность регулятору, гостайна |
| G3 | Тип «выездная работа»/командировка (ставка/компенсация) | ИБ = выезды. REQ-0006 табель |

### Блок H — стратегические рычаги (CRM+time в одном контуре)
- H1 прогноз загрузки × воронка CredosCRM · H2 бенчмарк норм ИБ-работ → оценка КП · H3 утилизация → обоснование найма.

---

## СЛОЙ 5 — ежедневное удобство ввода (рядовой сотрудник)

> Уже есть (не дублировать): клавнавигация, seed-печать, Shift+Enter заливка будней, подсветка «сегодня», warn>12ч, копи-неделя±часы, дубль строки (Неделя), default-activity, recent, инлайн-коммент (day), cheatsheet.

### Момент «открыл»
| ID | Что | Трение |
|---|---|---|
| UC1 | Автофокус 1-й пустой ячейки при открытии | сейчас клик мышью |
| UC2 | Запоминание контекста (режим/проект/неделя между сессиями) | сброс на «Неделя» |
| UC3 | Прогресс недели в шапке «32/40ч» + бар | не видно сколько добить |

### Момент «ввожу»
| ID | Что | Трение |
|---|---|---|
| UC4 | Гибкий формат `1:30`/`1ч30м`/`1h30` | `parseHours` (`format.ts:18`) только `1.5`/`1,5`. Люди думают в ч:мин |
| UC5 | «Добить до нормы» (день→8, неделя→40 остатком) | ручной подбор |
| UC6 | Range-select ячеек + один ввод (Excel) | только Shift+Enter всю строку |
| UC7 | «Копировать вчера» в day-режиме | в День ускорителя нет |

### Момент «проверяю/отправляю»
| ID | Что | Трение |
|---|---|---|
| UC8 | Бейдж-счётчик в навменю «N дней не заполнено / черновик не отправлен» | забывают сдать |
| UC9 | Pre-submit чеклист пробелов (= REQ-0015) | отправляют неполное вслепую |

### Момент «ошибка/возврат»
| ID | Что | Трение |
|---|---|---|
| UC10 | Дружелюбная ошибка + «Повторить» | голое «Не удалось загрузить: {error}» (`weekly-grid.tsx:98`) |
| UC11 | Онбординг пустого стейта (мини-пример) | первый вход непонятен |
| UC12 | Undo Ctrl+Z (повтор слоя 1) | страх ошибиться |

**Дёшево-вперёд:** UC1 + UC4 + UC10 — копеечные, бьют по ежедневному трению всех 42. Сверка: UC4 (Timetta формат Decimal/HH:MM), UC6 (Kimai QuickEntry), UC7 (Kimai duplicate), UC8 (Timetta dueDate-нудж).

---

---

## СЛОЙ 6 — Timetta API Intel (из postman 285 эндпоинтов + recon 2026-06-21)

> Дизайн-референсы из реального Timetta API. Не внедрять blindly — сверить с нашей моделью. Arch ADR при расхождении.

### Блок I — паттерны для нашего бэклога

| ID | Timetta pattern | URL/endpoint | Применение у нас | Когда |
|---|---|---|---|---|
| I1 | `GetIdForPeriod(date, userId)` | `GET /odata/TimeSheets/GetIdForPeriod(date=...,userId=...)` | logic-function хелпер «период→timesheet ID» для QuickTrack | W4 |
| I2 | `TrackTime` action | `POST /odata/TrackTime` | быстрый ввод без grid (мобильный/CLI) | W5+ |
| I3 | `BookingEntries/SwitchToSoft` + `SwitchToHard` | отдельные POST-actions | REQ-0009: Soft/Hard booking как state actions, не флаг | W5 |
| I4 | `ResourcePools` | CRUD + `GET /odata/ResourcePools` | пулы людей отдельно от Department — смотреть при REQ-0009 ресурс-план | W5 |
| I5 | `ActsOfAcceptance + $expand=lines + SetState` | 15 эндпоинтов | G1: `credosTimeActOfAcceptance` объект, lines→TimeEntry, state machine DRAFT→SENT→SIGNED | W6+ |
| I6 | `reporting.timetta.com/OData` (отдельный домен) | `GET reporting.timetta.com/OData/ResourcePlan` | W4-1 OLAP = отдельный aggregate endpoint, не inline | W4 ADR |
| I7 | `TimeSheets/{id}/SetState` | POST action | подтверждает нашу модель: approve через action, не PATCH status | уже правильно |

### Блок I2 — Round 2 intel (2026-06-22): P&L + Rate Matrix + Lifecycles

| ID | Что | Детали | Применение | Когда |
|---|---|---|---|---|
| I8 | **P&L алгоритм** | Баланс=Выручка−Затраты; 4 представления (План/Оценка/Факт/Прогноз); затраты=проводки(таймшиты+субподряд); CCR как % годовых | REQ-0002 дизайн: не просто «ставка×часы», а lifecycle проводок + CCR | E1 бэклог |
| I9 | **Rate Matrix best-match** | 7 аналитик: Role/Level/Grade/Competence/ResourcePool/Location/LegalEntity. **WorkType ∉ Rate Matrix** (подтверждено Round 6 — WorkType = категория отчётности, не финансовая аналитика). Приоритет: полное совпадение→базовая ставка→стандартная проекта. Billing Rate + Cost Rate. | REQ-0002 E1: ставка зависит от **Role** сотрудника на проекте, НЕ от WorkType записи. WorkType в ADR-E1 не использовать как аналитику ставки. Для Кредо-С минимум: Role+LegalEntity | E1 бэклог |
| I10 | **TimeSheet APPROVE lifecycle** | При APPROVE: проводки «Себестоимость труда» + «Себестоимость отсутствий» автоматически | Наш approve сейчас = только флаг. Future T3: lifecycle effects при approve (себестоимость) | W6+ |
| I11 | **Project Lifecycle** | Черновик→Согласован→Архивирован(read-only)/Отменён(read-only) | D2 архивация: read-only после archive = правильный паттерн | W5 |
| I12 | **Deal Lifecycle** | Новая→Квалификация→Переговоры→Выиграно/Проиграно (финальные read-only) | E2 (пресейл окупается?): связать Deal.result с hours пресейла | W5+ |
| I13 | **WBS AI Agent** | `ai-wbs-agent-service.ai.svc.cluster.local/WBSAgent/Run` (mode: full/quick) | H2 (бенчмарк норм ИБ-работ) — AI планирование WBS как паттерн | H-блок |

**ADR-совет (Rate Matrix):** когда придём к REQ-0002 P&L — зафиксировать в ADR: модель Rate Matrix (best-match, мин. 2 аналитики Role+LegalEntity) вместо простого поля ставки. Это позволит позже добавить Competence/Level без ломающего изменения схемы.

### Что НЕ берём (не наш домен сейчас)
- **Invoices** — бэклог (G1 prerequisite)
- **Accounting Entries/Periods** — E1 бэклог (ставки не вводим до RBAC)
- **Issues** — task tracking не наш домен
- **Programs** — иерархия проектов слишком рано
- **Time Off Requests** — не в roadmap
- **Certificates** — не применимо

### Auth-паттерн (для будущей интеграции Директум5)
`client_id=external` + password grant → Access Token 1ч / Refresh 15д / API Token 1год (`/settings/api-tokens`). Когда понадобится pull из Директум5 или push в 1С.

### Блок I3 — Round 4 + Round 5 intel (2026-06-22)

| ID | Что | Детали | Применение | Когда |
|---|---|---|---|---|
| I14 | **AI-бот архитектура Timetta** | Модель: **qwen3-235b** (Alibaba/Qwen, НЕ YandexGPT). Инфра: Yandex Cloud ALB. 3 режима: «без источников» / «с справкой/Wiki» / «с сущностями Timetta». System prompt защищён через инфра-прокси ([END OF FIXED MESSAGE]+UUID). Уязвимость: temp=0.0 + эмоциональный фрейм = экстракция prompt | Дизайн-референс для будущего AI-ассистента Кредо-С: 3-режимная архитектура, защита на инфра-уровне, НЕ только на промпт-уровне | H-блок |
| I15 | **Project финансовые поля** ($metadata Round 5) | `billingMode` (T&M/фикс/NTE), `corporateTaxRate`, `billingDeferment`, `collectionDeferment`, `billingAccumulationPeriod`, `isAccrueCapitalCharge`. + флаги: `allowTimeEntry` (запрет записей), `skipManagerApprove` (проект-уровень approval skip), `isAutoPlanning`. 3 даты: `startDate` / `plannedStartWorkDate` / `estimatedStartWorkDate` | REQ-0002 P&L: `billingMode`+`corporateTaxRate`+`billingDeferment` — параметры проекта влияют на P&L. REQ-0016 (бэклог): `allowTimeEntry`+`skipManagerApprove`. D2 архивация: `allowTimeEntry`=false при архиве | E1/REQ-0002 |
| I16 | **ActOfAcceptance дата-модель** (Round 5) | **Act агрегирует НЕСКОЛЬКО таймшитов** (не 1:1!). Lifecycle 5 состояний: Черновик→На согласовании→Согласовано→Признан/Отменён. Header: project, client, period, totalAmount, `amountBC` (базовая валюта), currency. Lines (ActLine): act, entry/role, hours, rate, amount, `exchangeRate`, `accountId` (финансовая статья). 74 поля + 23 связи | **G1 «Акты»**: модель `credosTimeAct` (header) + `credosTimeActLine` (строки). Акт = документ за месяц/этап, агрегирует N недель. Lifecycle переиспользует наш approval-паттерн. `accountId` → связь с P&L (I8). `amountBC` → мультивалюта с первого дня | G1 W6+ |
| I17 | **ProjectVersion / Baseline** (Round 5) | `sourceProjectId` — snapshot проекта. Baseline = publish-момент (план заморожен при согласовании). Использован и для клонирования проектов (`sourceProjectId` в копии) | D3 (audit-log/версионирование): паттерн «заморозить план при согласовании бюджета». При P&L: baseline = точка сравнения план/факт | D3 W7+ |

### Блок I5 — Round 7 подтверждения (2026-06-22): RateMatrix API + FinancialAccount + Act-архитектура

| ID | Что | Детали | Применение | Когда |
|---|---|---|---|---|
| I22 | **RateMatrixLine — 9 полей подтверждено** | $metadata + API + бот сошлись. Поля: `roleId`, `legalEntityId`, `level`, `grade`, `competence`, `resourcePoolId`, `locationId` (7 аналитик) + `rate`, `effectiveDate/expiryDate`. На стенде Timetta: только `roleId + legalEntityId` заполнены (остальные null). Билл. Rate: 3 строки; Cost Rate: 8 строк | **MVP подтверждён**: `credosTimeRate` = {roleId, legalEntityId, costRate, billingRate, effectiveDate}. 2 типа ставки на одной строке — не разносить в разные объекты. Остальные 5 аналитик = nullable, добавить позже без breaking change | E1 бэклог |
| I23 | **FinancialAccount — 12 дефолтных, 6 системных** | isSystem=true (6): DirectLabor, Revenue, TimeOffCost, SubcontractorLaborCost, CapitalCharge, CorporateTax. isSystem=false (6): MTRL, CNTR, TRVL, TRNS, ENTRT, RISK. `includedInBalance` — участвует в P&L-балансе. Типы: Revenue(REV)/Cost(COS)/Expenses(EXP)/Other(OTH) | G1: ActLine.financialAccountId → «DirectLabor» при approve таймшита (COS). «Revenue» ← из актов выполненных работ. «CapitalCharge»/«CorporateTax» = CCR/налог из I8. Готовый chart-of-accounts для REQ-0002 P&L | G1/REQ-0002 |
| I24 | **TimeSheet→Act: НЕ авто, прямой FK отсутствует — подтверждено $metadata** | Акт создаётся вручную. `TimeSheetLine` ↔ `ActLine` — **прямой FK НЕТ** (подтверждено через $metadata Round 8). Единственная связь — общий `projectTaskId` (WBS). Часы из таймшита в акт НЕ копируются/не подтягиваются автоматически. Пользователь вводит часы в акт отдельно. | **ВЫВОД для G1**: наша модель — NOT дефектная относительно Timetta, а ПРОЩЕ. `credosTimeActLine` = {projectId, workTypeId, period, quantity, rate, amount}. Нет FK к timesheet — намеренно. UX-опция: «рассчитать из записей» = pre-fill quantity без сохранения FK. Зафиксировать в G1-spec как explicit design decision. | G1 W6+ |
| I25 | **RateBC — авто-конвертация в базовую валюту** | Rate в базовой валюте рассчитывается автоматически по курсу на `effectiveDate` записи | Для Кредо-С: всё в RUB — не блокирует. При мультивалюте (будущее): `amountBC` в ActLine (I16) = `amount × exchangeRate` на дату. Хранить `currency` + `exchangeRate` с первого дня | G1/future |

### Блок I6 — Round 8 (2026-06-22): Invoice + UX/UI flows + ключевые «НЕТ»

| ID | Что | Детали | Применение | Когда |
|---|---|---|---|---|
| I26 | **Invoice: 84 поля + InvoiceLine: 28 полей** | Invoice = документ выставления счёта клиенту (после Act). InvoiceLine = строка счёта. Invoice→Act связь существует. Полный финансовый пайплайн: TimeEntry → TimeSheet → Act → Invoice | G1→Invoice пайплайн: `credosTimeInvoice` → отдельный объект после G1. InvoiceLine.quantity + rate + amount аналогичны ActLine. Не смешивать с Act — разные lifecycle | G2 (после G1) |
| I27 | **TIMESHEET_UX_FLOW.md — 5 UX-блоков задокументированы** | (1) Заполнение сетки: ячейка=день×проект, ввод часов, быстрый ввод строки, копирование прошлой недели. (2) Менеджер: 4 цвета статусов (черновик/отправлен/одобрен/отклонён), фильтр по сотруднику/периоду, batch-approve. (3) Личный кабинет: мои часы + недели со статусами. (4) Акт — 2 способа создания (см. I28). (5) Capacity Board: таблица загрузки + тепловая карта | UX-референс для: approval-bar (4 цвета), REQ-0014 личный кабинет, G1 wizard, capacity-board тепловая карта (будущее) | фронт |
| I28 | **Акт: 2 способа создания (ручной процесс, НЕ авто)** | Способ 1: вручную строку за строкой (выбор проекта+WorkType+период+часы). Способ 2: «рассчитать из записей» = pre-fill из Σ entries (НО: FK не сохраняется, это helper ввода). Act НЕ авто-агрегирует таймшиты. | G1 UX: кнопка «Заполнить из записей» = pre-fill quantity, пользователь корректирует и сохраняет. Не хранить FK на записи (I24 подтверждён). | G1 |
| I29 | **WorkType НЕ маппится на FinancialAccount (ручной выбор) — КРИТИЧНО для G1** | Пользователь при создании ActLine вручную выбирает FinancialAccount из справочника. Нет авто-маппинга WorkType→Account. | **МЕНЯЕТ G1 дизайн**: ActLine нужен `financialAccountId` selector в UI (dropdown из 12 счетов). Нельзя делать авто-подстановку по WorkType. Default: DirectLabor (пользователь меняет если нужно). | G1 |
| I30 | **Автосогласование НЕ поддерживается** | Только ручной воркфлоу: сотрудник → отправляет → руководитель → одобряет/отклоняет. Нет cron/auto-approve. Lazy creation таймшитов (по запросу, не кроном) — подтверждён I11. | Наша реализация верна (ручной approval-bar). Auto-approve не делать. Таймшит lazy creation — уже в плане. | REQ-0003 |

**ADR-совет (G1):** ActOfAcceptance не является простой распечаткой таймшита — это агрегированный финансовый документ. `credosTimeAct` нужен отдельный объект, не вычисляемый из entries. Lifecycle = 5 состояний (как approval, не как timesheet). `ActLine.accountId` — статья затрат для P&L bridge (I8+I16 связка).

### Блок I4 — Round 6 подтверждения (2026-06-22)

| ID | Что | Детали | Применение | Когда |
|---|---|---|---|---|
| I18 | **WorkType ∉ Rate Matrix** | WorkType = категория работ для отчётности/классификации. Ставка определяется только по **Role** сотрудника. WorkType не является аналитикой Rate Matrix | Скорректировать ADR-E1: WorkType не участвует в выборе ставки. Наш `workTypeId` — отчётная метка, не тариф | E1 бэклог |
| I19 | **Lazy creation таймшитов** | Таймшит создаётся при ПЕРВОМ открытии периода (не крон-джобом, не заранее). Аналог: lazy initialization | UC-auto: при открытии недельного grid — проверить/создать таймшит для периода. Дёшево, без фоновых задач. Dev2 зона (logic-function) | W-next |
| I20 | **Непрерывность timeline — спец-периоды** | Разрывы в таймшит-timeline (нет активности N недель) закрываются специальными периодами (тип: closure/absence-period). Обеспечивает целостность отчётного периода | G1: акт за месяц может содержать «пустые» недели — учесть при агрегации ActLines. Разрыв ≠ ошибка. При подсчёте часов за период пропущенные недели = 0ч, не null | G1 W6+ |
| I21 | **Авто-согласование: НЕТ** | Только ручной воркфлоу. `skipManagerApprove` = флаг «пропустить шаг согласования» на проекте, НЕ «авто-апрув» | Наш approval flow правильный. `skipManagerApprove` (I15/REQ-0016) = пропуск конкретного шага, статус не меняется автоматически | REQ-0016 |

---

## Сквозные приоритеты (рекомендация аналитика)
1. 🔴 **A1 research current-user** — линчпин (approval+личный кабинет+CISO-005). ВЗЯТ.
2. 🔴 **T1 error-boundary + T2 норма-один-источник** — дёшево, бьют по случившемуся/доверию. ВЗЯТЫ.
3. 🔴 **B1/B2 shared/ui** — база консистентности. ВЗЯТЫ.
4. ~~E1 ставки→рентабельность~~ → 🟢 **БЭКЛОГ** (решение заказчика 2026-06-21: ставки пока не вводим, до RBAC).
5. 🔴 **F орг-риски** — без отдачи сотруднику данные фиктивны = ROI в ноль (внедренческое, не код).
6. 🔵 Дёшевый ежедневный UX: UC1/UC4/UC10.
