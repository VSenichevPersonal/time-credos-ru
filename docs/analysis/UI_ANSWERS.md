# UI-ответы аналитика — блоки A · B · C · D

**Дата:** 2026-06-21  
**Автор:** Аналитик AITEAM (итерация 95)  
**Статус:** РЕШЕНИЯ ДЛЯ ЗАКАЗЧИКА (ответы на UI_QUESTIONS_A/B/C/D)  
**Формат:** `**X.Y** → (вариант) «обоснование» [ссылка/ограничение]`

Сокращения: Ref=T (Timetta), Ref=K (Kimai), RemDOM=ограничение Remote DOM, CISO=ограничение безопасности, NB=no-billable.

---

## A1. Таймшит — тулбар и строки

**A1.1** → **(а)** оставить 3 режима. Week = основной, Day = фокус-ввод, Project = агрегат. Мёртвых нет. Ref=T.

**A1.2** → **(а)** сохранять выбранную неделю/день при переключении режима. Ref=T: период сохраняется между видами.

**A1.3** → **(а)** группировка строк по проекту внутри той же недели. Не отдельная таблица — так виден контекст временного периода.

**A1.4** → **(а)** как сейчас, слева у заголовка. Сегмент-контрол по центру (б) дальше от ‹ › кнопок навигации.

**A1.5** → **(а)** «Сегодня» = текущий период активного режима. В Неделю — текущая неделя; в День — сегодня. Ref=T.

**A1.6** → **(в)** клик по заголовку = datepicker (выбор недели/дня), отдельная кнопка «Сегодня» остаётся. Ref=K/T.

**A1.7** → **(б)** datepicker достаточно (решено через A1.6). Отдельный список недель — избыточно.

**A1.8** → **(а)** «13–19 янв 2026». Диапазон дат — понятнее номера недели. Ref=T.

**A1.9** → **(а)** всегда прошлая неделя. Простота, соответствует Ref=T «Копировать предыдущую неделю».

**A1.10** → **(в)** дефолт «Строки и часы» (со часами) — чаще нужно именно это.

**A1.11** → **(б)** «Очистить неделю» вынести из CopyMenu. Деструктивное действие внутри меню копирования — UX-ловушка. Место: отдельная кнопка с confirm или в «Прочее».

**A1.12** → **(б)** норма дня через `useDailyNorm` — SSOT. `DAILY_NORM_HOURS=8` хардкод убрать. Ref=T «расставить по расписанию» = норма из графика.

**A1.13** → **(а)** «Заполнить неделю для всех» — решено в TIMESHEET_CELL_CONTROLS §F5.

**A1.14** → **(а)** только пустые будни. Не перезаписывать введённое — Ref=K/T.

**A1.15** → **(а)** производственный календарь (`isNonWorkingDay`). `isWeekend` хардкод убрать. Ref=K: рабочая суббота считается.

**A1.16** → **(в)** согласовать финальный список 1:1 с осями OLAP-отчётов. Минимально добавить «Группа видов работ» как 8-ю ось.

**A1.17** → **(б)** мультиселект-чипы для всех фильтров. Ref=T: мультиселект в фильтрах.

**A1.18** → **(а)** ряд пилюль с ✕ + «Сбросить всё». Переиспользовать `filter-pills.tsx` из Отчётов.

**A1.19** → **(а)** фильтр «Сотрудник» только для manager. CISO-007 минимизация ПДн.

**A1.20** → **(а)** фильтры = только отображение строк. Действия (заполнить/копировать) по скрытым строкам — риск нечаянного изменения.

**A1.21** → **(в)** запоминать последний набор фильтров (localStorage/settings). Минимальная инвестиция, ощутимый UX-выигрыш.

**A1.22** → **(б)** проект обязателен при AddRow, вид работ — опционально. Ref=K: запись валидна с проектом, вид дозаполняется.

**A1.23** → **(в)** спрашивать при дублировании строки: «с часами?». Даёт контроль без угадывания.

**A1.24** → **(а)** «Заполнить будни» в RowMenu = норма дня везде. Единый SSOT.

**A1.25** → **(а)** добавить «Комментарий к записи…» в RowMenu. Кнопка ✎ в ячейке недостижима мышью (баг onClick+onMouseDown).

**A1.26** → **(в)** переименовать: «Обнулить часы» и «Убрать строку». Разница очевидна.

**A1.27** → **(а)** кнопка ⋯ слева фиксированная. RemDOM: hover ненадёжен, правый клик рискован.

**A1.28** → **(в)** и футер-тоталы (`footer-totals.tsx`), и индикатор «факт/норма» в шапке периода. Ref=T.

---

## A2. Планирование — доска

**A2.1** → **(а)** планирование только в срезе «Отделы» — MVP. Per-person assignment (б) — roadmap v2. Зафиксировать как ограничение.

**A2.2** → **(в)** проекты видны через раскрытие отдела — как сейчас. Третий срез «Проекты» откладываем.

**A2.3** → **(а)** показывать подсказку «В режиме планирования доступен только срез «Отделы»». Молчать (в) — непонятно.

**A2.4** → **(в)** FTE% в тултипе строки сотрудника. Не загромождать ячейку.

**A2.5** → **(б)** загрузка% + число свободных часов вместе в ячейке. Ref=T heatmap + tooltip с деталями.

**A2.6** → **(а)** метрика по умолчанию — Загрузка%. Ref=T.

**A2.7** → **(а)** пороги ±5/15% оставить. При необходимости — вынести в настройки позже.

**A2.8** → **(в)** тонкая полоса факта под ячейкой плана. Не перегружает, даёт сравнение. Ref=T actuals overlay.

**A2.9** → **(б)** серая ячейка «нет ёмкости» при `capacity=0`. «—» (а) менее информативно.

**A2.10** → **(а)** переключатель SOFT-броней прямо в `board-toolbar`. Ref=T: показывать/скрывать предварительные.

**A2.11** → **(а)** иконка (▪/пунктир) + добавить подпись «предв.» на SOFT-маркер.

**A2.12** → **(в)** кнопка «+ бронь» в строке отдела/сотрудника → форма. RemDOM: drag невозможен. Ref=T (у них drag, у нас — клик).

**A2.13** → **(а)** кнопка «Подтвердить бронь» (SOFT→HARD) на карточке брони. Важный workflow пресейла.

**A2.14** → **(а)** разрешать overbooking + warning (обводка+▲). Ref=T: мягкое предупреждение.

**A2.15** → **(в)** счётчик перегрузов в легенде. Баннер (а) агрессивен при нормальной работе.

**A2.16** → **(а)** 3 уровня достаточно (отдел → проекты → доли по отделам).

**A2.17** → **(а)** кнопка «Развернуть всё / Свернуть всё» в тулбаре.

**A2.18** → **(б)** часы per-период (per-неделя) — гибче, ближе к Ref=T.

**A2.19** → **(в)** `startDate` = начало текущего периода при первом вводе. Предсказуемее «сегодня».

**A2.20** → **(а)** валидация `endDate ≥ startDate` + подсветка просроченных проектов.

**A2.21** → **(а)** autosave + SaveIndicator — как сейчас. Явная кнопка (б) ломает скорость работы.

**A2.22** → **(а)** добавить ‹ › навигацию горизонта. Ref=T: прокрутка горизонта.

**A2.23** → **(б)** добавить квартал. Год — избыточен для этого инструмента.

**A2.24** → **(б)** добавить кнопку «Сегодня» / «К текущему периоду».

**A2.25** → **(а)** по умолчанию 8 недель вместо 16. Нет горизонтального скролла в виджете.

**A2.26** → **(б)** «Планировать» доступно и руководителю проекта (не только isManager). Уточнить матрицу ролей с заказчиком.

**A2.27** → **(б)** явный «Выйти из планирования» вместо toggle. Toggle-тумблер непонятен как режим.

**A2.28** → **(а)** сразу PATCH. Черновик плана — сложность v2. MVP: inline-ввод сразу сохраняется.

**A2.29** → **(а)** клик → форма «Перенести на период X». RemDOM: drag недоступен.

**A2.30** → **(в)** роль-заглушка «позже». Требует схемных изменений (employee NOT NULL), выходит за MVP.

---

## A3. Отчёты (OLAP)

**A3.1** → **(а)** добавить 5-ю вкладку «Табель Т-13». Табель готов, нужен только вход.

**A3.2** → **(а)** «Незаполненные» — вкладка отчётов. В Согласовании — другой контекст (решение руководителя).

**A3.3** → **(б)** настройка N месяцев для тренда. «Синхрон с периодом» (в) сломает смысл тренда.

**A3.4** → **(а)** «Проекты» и срез «Проект» — разное: вкладка даёт план/факт/остаток, срез — OLAP разбивку.

**A3.5** → **(а)** 7 рабочих осей. stage без справочника — не показывать в UI.

**A3.6** → **(в)** preferred-путь по умолчанию + кнопка «Развернуть по…» для выбора оси. Ref=T: произвольная последовательность.

**A3.7** → **(в)** stage — позже, когда будет справочник этапов в фронте.

**A3.8** → **(а)** drill на месте (стек + крошки). Отдельная страница (б) теряет контекст.

**A3.9** → **(б)** «Все › … › Текущий» — схлопывать средние уровни при глубоком drill.

**A3.10** → **(б)** кнопка «+ фильтр» с выбором оси/значения. Плюс drill как сейчас. Ref=T: независимые фильтры + drill.

**A3.11** → **(а)** точечное снятие фильтра: ✕ убирает только этот фильтр, не откатывает уровень.

**A3.12** → **(а)** крошки = навигация (стек уровней), пилюли = кросс-фильтры (независимые). Разная семантика, держать раздельно.

**A3.13** → **(б)** «Клиентское vs внутреннее» (по категории проекта). NB: «Биллируемость» запрещена.

**A3.14** → **(а)** тултип с формулой «client_h / fact_h × 100%». Термин «Утилизация» оставить.

**A3.15** → **(а)** дельта к прошлому периоду (▲▼%) в KPI-карточках.

**A3.16** → **(а)** «—» при срезе Проект для карточки «Норма ч».

**A3.17** → **(а)** добавить неделю в период отчётов. P2-фикс, уже реализован для drill-вывода.

**A3.18** → **(а)** смена среза сбрасывает drill — ожидаемо. Drill привязан к конкретному значению оси.

**A3.19** → **(в)** запоминать последний период per-раздел.

**A3.20** → **(а)** datepicker диапазона from–to. CSV уже поддерживает произвольный диапазон, UI отстаёт.

**A3.21** → **(б)** генерить файл на бэке → ссылка для скачивания. RemDOM: Blob/download недоступны. Приоритет.

**A3.22** → **(б)** чекбоксы групп полей перед экспортом.

**A3.23** → **(а)** без финансов. NB: no-billable-concept.

**A3.24** → **(а)** 5-я вкладка «Табель» в дашборде отчётов.

**A3.25** → **(а)** toggle часы/коды + **(в)** только коды для официального бланка. Оба режима нужны.

**A3.26** → Конфликт CISO-007 vs юридическое требование Т-13. Решение: отдельное право «Кадры» — только его носители экспортируют Т-13 с ФИО. По умолчанию — код. Согласовать с CISO.

**A3.27** → **(а)** иллюстрация + текст + кнопка-действие. Impeccable: empty states = onboarding moment.

**A3.28** → **(а)** скрытие ФИО (код) — до поступления identity в payload (CISO-005 P1).

---

## A4. Согласование

**A4.1** → **(а)** submit период целиком. Ref=T: таймшит = период.

**A4.2** → **(а)** флаг `approvalRequired` на проекте. Гибкость: не все проекты требуют.

**A4.3** → **(в)** SUBMITTED редактируемо + кнопка «Отозвать отправку» (SUBMITTED→DRAFT). Ref=T: recall до approve.

**A4.4** → **(а)** да, кнопка «Отозвать» для сотрудника (пока не открыт руководителем).

**A4.5** → **(в)** кнопка Submit и в футере таймшита, и в «Мои периоды».

**A4.6** → **(а)** предупреждать + разрешать (мягкое). Ref=T ValidationRules: soft block.

**A4.7** → **(б)** факт < норма минус подтверждённые отсутствия. Больничный/отпуск не должен давать «недобор».

**A4.8** → **(а)** единый источник нормы SSOT (`useDailyNorm`). Совпадать с «Заполнить 8×5».

**A4.9** → **(а)** предупреждать о ПЕРЕРАБОТКЕ (факт > норма×1.5?) при submit. Обе проверки.

**A4.10** → **(в)** per-сотрудник в очереди руководителя. Approve всех SUBMITTED одного сотрудника разом. Ref=T.

**A4.11** → **(а)** отдельный экран-очередь руководителя (View `credos-time-approval`). Ref=T.

**A4.12** → **(б)** причина отклонения — на сотрудника (не на весь батч, не на строку).

**A4.13** → **(а)** нет частичного reject. Весь submit одного сотрудника — единое решение.

**A4.14** → **(а)** комментарий только при reject — как сейчас.

**A4.15** → **(а)** toast «Согласовано N, пропущено M (SoD/ошибки)».

**A4.16** → **(а)** подсказка «Ваши записи будут согласованы вышестоящим». Прозрачность SoD.

**A4.17** → **(а)** согласует вышестоящий руководитель по иерархии отделов.

**A4.18** → **(а)** строгий SoD в проде. CISO: SoD обязателен, dev-fallback только в dev-среде.

**A4.19** → **(а)** полный RBAC по роли + иерархии. isManager — UX-гейт, но нужен серверный гейт.

**A4.20** → **(а)** 4 статуса достаточно. +REVOKED добавить при реализации отзыва (A4.25).

**A4.21** → **(б)** агрегат-бейдж + счётчики «3 одобрено, 1 отклонено».

**A4.22** → **(а)** цвета везде согласованно: бейдж периода + точки строк + «Мои периоды».

**A4.23** → **(а)** баннер над таймшитом при REJECTED. Плюс **(б)** в «Мои периоды». Оба места.

**A4.24** → **(а)** замок-иконка 🔒 на ячейках. Уже есть в коде.

**A4.25** → **(а)** кнопка «Отозвать согласование» (APPROVED→SUBMITTED/DRAFT). Только manager/admin. Ref=T: Reopen.

**A4.26** → **(а)** отзыв + **(б)** корректирующая запись для случаев без отзыва.

**A4.27** → **(б)** кнопка «Создать акт» — ручной в первой фазе, авто — после интеграции 1С.

**A4.28** → **(а)** часы по клиенту/проекту за период + **(в)** форму Акта согласовать с бухгалтерией Директума.

**A4.29** → **(в)** и in-app (колокольчик), и email. Ref=T/K: email-уведомления о статусах.

**A4.30** → **(а)** единый механизм напоминаний — связать с «Незаполненные» + `reminderDayOfWeek`.

---

## B1. Личный кабинет «Мои трудозатраты»

**B1.1** → **(а)** две вкладки (Мои часы / Мои периоды), Согласование — отдельный пункт меню. Ref=T.

**B1.2** → **(а)** CTA-кнопка в баннере → открывает «Записи» на проблемной неделе.

**B1.3** → **(а)** строка в «Мои периоды» кликабельна → таймшит этой недели. Ref=T.

**B1.4** → **(а)** текущая неделя по умолчанию в «Мои часы».

**B1.5** → **(б)** добавить +статус согласования («На согласовании X ч», «Согласовано Y ч»).

**B1.6** → **(б)** «Перегруз X ч» / «Недобор X ч» словами вместо знаков ±. Чище.

**B1.7** → **(в)** число + цвет по порогу (например <70% = жёлтый, <50% = красный).

**B1.8** → **(б)** клик на KPI → «Отчёты» с пред-фильтром по сотруднику + периоду.

**B1.9** → **(г)** показывать только за закрытые/прошедшие дни недели. Не ругать за понедельник в среду.

**B1.10** → **(б)** текущая + N прошлых незаполненных недель.

**B1.11** → **(в)** success + кнопка «Отправить на согласование» при заполненной неделе.

**B1.12** → Да: кнопка в баннере → конкретная неделя с недобором.

**B1.13** → **(б)** дайджест: незаполнившие + «На согласовании у меня N».

**B1.14** → **(б)** руководитель видит ФИО своей команды независимо от `revealEmployeeNames` — законный обработчик. CISO согласовать.

**B1.15** → **(в)** кнопка «Открыть таймшит сотрудника» (при наличии прав).

**B1.16** → **(в)** дайджест руководителя — отдельная вкладка «Команда» в ЛК.

**B1.17** → **(б)** бейдж «согласовано N из M» вместо чисто агрегатного статуса.

**B1.18** → **(б)** «N отклонено» + раскрытие списка причин по строкам.

**B1.19** → **(б)** фильтр по году/кварталу. 26 недель — минимум, нужно смотреть прошлые.

---

## B2. Карточки сущностей и реестры

**B2.1** → **(в)** редкие вкладки (Связи с 1С, Брони) за «…ещё». 10 вкладок = перегруз.

**B2.2** → **(а)** Сводка открывается первой — она даёт текущий статус проекта.

**B2.3** → **(а)** держать раздельно: Сводка = метрики+прогресс, Обзор = реквизиты.

**B2.4** → **(а)** добавить вкладку «Обзор» для сотрудника (ФИО/должность/email/код).

**B2.5** → **(а)** добавить вкладку «Загрузка» в карточку отдела (матрица из REQ-0016).

**B2.6** → **(б)** только инлайн-таблица (отфильтрованная по родителю). RECORD_TABLE без фильтра = confusion. Ref=T.

**B2.7** → **(в)** убрать RECORD_TABLE из карточек, оставить только инлайн-relation.

**B2.8** → **(б)** сократить: убрать контекстные колонки (в карточке проекта — «Проект», в карточке сотрудника — «Сотрудник»).

**B2.9** → **(а)** скрывать колонку-родителя в инлайн-реестре контекстно.

**B2.10** → **(а)** принять двойной переход (нативно). Добавить front-component «Проекты, где работал» в сотрудника — отдельная задача.

**B2.11** → **(б)** разделить реквизиты на «Основное» / «Плановое» / «Прочее».

**B2.12** → **(а)** добавить TEXT-поле `name`, заголовок карточки = name, code = подзаголовок. Ref=T.

**B2.13** → **(а)** добавить TEXT-поле `name` для отдела, заголовок = name.

**B2.14** → **(б)** добавить вычисляемые метрики (Σ FTE, активные проекты) через front-component.

**B2.15** → **(а)** текущий набор метрик Сводки достаточен для MVP.

**B2.16** → **(а)** только часы в «Бюджете». NB: no-billable-concept.

**B2.17** → **(в)** две секции в «Команде»: «Забронированы/Назначены» и «Фактически работали».

**B2.18** → **(б)** добавить кнопки доменных действий в шапку карточки (Submit, Создать акт).

**B2.19** → **(а)** запрет удаления/правки после APPROVED. CISO-011: cannot_modify_approved.

**B2.20** → **(б)** front-component «Проекты, где работал» в карточке сотрудника.

**B2.21** → **(а)** нативные attachments достаточно для Документов.

**B2.22** → **(б)** карточка этапа с plan/fact по этапу полезна.

---

## B3. Навигация / сайдбар

**B3.1** → **(б)** «Согласование» вынести в отдельную группу. Это менеджерская функция, не ввод. Ref=T.

**B3.2** → **(а)** пропущенная позиция 1 — задел. Оставить.

**B3.3** → **(а)** разреженные позиции внутри групп — задел под вставки. Нормализовать только при сдаче в прод.

**B3.4** → **(б)** «Мои трудозатраты» — отдельно над группами как личный раздел. Ref=T.

**B3.5** → **(а)** ЛК = дашборд, Записи = сетка. Разное, переименовать «Записи» → «Таймшит».

**B3.6** → **(б)** «Согласование» — отдельная группа (уже решено в B3.1).

**B3.7** → **(в)** переименовать дубли: «Производственный календарь» (PAGE_LAYOUT, основной) и скрыть VIEW от обычного пользователя (только для admin).

**B3.8** → **(а)** «Табель Т-13» остаётся в «Отчётах».

**B3.9** → **(б)** порядок Справочников: Сотрудники → Отделы → Проекты → Этапы → Производственный календарь → Связи 1С.

**B3.10** → **(б)** развести иконки уникально по смыслу.

**B3.11** → **(а)** Cmd+K достаточно. Inline-фильтр сайдбара — только если >15 пунктов.

**B3.12** → **(б)** по умолчанию открыта только «Ввод времени», прочие свёрнуты. Задать через SDK.

**B3.13** → **(б)** «Настройки · данные» скрыть от обычного пользователя (только admin-роль).

**B3.14** → **(а)** «Настройки» (основные) должны идти выше технического пункта.

**B3.15** → **(а)** standalone, нативные CRM-объекты скрыты намеренно. Добавить «Клиенты/Компании» в «Справочники» — так как `company` проекта = клиент.

**B3.16** → **(а)** только устаревшие комментарии. Почистить при следующем проходе nav-items.

---

## B4. Настройки

**B4.1** → **(б)** «Безопасность» (`revealEmployeeNames`) — отдельная секция СВЕРХУ с warn-цветом и пояснением 152-ФЗ.

**B4.2** → **(б)** пересчитать и зафиксировать ровно 12 обязательных параметров в REQ-0019.

**B4.3** → **(а)** единый источник нормы: `normHoursPerDay` из Settings = `useDailyNorm` = норма для «Заполнить 8×5». `fillTemplateHours` убрать или сделать алиасом.

**B4.4** → **(а)** `defaultCapacityFactor` = дефолт для новых отделов. Отдел переопределяет его локально.

**B4.5** → **(б)** визуально вложить пороги (`overtimeWarnHours`, `minHoursPerWeek`) под `warnOnScheduleDeviation` toggle (disabled при off).

**B4.6** → **(б)** `min` + `max` + суффикс единиц («ч», «недель», «%»).

**B4.7** → **(в)** хранить как долю (0.8), отображать как проценты (80%). Ref=T/Kimai: ёмкость в %.

**B4.8** → **(а)** смысловые подписи всем toggle: «Требовать согласование / Не требовать», «ФИО видны / Скрыты» и т.д.

**B4.9** → **(б)** расширить `approvalPeriod`: добавить BIWEEK (раз в две недели). Ref=T: настраиваемый период.

**B4.10** → **(б)** добавить колонку «Руководитель отдела» в таблицу отделов настроек.

**B4.11** → **(б)** добавить ссылку «Управление отделами →» на справочник.

**B4.12** → **(а)** активный = действующее FTE-назначение на сегодня.

**B4.13** → **(б)** пагинация для крупных оргструктур (>50 отделов).

**B4.14** → **(а)** autosave + «Сохранено ✓» toast. Явные Save/Cancel не нужны для per-field правки.

**B4.15** → **(б)** error-toast «Не удалось сохранить: <причина>». Тихий откат без ошибки = UX-ловушка.

**B4.16** → **(б)** inline-ошибка под полем при невалидном значении (ниже min/выше max).

**B4.17** → **(б)** confirm-диалог при включении `revealEmployeeNames` («Вы раскрываете персональные данные сотрудников согласно 152-ФЗ…»).

**B4.18** → **(в)** глобальный флаг + исключение: руководитель видит ФИО своей команды (решено в B1.14).

**B4.19** → **(а)** стабильный код сотрудника (читаемый, не UUID).

**B4.20** → **(а)** один singleton на приложение. Мультитенантность — отдельная архитектурная задача.

**B4.21** → **(в)** оставить только справочники, связанные с настройками (категории, статусы, виды работ).

**B4.22** → **(б)** добавить секцию «Производственный календарь» в настройки (праздники/переносы/короткие дни).

---

## C1. Справочники (реестры)

**C1.1** → **(в)** настраиваемые колонки через Twenty saved views. Ref=T.

**C1.2** → **(в)** добавить флаг «активен» (архивирование). NB: «биллируемый» запрещён.

**C1.3** → **(в)** видимость ФИО в реестре сотрудников = настройка `revealEmployeeNames`. CISO-007.

**C1.4** → **(в)** переключатель «дерево / список» для Отделов. Ref=T: оргструктура деревом.

**C1.5** → **(б)** быстрая форма (мин. поля) + дозаполнение в карточке. Ref=K: quick-create.

**C1.6** → **(б)** inline-правка статуса/план-часов в реестре Проектов. RemDOM: осторожно — Twenty native inline только в card relation.

**C1.7** → **(б)** архивация (active=false) вместо физического удаления для всех справочников с историей трудозатрат. Ref=K/T: soft-delete.

**C1.8** → **(б)** предупреждение перед удалением проекта: «Будет удалено N этапов, M записей времени».

**C1.9** → **(б)** поиск по SELECT-меткам (для Отделов по code, для Календаря по типу дня).

**C1.10** → **(б)** пресеты «Мои» / «Активные» при открытии реестров.

**C1.11** → **(а)** сортировка по labelIdentifier (код/ФИО) по умолчанию — предсказуемый порядок.

**C1.12** → **(в)** кнопка «Сгенерировать год» (пн-пт=рабочие 8ч, сб/вс=выходные) + ручная правка праздников/переносов. Трудоёмкость построчного ввода недопустима.

**C1.13** → **(в)** переключатель: месячная сетка + список. Сетка для наглядности, список для правки.

**C1.14** → **(а)** Производственный календарь = SSOT рабочих/нерабочих дней. Рабочая суббота = рабочий день.

**C1.15** → **(а)** `hours` дня из календаря — приоритет. Глобальная норма `normHoursPerDay` = fallback если дня нет в календаре.

**C1.16** → **(а)** BillingLink — временно ручной ввод, цель = авто-синхронизация из 1С.

**C1.17** → **(в)** 1 проект → много документов (MANY_TO_ONE на проект). M:N — отдельная задача если понадобится.

**C1.18** → **(б)** добавить контрагент + статус оплаты в BillingLink.

**C1.19** → **(б)** inline-подсказки (?) у каждого параметра Settings. Ref=K/T.

**C1.20** → **(а)** в Settings только `approvalRequired` и `capacityFactor` по отделам. CRUD — в справочнике.

**C1.21** → **(а)** `capacityFactor` важен на уровне отдела — разные отделы имеют разные накладные расходы.

**C1.22** → **(в)** показывать «переопределено» только при отличии от дефолта.

**C1.23** → **(в)** переставить порядок вкладок карточки проекта: Сводка → Команда → Трудозатраты → Обзор → Бюджет → Этапы → (редкие).

**C1.24** → **(б)** валидация суммы FTE долей по отделам. Ref=T: контроль 100%.

**C1.25** → **(б)** предупреждение при Σ FTE сотрудника > 100% на дату. Жёсткий запрет (в) — не MVP.

---

## C2. Booking / Брони

**C2.1** → **(б)** quick-create с доски: клик по ячейке сотрудник×период → форма с предзаполненными сотрудником и датами. RemDOM: drag недоступен. Ref=T/Float.

**C2.2** → **(б)** при создании обязателен `project` ИЛИ `company`. Висячие брони = засор данных.

**C2.3** → **(б)** ввод брони в FTE% (доля занятости). Пересчёт в часы автоматически. Ref=Float/T.

**C2.4** → **(в)** предупреждать при создании если период пересекается с отпуском/больничным сотрудника.

**C2.5** → **(б)** кнопка «Подтвердить бронь» (SOFT→HARD) на карточке брони. Ref=Float: «tentative → confirmed».

**C2.6** → **(в)** при конвертации SOFT→HARD: требовать project (если была под company), логировать кто/когда.

**C2.7** → **(б)** предупреждение перед подтверждением HARD если создаёт overbooking.

**C2.8** → **(а)** два основных сценария: 1) проект известен, 2) пресейл — только клиент. Бронь без обоих — крайний случай.

**C2.9** → **(а)** пресейл-бронь (company без project) = отдельный цвет/слой «Пресейл». Ref=T: pipeline для пресейлов.

**C2.10** → **(б)** кнопка «Преобразовать в проектную бронь» при создании проекта из пресейла.

**C2.11** → **(б)** бронь на роль/должность (TBD-ресурс) — roadmap. employee = NOT NULL сейчас, схема требует изменения.

**C2.12** → **(а)** при найме конкретного человека: замена `employee` у той же записи брони-заглушки.

**C2.13** → **(а)** роль-заглушка учитывается в demand отдела — показывает дефицит явно.

**C2.14** → **(в)** клик на маркер брони → переход в карточку брони. RemDOM: позиционированный поповер сложен без getBoundingClientRect.

**C2.15** → **(б)** тумблер «показать/скрыть SOFT» прямо на доске. Ref=T.

**C2.16** → **(а)** overbooking разрешён + только подсветка (мягко). Ref=T/Float.

**C2.17** → **(б)** показывать дефицит в часах «−12 ч» при overbooking.

**C2.18** → **(б)** пунктирная зона риска (потенциальный overbooking с учётом SOFT) — отдельный режим прогноза.

**C2.19** → **(в)** добавить в реестр броней колонки: Компания + Заметка + признак overbooking.

**C2.20** → **(в)** добавить вкладку «Брони» в карточку клиента (Company) — для пресейл-сценария.

**C2.21** → **(в)** карточка брони: сравнение бронь vs факт по сотруднику+проекту+период (без FK).

**C2.22** → **(а)** бронь и план отдела — раздельные оси. Не связывать FK.

---

## C3. Фильтры / Виды / Сортировка

**C3.1** → **(б)** добавить «Вид работ» и «Группа видов работ» — итого 8 рабочих осей в UI.

**C3.2** → **(в)** и пошаговый drill (как сейчас), и мультиселект-фильтр по каждой оси (несколько значений OR). Ref=T.

**C3.3** → **(в)** уточнить недостающие 3 оси с заказчиком. Кандидаты: «Клиент» (company), «Статус согласования», «Этап».

**C3.4** → **(б)** фильтр по категории проекта (CLIENT/INTERNAL…) — глобальный на всех срезах.

**C3.5** → **(в)** добавить «неделю» + произвольный диапазон from-to. CSV уже поддерживает, UI отстаёт.

**C3.6** → **(б)** 3-4 пресета: «Текущий месяц», «Прошлый месяц», «Текущий квартал», «YTD».

**C3.7** → **(б)** сравнение с предыдущим периодом (Δ%) в отчётах.

**C3.8** → **(б)** «Мой отдел» по умолчанию для руководителя при открытии отчётов.

**C3.9** → **(в)** хранить drill/фильтры в URL-параметрах — расшариваемые ссылки на отчёт. RemDOM: проверить доступность URL через SDK.

**C3.10** → **(б)** персональные + общие (admin настраивает командные шаблоны).

**C3.11** → **(а)** «факт убыв.» — самые загруженные сверху. Полезнее для менеджера.

**C3.12** → **(а)** одной колонки сортировки достаточно.

**C3.13** → **(в)** группировка с подытогами по отделу в табеле Т-13.

**C3.14** → **(в)** явная кнопка «Сбросить» + индикатор «Фильтры (N)».

**C3.15** → **(б)** хранить drill-path в URL (решено в C3.9).

**C3.16** → **(в)** preferred-ось по умолчанию + возможность сменить. Ref=T.

**C3.17** → **(б)** прочерк «—» с пояснением «норма не применима для этого среза».

**C3.18** → **(в)** единый период-селектор компонент, фильтры — per-экран.

**C3.19** → **(а)** общий период на сессию, но с возможностью отвязать на конкретном экране.

**C3.20** → **(а)** чипы активных фильтров с × — единый паттерн, уже есть в Отчётах.

**C3.21** → **(а)** единый empty-state «Ничего не найдено — сбросить фильтр» по всему приложению.

---

## C4. Экспорт / Печать

**C4.1** → **(а)** критичный блокер. Нужен реальный download. RemDOM: Blob/anchor.download недоступны.

**C4.2** → **(б)** бэк генерит файл → presigned URL → ссылка для скачивания. Приоритет разработки.

**C4.3** → **(в)** XLSX + PDF. XLSX для импорта в Excel, PDF для подписи Т-13.

**C4.4** → **(б)** добавить в detail CSV: этап + комментарий записи.

**C4.5** → **(в)** пресеты «для 1С» / «полный» / «краткий» — проще чем чекбоксы полей.

**C4.6** → **(а)** разделитель `;` жёстко — 1С-совместимость приоритет. XLSX решит проблему Excel.

**C4.7** → **(а)** обезличенный стабильный код сотрудника вместо ФИО при `revealEmployeeNames=false`. CISO-007.

**C4.8** → **(в)** и CSV (импорт в 1С), и PDF (подпись руководителем). Ref=1С:ЗУП.

**C4.9** → **(а)** 6 кодов Т-13 хватает + **(б)** включить по умолчанию для кадровиков.

**C4.10** → **(б)** двухстрочная запись в ячейке: верх — код (Я/ОТ/Б), низ — часы. Как в бумажном Т-13.

**C4.11** → **(в)** предупреждать: «Табель по проекту не содержит данные отсутствий».

**C4.12** → **(в)** подытоги: Σ по отделу + сводка по кодам (Σ отпускных дней, Σ больничных).

**C4.13** → **(в)** цель — двусторонний API-обмен 1С↔Time. Фаза 1: ручная выгрузка файла. Фаза 2: API.

**C4.14** → **(в)** оба потока в 1С: Т-13 (для расчёта ЗП) + трудозатраты по проектам (для себестоимости).

**C4.15** → **(а)** сопоставление через `externalCode`/`externalId` — поля есть в моделях.

**C4.16** → **(а)** выгружать только APPROVED записи в 1С. Для расчёта ЗП нужны только утверждённые.

**C4.17** → **(а)** печать Т-13 в первую очередь. Сводка — позже.

**C4.18** → **(а)** PDF с бэка (готов к подписи). RemDOM: window.print недоступен.

**C4.19** → **(а)** полный бланк Т-13 с реквизитами организации, должностями и строками подписей. Реквизиты — из настроек системы.

**C4.20** → **(б)** имя файла: добавить отдел/проект `timesheet-detail_ОтделX_2026-06-01_2026-06-30.csv`.

**C4.21** → **(б)** «Экспорт текущего вида» с любого среза отчёта в добавление к detail/табелю.

**C4.22** → **(б)** логировать факт экспорта с ФИО: кто выгрузил, когда, какой период/фильтр. CISO-007 / 152-ФЗ аудит.

---

## D1. Состояния (пусто / загрузка / ошибка / нет-доступа)

**D1.1** → **(а)** единый компонент `StateView` (иконка + заголовок + подсказка + опц. кнопка-действие) на весь модуль. Impeccable: один стиль состояний.

**D1.2** → **(а)** скелетоны для всех таблиц/сеток (таймшит, capacity, my-time, project-team). Текст «Загрузка…» только для мелких inline-блоков.

**D1.3** → **(в)** generic серые полосы + правильное число колонок под каждую сетку. Точная форма (а) — избыточно.

**D1.4** → **(а)** единый SaveIndicator вверху тулбара на всех экранах с autosave.

**D1.5** → **(а)** ErrorState + «Повторить» на всех экранах с загрузкой данных.

**D1.6** → **(а)** добавить `onRetry` в `my-time/my-periods`. Пропущено, нужно унифицировать.

**D1.7** → **(а)** три типа ошибок: «Нет соединения», «Нет прав», «Ошибка сервера». Impeccable error templates.

**D1.8** → **(б)** «Подробнее» — только для manager/admin. Пользователю raw exception не нужен.

**D1.9** → **(в)** emoji только для позитива (🎉 «все заполнили»). В остальных — нейтральный тон.

**D1.10** → **(б)** действие в empty-state там где осмысленно (таймшит → «Добавить строку», незаполненные → «Открыть таймшит»).

**D1.11** → **(а)** добавить кнопку «Добавить строку» в empty-состояние таймшита.

**D1.12** → **(а)** два разных текста: «Нет данных» vs «Нет данных за выбранный период — [Сменить период]».

**D1.13** → **(а)** кнопка «Сбросить фильтры» / «Сменить период» прямо в пустом блоке.

**D1.14** → **(а)** различать: «Нет сотрудников» vs «ФИО скрыты настройкой — обратитесь к администратору».

**D1.15** → **(а)** явный экран «Недостаточно прав — обратитесь к …». Impeccable: permission denied state.

**D1.16** → **(а)** добавить действие «Написать администратору» в unlinked-состояние.

**D1.17** → **(б)** баннер «Неделя согласована — редактирование недоступно» над таймшитом при APPROVED периоде.

**D1.18** → **(б)** кнопка «Скопировать в буфер» (Clipboard API) + подсказка «Вставьте в Excel как CSV».

**D1.19** → **(а)** глобальный баннер офлайн «Нет соединения — изменения не сохраняются».

**D1.20** → **(а)** показывать загруженные данные + inline-warning «Часть данных не загружена».

---

## D2. Доступность / Клавиатура

**D2.1** → **(а)** `role="grid"/"row"/"gridcell"` + `aria-rowindex/colindex` на таймшит-сетку. Приоритет a11y.

**D2.2** → **(а)** полная координата в `aria-label` ячейки: «[Проект] [Вид работ], [день недели], [N] часов».

**D2.3** → **(а)** capacity/reports — ниже приоритет. `role="table"` для reports достаточно.

**D2.4** → **(б)** статусные и блокирующие `title` → дублировать в `aria-label`. Системно (а) — большой объём.

**D2.5** → **(б)** tooltip по focus показывает полное имя при ellipsis (focus-visible).

**D2.6** → **(а)** `aria-label` обязателен для всех icon-buttons. `title` недоступен с клавиатуры.

**D2.7** → **(в)** добавить Home/End (начало/конец строки) в навигацию таймшита.

**D2.8** → **(б)** capacity/reports: Tab по строкам (drill/focus) достаточно.

**D2.9** → **(а)** Esc закрывает любой поповер/меню — единообразно. Cheatsheet тоже.

**D2.10** → **(а)** focus-trap при открытии поповера/меню + возврат фокуса на триггер при закрытии.

**D2.11** → **(а)** Esc внутри input поповера = отмена + закрытие поповера.

**D2.12** → **(а)** добавить polite-объявления: смена периода, результат bulk-fill, смена среза доски.

**D2.13** → **(в)** aria-live «Итого X ч» — только при сохранении, не при каждой правке ячейки.

**D2.14** → **(а)** поднять тон `textFaint` на цветных фонах (weekendBg, todayCol, heatmap-tint) до WCAG AA.

**D2.15** → **(а)** добавить иконку «!» / «🔒» к warn-строкам настроек (не только цвет).

**D2.16** → **(а)** 3-4 дискретные ступени heatmap вместо плавного градиента + легенда.

**D2.17** → **(а)** ≥24px hit-zone desktop через невидимый padding. ≥44px при touch-режиме (D3.14).

---

## D3. Адаптив / Мобайл / Узкий экран

**D3.1** → **(а)** десктоп-приоритет, планшет «не ломается». Телефон — отдельная задача v2.

**D3.2** → **(а)** фикс-лейаут + h-scroll только там где физически не влезает (capacity-доска). RemDOM: нет @media.

**D3.3** → **(а)** sticky левые 2 колонки (Проект + Вид работ) + h-scroll по 7 дням. Соответствует текущему `week-header.tsx`.

**D3.4** → **(а)** дни (ввод часов) в приоритете. Проект/Вид работ — ellipsis + tooltip по focus.

**D3.5** → **(в)** режим «День» (day-view) = мобильный ответ для узкого. Строки = один день, ввод в столбец.

**D3.6** → **(а)** sticky: колонка имён + sticky хедер периодов (первая строка заголовков).

**D3.7** → **(а)** окно периодов + навигация ‹ › (уже решено A2.22). Бесконечный h-scroll запрещён.

**D3.8** → **(а)** гранулярность (недели/месяцы) — в overflow-меню «▾» тулбара доски.

**D3.9** → **(а)** column picker + h-scroll для detail-отчёта.

**D3.10** → **(б)** KPI-карточки 2×2 на узком. Главная метрика (Утилизация) крупнее.

**D3.11** → **(а)** вторичные кнопки тулбара (Копировать/Очистить/Cheatsheet) в overflow-меню «⋯» при нехватке ширины.

**D3.12** → **(а)** навигация сайдбара — хостовая (Twenty), не наша зона.

**D3.13** → **(а)** явный порядок overflow: важные кнопки остаются, вспомогательные уходят первыми.

**D3.14** → **(а)** один desktop-размер. Планшет принимает desktop-лейаут.

**D3.15** → **(в)** preset-чипы (8 ч / 4 ч / норма) как основной ввод на touch-устройствах.

---

## D4. UX-копирайтинг / Тексты

**D4.1** → **(а)** «Баланс» как основное название + hint «Gap = спрос − ёмкость». «Gap» понятен HR, но для остальных — не очевиден.

**D4.2** → **(а)** «Утилизация» — оставить + tooltip «Доля клиентских часов от факта».

**D4.3** → **(а)** выровнять формулировки: «X ч» везде где часы, «X%» где процент. Убрать смешение.

**D4.4** → **(а)** «Доля ставки (FTE)» и «Численность». FTE в скобках — для тех кто знает термин.

**D4.5** → **(а)** «Заполнить норму на всю неделю» (тулбар). Решено в TIMESHEET_CONTROLS §F5.

**D4.6** → **(а)** три разных имени из §F5: «Заполнить неделю для всех» / «Заполнить будни этой строки» / убрать чип.

**D4.7** → **(а)** «рабочие дни (по производственному календарю)» — явно, чтобы включались рабочие субботы.

**D4.8** → **(а)** везде «норма дня» (не «8 ч»). При короткий день = 7ч текст будет корректен.

**D4.9** → **(б)** «Только проекты и виды работ» вместо «Только строки». Точнее.

**D4.10** → **(в)** одна кнопка «Копировать» + чекбокс «с часами». Радио-группа (а) избыточна.

**D4.11** → **(а)** счётчик в confirm: «Удалить 12 записей неделя? Это действие нельзя отменить».

**D4.12** → **(а)** кнопка confirm = само действие: «Удалить строку», «Очистить часы», «Очистить неделю». Не «OK/Да». Impeccable.

**D4.13** → **(а)** «Удалить строку» (строка исчезает) vs «Очистить часы» (строка остаётся, часы обнуляются).

**D4.14** → **(а)** `rejectComment` обязателен — уже реализовано, подтверждаем. Placeholder «Что исправить сотруднику…» достаточен.

**D4.15** → **(а)** фикс-словарь 4 статуса везде: «Черновик / На согласовании / Согласовано / Отклонено». «submitted», «draft» убрать из UI-текстов.

**D4.16** → **(а)** добавить «как» исправить в toast валидации: «Неделя недозаполнена: X из Y ч. Заполните оставшиеся рабочие дни».

**D4.17** → **(а)** «Отправить N записей на согласование» — явный счётчик.

**D4.18** → **(а)** единый словарь: «Сохраняю…» / «Сохранено ✓» / «Ошибка сохранения» на всех экранах с autosave.

**D4.19** → **(а)** короткий hint «Gap: нехватка или избыток ресурсов» + ссылка «Подробнее о метриках».

**D4.20** → **(в)** и inline-hint по месту (при hover/focus на метрике), и глобальная «?»-справка терминов.

**D4.21** → **(б)** hint по месту при первом входе на доску (tooltip-onboarding при первом показе).

**D4.22** → **(а)** em-dash «—» — норма для русского UI. Запрет только на `--`.

---

## Сводка решений

| Блок | Вопросов | Закрыто | Ключевые решения |
|------|----------|---------|-----------------|
| A1–A4 (таймшит/план/отчёты/согл.) | 116 | 116 | norma SSOT, calendar isNonWorkingDay, datepicker, filter-pills, Т-13 вкладка, recall, A3.21 download-via-backend |
| B1–B4 (ЛК/карточки/nav/settings) | 79 | 79 | CTA в баннере, мягкое RBAC ФИО, overflow-tabs, autosave+error-toast, B4.17 confirm ПДн |
| C1–C4 (справочники/booking/фильтры/экспорт) | 90 | 90 | soft-delete, calendar SSOT, quick-create брони, FTE%, C4.2 backend-download, C4.13 API-обмен |
| D1–D4 (состояния/a11y/адаптив/copy) | 74 | 74 | StateView компонент, role=grid, Esc везде, 4 статуса, словарь «Заполнить» из F5 |
| **Итого** | **359** | **359** | |

**Сквозные приоритеты:**
1. **Скачивание файлов** (C4.1–C4.2 = A3.21) — backend presigned URL. Blocker экспорта.
2. **Норма дня SSOT** (A1.12, A1.24, A4.8, B4.3) — `useDailyNorm` везде, `DAILY_NORM_HOURS=8` убрать.
3. **Производственный календарь SSOT** (A1.15, C1.14) — `isNonWorkingDay` вместо `isWeekend`.
4. **Backend download** для Т-13 PDF + CSV.
5. **role=grid** на таймшит-сетку (D2.1) — крупнейший a11y-долг.
6. **Отзыв согласования** (A4.25) + **Recall** (A4.4) — нереализованы.
7. **Unified StateView** (D1.1) — ошибки/скелетоны/empty по всему модулю.
