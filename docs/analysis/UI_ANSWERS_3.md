# UI-ответы аналитика — волна-3: W3-A · W3-B · Planning-Records (+ Planning-Models ссылка)

**Дата:** 2026-06-21
**Автор:** Аналитик AITEAM (итерация 106)
**Статус:** РЕШЕНИЯ ДЛЯ ЗАКАЗЧИКА (ответы на UI_QUESTIONS_W3_A/W3_B + PLANNING_RECORDS_CONSISTENCY)
**Парные:** UI_ANSWERS.md (A·B·C·D), UI_ANSWERS_2.md (E·F·G + Dialog)
**Формат:** `**X.Y** → (вариант) «обоснование» [ref]`

Сокращения: T=Timetta, K=Kimai, RemDOM=Remote DOM, CISO=безопасность, NB=no-billable, imp=impeccable.

> **Planning-Models (38 вопросов A1–I5)** — отвечены в SIGNALS итерация 95 (полный разбор). Сводка ключевого в конце этого файла (§ Planning-Models дайджест), чтобы arch имел всё в одном месте.

---

## W3-A. Таймшит/ячейки — углубление (после WI-01)

### Группа 1. Клавиатура «клик=ввод»

**W3A.1** → **(а)** печать заменяет старое (Excel overwrite). Подтверждаю текущее. Ref=T/K.

**W3A.2** → **(а)** Esc только откат ввода, выделение остаётся. Ref=T/K.

**W3A.3** → **(б)** Tab wrap на первый день следующей строки. Ускоряет заполнение недели. Ref=T. (Совпадает с E4.2.)

**W3A.4** → **(б)** Enter на последней строке → фокус в AddRow (поле «код проекта»). Ускоритель «забил неделю, добавляю проект». Ref=K.

**W3A.5** → **(б)** Shift+Enter = «вверх» (вернуть табличную норму), bulk-fill перевесить на **Alt+→** или **Ctrl+D** (заполнить будни). Конфликт развести в пользу привычки. bulk-fill дублируется в меню ⋯, потери нет. Ref=T/K.

**W3A.6** → **(а)** после Enter ячейка ниже выделена, не в edit (печать продолжит). Ref=T/K.

**W3A.7** → **(а)** Delete мгновенно обнуляет. Но добавить **undo-тост** (F3.1/E1.22) — защита от случайного стирания при навигации.

**W3A.8** → **(б)** Home/End = первая/последняя РЕДАКТИРУЕМАЯ ячейка (пропуск locked) + **(в)** Ctrl+Home/End = края всей сетки. Ref=Excel. (D2.7.)

### Группа 2. Меню ⋯

**W3A.9** → **(в)** порядок по частоте: Заполнить будни нормой → Комментарий → Дублировать → [разд] → Обнулить → Убрать. Ежедневное действие наверх. Ref=K/T.

**W3A.10** → **(в)** иконки только у деструктивных (Обнулить/Убрать) — акцент на риске. Остальное — текст+hint (читаемее под RemDOM).

**W3A.11** → **(б)** confirm только «Убрать строку» (необратимо) + счётчик «удалить N записей»; «Обнулить» — мгновенно + undo-тост. Баланс скорости/защиты. D4.11/D4.12.

**W3A.12** → **(б)** для lock-строки + «Посмотреть комментарии» (read-only). Полезно для аудита согласованной недели. Ref=T.

**W3A.13** → **(а)** оставить 3 группы (создать / заполнить-править / удалить). Не переусложнять. imp: 2-3 кластера.

### Группа 3. Норма без чипа

**W3A.14** → **(а)** достаточно печати+меню для MVP. Точечный affordance (dbl-click/панель) — только если W3A.16 покажет частый сценарий. keep-it-simple.

**W3A.15** → **(а)** если понадобится точечная норма — клавиша «N»/Space на активной ячейке (надёжнее dbl-click под RemDOM).

**W3A.16** → **запрос к заказчику:** частота точечной нормы? Гипотеза (а) «заполняют всю неделю разом» → точечный ввод не нужен. Определяет нужность W3A.14/15.

**W3A.17** → **(б)** плейсхолдер-норма дня бледным в пустой ячейке («7» в короткий день из useDailyNorm). Дёшево, информативно, не навязчиво. Ref=T.

### Группа 4. Дубль с часами

**W3A.18** → **(в)** два отдельных пункта меню: «Дублировать строку» / «Дублировать со часами». Понятнее сабменю под RemDOM. A1.23.

**W3A.19** → **(а)** дефолт БЕЗ часов (дубль = для другого вида работ на том же проекте, hint это говорит). Подтверждаю текущий смысл.

**W3A.20** → **(в)** разрешить дубль, показать предупреждение «такая строка (проект+вид) уже есть». Жёсткий запрет (а) — грубо; суммирование (б) — скрытая магия. Целостность через warning.

### Группа 5. Комментарий

**W3A.21** → **(а)** комментарий только по дню с часами (= аллокация, соответствует модели). Ref=T/K.

**W3A.22** → **(а)+(в)** комменты только к часам; отсутствия (больничный/отпуск) — отдельный механизм (коды Т-13 Я/ОТ/Б, C4.9). Не смешивать. Ref=T.

**W3A.23** → **(б)** счётчик в пункте меню «Комментарий к записи (N)» — видно наличие не открывая. Дёшево. Ref=T.

**W3A.24** → **(б)** Enter сохраняет + переходит к следующему полю дня (Tab-подобно); панель закрывает Esc/последнее поле. Удобно для многодневных правок.

### Группа 6. Размеры/узкий/перенос

**W3A.25** → **(а)** 64×40px (принято в F-блоке). Привести код (сейчас 58×32) в соответствие. **Зафиксировать рассинхрон код↔решение.** Ref=T ~64×40.

**W3A.26** → **(а)** sticky Проект+Вид работ, h-scroll по дням. D3.3. Ref=T/K.

**W3A.27** → **(а)** жёсткий ellipsis + tooltip по focus. Фикс-высота, ровная сетка (tabular). D3.4. Ref=T.

**W3A.28** → **(а)** sticky шапка дней + sticky футер «Итого» при вертикальном скролле. D3.6. Ref=T/K.

**W3A.29** → **(а)** «Проект» эластичный сжимается первым, «Вид работ» держит 176px; **(в)** на очень узком — объединить в одну колонку (2 строки) как fallback (GRID_TEMPLATE_SINGLE уже есть).

**W3A.30** → **(а)** 14px при ячейке 40px (F-блок). Ref=T ~14px неделя.

---

## W3-B. Планирование — углубление (после WI-05 + WI-11)

### Зона 1. «Вручную по месяцам» (фаза-2)

**W3B.1** → **(а)** только месяцы в ручной таблице (компактно в поповере). Недели — опция при коротких проектах позже. Ref=T booking-scale.

**W3B.2** → **(а)** абсолютные часы в слот (привычно, Σ видна). % — отдельная единица (зона 2), не смешивать с ручным методом.

**W3B.3** → **(в)+авто-«довести равномерно»** разрешить Σ слотов ≠ план, мягкое предупреждение «−20 ч» + кнопка «довести равномерно остаток». НЕ блокировать. Ref=T ручной метод не форсит Σ.

**W3B.4** → **(а)** сохранять введённое + алерт о расхождении при сдвиге дат/объёма. Ref=T «значения остаются», PLANNING_MODELS C3.

**W3B.5** → **(а)** предзаполнить таблицу текущим равномерным раскидом при EVEN→MANUAL. Правишь от готового. Q13.

**W3B.6** → **(а) Вариант A: отдельный объект `credos-time-plan-slot`** (projectId, periodKey, hours). Прецедент REQ-0013 (доли отделов = отдельный объект). JSON-поле (B) не фильтруется/не индексируется. **Зона Dev2, блокирует фазу-2.**

**W3B.7** → **(б) offset от старта** ИЛИ абсолютные даты слота start/end. Календарный ключ (`2026-07`) хрупок к сдвигу дат. Ref=T: форма раскида переезжает с датами.

**W3B.8** → **(а)** слот раскидывать по рабочим дням внутри месяца той же `plannedHoursInPeriod` (бесшовно с WI-05). Минимум изменений.

**W3B.9** → **(а)** только проект на MVP-2. dept-plan/booking — позже (та же формула общая, расширяемо).

### Зона 2. Единицы плана

**W3B.10** → **(а) только часы** на обозримую фазу. go/no-go = no для FTE/%. **Гипотеза ГИП-1 верна: Кредо-С мыслит бюджетом проекта в часах.** keep-it-simple. **(запрос к заказчику для подтверждения)**

**W3B.11** → (если добавим) дропдаун-суффикс у инпута объёма. Q10. Но см. W3B.10 — пока no.

**W3B.12** → (если «ч/нед») Σ производная, показывать «Σ ≈ 480 ч за 12 нед». Ref=T «% расписания». Отложено (W3B.10).

**W3B.13** → (если FTE%) % от рабочих часов периода × 1 ставка (Ref=T «% расписания»). Отложено.

**W3B.14** → нормализовать в часы (`plannedEffort`) + `planUnit` для отображения. Доска не трогается. Q11. Отложено.

### Зона 3. Превью

**W3B.15** → **(а)** авто-порог 70 дней (без лишних контролов). imp: меньше контролов в поповере.

**W3B.16** → **(б) бар = util%** (заполнение до ёмкости, перелив красным). Util нагляднее «влезает ли», совпадает с визуальным языком доски (ratio=load/capacity). Устраняет рассинхрон превью↔доска.

**W3B.17** → **(а)** без детализации по клику. Превью = быстрый sanity-check, не доска. keep-it-simple.

**W3B.18** → **(б) против СВОБОДНОЙ ёмкости** (за вычетом других проектов). **КРИТИЧНО:** текущее превью vs полная ёмкость скрывает реальный овербукинг. Передать загрузку др. проектов в панель. Ref=T «вся доступная ёмкость» учитывает занятость. **Баг-риск фазы-1.**

**W3B.19** → **(а)** когда добавим авто-конец — показать до горизонта с пометкой «открытый». Пока ПО обязательна (W3B.23).

### Зона 4. Овербукинг + endDate

**W3B.20** → **(б)** жёлтый 90-100%, красный >100%. Прецедент threshold=0.9 в `firstFreePeriod`. Мягкий порог информативнее.

**W3B.21** → **(б)** инлайн-баннер «⚠ овербукинг в N периодах» над кнопкой, кнопка активна (не блокировать). window.confirm запрещён. Ref=T не блокирует, imp предупреждать.

**W3B.22** → **(в) превью на долю каждого долевого отдела** (REQ-0013 sharesByProject). **КРИТИЧНО:** для мульти-отдельного проекта сравнение с одним dept неверно. Минимум — сумма ёмкостей долевых отделов. **Баг-риск.**

**W3B.23** → **(б) разрешить пусто, тянуть до горизонта** — устранить рассинхрон панель↔доска. Панель сейчас требует ПО, доска тянет до horizonEnd → рассогласование. Унифицировать в пользу «как доска» + пометка «открытый план». (Корректирую свой прежний Q17 «обязать» — рассинхрон важнее, доска уже показывает открытый.)

**W3B.24** → **(в) авто-конец позже.** Фронт-метод (объём÷ёмкость) — не MVP, спрос не подтверждён. keep-it-simple. Ref=T «Фронтальное».

### Зона 5. План×сотрудник + методы Timetta

**W3B.25** → **(в) да, но позже.** Отдела+долей хватает на MVP (ГИП-1). План×человек — отдельный экран ресурс-плана, фаза-3. PLANNING_MODELS D2.

**W3B.26** → **(а)** отдельный экран «ресурсный план проекта» (вкладка «Ресурсы»). Поповер не вместит матрицу сотрудник×период. Ref=T.

**W3B.27** → **(а)** Σ по сотрудникам = план проекта (декомпозиция, как `plannedEffortShare`). Ref=REQ-0013.

**W3B.28** → **(а) хватит равномерно+вручную (80% кейсов).** Фронт/бэк/вся-ёмкость/% — без подтверждённого спроса не строим. PLANNING_MODELS B3/B4/B5.

**W3B.29** → (если фронт/бэк) простой тумблер старт/финиш (фикс. профиль). Отложено (W3B.28).

**W3B.30** → **(а) одно и то же — не плодить.** Единица «%» = метод «% расписания». Для Кредо-С слить. **Решить до реализации, чтобы не делать две двери в одну комнату.**

### Гипотезы

**ГИП-1** → ✅ ПОДТВЕРЖДАЮ. Кредо-С мыслит бюджетом проекта в часах. Зона 2 (FTE/%) и план×сотрудник — НЕ приоритет. Ценность: «вручную по месяцам» + фикс овербукинга по свободной ёмкости.

**ГИП-2** → ✅ ПОДТВЕРЖДАЮ. Главная боль = «куда легли часы и не перебронировал ли». Приоритет: util%-превью (W3B.16) + овербукинг vs СВОБОДНАЯ ёмкость (W3B.18) + мульти-отдел (W3B.22). Дешевле новых единиц, бьёт в исходное непонимание.

**ГИП-3** → ✅ ВЕРОЯТНО. «Вручную» нужен из-за ЭТАПНЫХ внедрений (анализ→настройка→ОПЭ). **Рекомендация: связать ручной раскид с `project-stages`** (этапы уже в модели) вместо голой таблицы период×часы — мощнее для РП. Показать заказчику оба варианта. **(запрос к заказчику)**

**ГИП-4** → ✅ ПОДТВЕРЖДАЮ. Рассинхрон панель↔доска (W3B.23) — источник багов. Унифицировать (решено в W3B.23 в пользу «тянуть до горизонта»).

---

## Planning-Records. Модель хранения плана (консистентность)

> Ответ заказчику на «создаёт ли Планировать записи плановых загрузок».

**Короткий ответ заказчику:** НЕТ, сейчас «Планировать» = PATCH трёх полей (plannedEffort+startDate+endDate), раскид по периодам ВЫЧИСЛЯЕТСЯ на лету, нигде не хранится. «Плановые загрузки» в сайдбаре = план отдела БЕЗ проекта (другой смысл, не детализация). Ручная правка по месяцам сейчас невозможна — нет хранимого слот-объекта.

**1** → **Г1 гибрид:** «Равномерно» = агрегат без записей, «Вручную» = создаёт слоты. Прямой ответ заказчику: в ручном режиме записи создаются и правятся; в равномерном — нет, всё из агрегата. Ref=REQ-0004 §B, T resource-plan.

**2** → **Г1 на лету** в равномерном режиме. Материализация плодит сотни строк ради того, что считается формулой.

**3** → **Г3 флаг `planMethod` per-проект:** MANUAL → слоты источник истины (plannedEffort = Σ слотов); EVEN → plannedEffort ведущий. Убирает рассинхрон. Ref=REQ-0004 fallback.

**4** → **Г2 старт: проект × период, с опц. полем `department`** (стыкуется с REQ-0013 позже) и опц. `employee` (REQ-0010 позже). Один объект, растёт по мере надобности.

**5** → **Г1 месяц** (соответствует «по месяцам» в запросе заказчика). Ref=buildPeriods.

**6** → **Г1 сохранять введённые значения** при сдвеге масштаба/срока, не пересчитывать. Ref=T booking-methods.

**7** → **Г1 из обоих** (доска + реестр). Двусторонняя правка = прямой ответ на «чтобы потом править».

**8** → **Г1 вкладка на карточке проекта + доска.** Отдельный пункт сайдбара — позже если понадобится. Не путать с текущими «Плановые загрузки» (dept-plan).

**9** → **Г1 фаза-2: inline по ячейке доски.** Поповер-таблица — переходный вариант. Excel-style правка естественна.

**10** → **запрос к заказчику (КЛЮЧЕВОЙ):** «Плановые загрузки» в сайдбаре = план отдела БЕЗ проекта (credosTimeDeptPlan). Гипотеза Г2: заказчик ожидал там детализацию плана проектов → нужен новый слот-объект + возможно переименование. **Уточнить понимание заказчика.**

**11** → **Г1 раздельно** (план отдела-без-проекта REQ-0012 ≠ слоты плана проекта). Разный смысл, calc-load суммирует как разные слои.

**12** → **Г1 да,** добавить правку `plannedEffortShare` в поповер при раскрытии проекта по отделам. Ref=A2.16/REQ-0013.

**13** → **Г1 слот общий (проект×период), делится по долям** при гранулярности «проект×период». Если позже «проект×отдел×период» — слот сразу по отделу.

**14** → **Г1 оставить раздельно** (бронь = резерв ≠ план-оценка). Инвариант Timetta REQ-0004 §C. Подтверждаю (= G1/A2-серия).

**15** → **Г1 одним объектом, поле `employee` опционально** — заполняется когда дойдём до «Люди» (REQ-0010). Не плодить второй объект.

**16** → **Г1 мягкое предупреждение** «Σ слотов = X, план = Y», не блок. Σ-сверка уже в поповере. (= W3B.3.)

**17** → **Г1 ручное число = есть число, календарь не вмешивается** (пользователь сам решил) + **Г2 предупреждать овербукинг** (механизм превью уже есть). Не нормировать ручной ввод по календарю.

**18** → **Г1 при EVEN→MANUAL заполнить слоты текущим равномерным раскидом** (готовая стартовая сетка). (= W3B.5.) Ref=plannedHoursInPeriod готов для предзаполнения.

**Рекомендованная модель (итог):** уровень 0 (агрегат+вычисляемый раскид, есть) для «Равномерно» + уровень 1 (слот-объект `credos-time-plan-slot` {project, department?, employee?, periodKey/offset, plannedHours, source}) для «Вручную». Флаг `planMethod` (EVEN/MANUAL) на проекте решает источник истины. Начать с проект×период, отдел/человека — опц. поля того же слота. **Зона Dev2 — блокирует фазу-2 WI-30.**

---

## Planning-Models — дайджест (полные ответы в SIGNALS итер.95)

- **A** единица: часы итого (A1); FTE/% нет на MVP (A2); бюджет-потолок да, усилить UI (A3); только часы NB (A4).
- **B** метод: равномерно дефолт (B1); **рабочие дни** да = WI-05 фикс (B2); фронт/бэк нет (B3); вся-ёмкость нет (B4); авто-без-овербукинга нет (B5).
- **C** ручной: да, правка по месяцам (C1); месяцы (C2); сохранять при сдвиге (C3); без гибрида (C4); без undo MVP (C5).
- **D** гранулярность: проект×отдел (D1); план×человек нет на MVP (D2); этапы без часов (D3); доли вручную (D4).
- **E** старт/горизонт: от startDate (E1); прошлое оставлять (E2); **проект без endDate — тянуть до горизонта** (E3, уточнено в W3B.23); горизонт настраиваемый (E4).
- **F** нерабочее: огибать выходные = WI-05 (F1); отпуск только ёмкость (F2); FTE только ёмкость (F3); общий календарь (F4).
- **G** план/бронь/факт: отдельные оси (G1); без авто-перераспределения (G2); прогноз нет MVP (G3); те же методы (G4).
- **H** овербукинг/роли: показывать конфликт (H1); без приоритетов (H2); РП soft / рук.отдела hard (H3); разграничение прав да (H4).
- **I** визуализация: тултип-разбивка (I1); без бейджа метода (I2); строка-сверка Σ да (I3); слои в ячейке да (I4); отдельный экран ресурс-плана нет MVP (I5).

---

## Сводка волны-3

| Блок | Вопросов | Закрыто |
|------|----------|---------|
| W3-A (таймшит/ячейки углубление) | 30 | 30 |
| W3-B (планирование углубление) | 30 | 30 |
| Planning-Records (модель хранения) | 18 | 18 |
| Planning-Models (дайджест, полн. в SIGNALS-95) | 38 | 38 |
| **Итого волна-3** | **116** | **116** |

**Критичные решения (влияют на бэк/архитектуру, первыми):**
1. **W3B.6/W3B.7 + Planning-Records §5** — модель хранения ручного раскида: отдельный объект `credos-time-plan-slot`, ключ offset/абсолютные даты. **Блокирует фазу-2, зона Dev2.**
2. **W3B.18 + W3B.22** — превью/овербукинг vs ПОЛНАЯ ёмкость и по одному отделу = **скрывает реальный конфликт (баг-риск уже в фазе-1)**. Исправить на свободную ёмкость + мульти-отдел.
3. **W3B.23** — рассинхрон панель(требует ПО)↔доска(тянет до горизонта): унифицировать в пользу «тянуть до горизонта».
4. **W3A.25** — рассинхрон код(58×32)↔решение(64×40): привести в соответствие.
5. **W3A.5** — конфликт Shift+Enter: освободить под «вверх», bulk-fill на Alt+→/Ctrl+D.

**Запросы к заказчику (нужен ввод):**
1. **Planning-Records #10** — «Плановые загрузки» = план отдела без проекта; ожидал ли заказчик там детализацию плана проектов?
2. **W3B.10** — только часы или нужны FTE%/ч-нед? (определяет объём фазы-2/3)
3. **ГИП-3/W3B** — «Вручную» через голую таблицу период×часы ИЛИ привязка к этапам проекта (`project-stages`)?
4. **W3A.16** — частота точечной нормы в одну ячейку (нужен ли быстрый affordance).

**Ожидается:** W3-C (ещё генерится) — отвечу отдельно.

**ИТОГО по всем волнам: 651 (волны 1-2) + 116 (волна-3) = 767 вопросов отвечено** (W3-C в ожидании).
