# Реализационный ТЗ — Волна-2 (work-items по ответам W4/W5/W6 + арх-ценность Timetta/Kimai)

**Дата:** 2026-06-21
**Автор:** арх-аналитик AITEAM (итерация 115)
**Статус:** ТЗ к раздаче Dev (READ-ONLY свод, код не трогался)
**Продолжение:** `IMPLEMENTATION_TZ.md` (WI-01..49). Нумерация продолжается с **WI-50**.
**Источники решений:**
- `UI_ANSWERS_W5_A.md` (30 edge-cases lifecycle/approval)
- `UI_ANSWERS_W4_W5.md` (W4-A/B/C + W5-B/C, 144 решения)
- `UI_ANSWERS_W6.md` (W6-A/B/C, 95 решений финализации)
- `TIMETTA_KIMAI_ARCH_VALUE.md` (6 арх-находок — секция «Как внедрить» ниже)
**Сверка с SIGNALS (2026-06-21/22):** submit-REJECTED ✓ · capacityFactor единый ✓ · CASCADE→RESTRICT ✓ — СДЕЛАНЫ, в WI ниже помечены [DONE]. Остаётся 4-й P0 **detail-CSV-PII (W5C.19)** — открыт.

---

## Легенда (как в основном ТЗ)
- **Зона:** Dev1 = фронт · Dev2 = бэк (logic/objects/calc/schema) · QA.
- **Усилие:** XS ≤ 0.25д · S ≤ 0.5д · M ≈ 1-2д · L ≥ 3д.
- **Приоритет:** P0 = баг/блокер/утечка ПДн · P1 = ядро запроса · P2 = улучшение/задел.
- **Риск [[twenty-sdk-apply-gotchas]]:** *additive* (только добавляем) · *behavior-change* (регресс нужен) · *two-phase* (schema→данные→UI) · *RemDOM* (нет host-DOM/drag/Blob/window).
- **NB-проверка [[no-billable-concept]]:** ни одно WI ниже НЕ вводит billable/bill-rate. cost-rate/деньги — ОТЛОЖЕНО до go-ahead заказчика (W4A.18).

---

# ЧАСТЬ 1. НОВЫЕ WI ИЗ ОТВЕТОВ W4/W5/W6

> Многие edge-cases W5-A углубляют уже существующие WI-10/WI-19 (approval). Где так — помечено «дельта к WI-NN». Новый код только там, где помечено «новое».

## P0 — баги/утечки/целостность (волна-2)

### WI-50. detail-CSV/экспорт: редакция ФИО по revealEmployeeNames (4-й P0, утечка ПДн) — Dev2
- **Решения:** W5C.19 🔴 · согласуется CISO-007/152-ФЗ · W4A.30 (две выгрузки: обезличенная управленка / именная ЗУП по праву).
- **Проблема:** detail-отчёт + detail-CSV пишут `employeeName` без проверки `revealEmployeeNames` → ФИО утекает в обход маскировки. SIGNALS: фикс не подтверждён, статус открыт.
- **Что менять:** `logic-functions/reports-detail.ts` (`computeDetail`/`detailToCsv`) — при `reveal=false` отдавать стабильный код вместо ФИО (как byEmployee/Т-13 уже делают). Право «Кадры/ЗУП» (WI-15) → именная выгрузка с логом факта (W4A.30, C4.22). Проверить ВСЕ срезы detail (экран + CSV) на единый резолв метки.
- **Зона:** Dev2 · **Усилие:** S · **Риск:** behavior-change (регресс PII-теста) · **Приоритет:** P0 (последний из 4 P0).

### WI-51. date → чистый день (DATE, не DATE_TIME) + нормализатор записи — Dev2
- **Решения:** W5C.2 🔴 · W5C.6 🔴 · W5C.7 · W5C.9 · G4.1.
- **Проблема:** индекс по `DATE_TIME` + гард-уникальности «по дню» → запись с произвольным временем (грид/CSV-импорт, вечер по МСК) создаёт дубль = двойной счёт `factHours` + сдвиг суток в отчёте/на границе года.
- **Что менять:** поле `date` записи → семантика чистого дня (DATE) ИЛИ нормализатор-хук на запись (truncate к 00:00 МСК). TZ воркспейса = МСК зафиксировать. Уникальный ключ `employee|project|workType|date(день)`. Связь dedup-скрипт (W5C.28 — подтвердить, что на проде не запускается, откат необратим).
- **Зона:** Dev2 · **Усилие:** M · **Риск:** two-phase + behavior-change (миграция существующих записей, регресс rollup) · **Приоритет:** P0 (корневой — двойной счёт).

### WI-52. hours==0 → ERROR в validateEntry (не сохранять 0) — Dev2
- **Решения:** W5C.27 · согласуется validateEntry (WI уже есть día-правила).
- **Проблема:** запись с `hours=0` проходит, не считается, но держит уникальный ключ → блокирует ввод нормальной записи = несогласованность.
- **Что менять:** `constants/validation.ts` (`validateEntry`) — `hours==0` → ERROR (не сохранять). Пустая ячейка = удаление записи (а не 0-запись).
- **Зона:** Dev2 (+Dev1 текст ошибки) · **Усилие:** XS · **Риск:** behavior-change · **Приоритет:** P0.

### WI-53. Отчёты по отделу НА ДАТУ записи (история EmployeeDepartment), не primary — Dev2
- **Решения:** W4A.28 🔴 · опирается на REQ-0011 (employee×отдел с датами).
- **Проблема:** скрытый баг отчётов — часы падают на primary-отдел сотрудника, игнорируя переводы → при смене отдела затраты ложатся не на тот отдел. 1С требует затраты на дату с учётом переводов.
- **Что менять:** `reports-calc.ts` + агрегаторы byDepartment — резолв отдела записи по `EmployeeDepartment` на `date` записи (история REQ-0011), а не текущий primary. (Файл `reports-calc.ts` уже в git-статусе модифицирован — свериться с идущей правкой.)
- **Зона:** Dev2 · **Усилие:** M · **Риск:** behavior-change (числа по отделам сдвинутся — это фикс) · **Приоритет:** P0 (искажение отчётности).

### WI-54. CSV: запятая для дробных часов (RU-локаль) — Dev2
- **Решения:** W6B.14 🔴 · W6A.3 · G4.5/G4.18 · W5B.22 (округление только на отображении).
- **Проблема:** detail/Т-13 CSV пишет часы точкой; RU-Excel с `;`-разделителем ждёт запятую → дробные часы ломаются.
- **Что менять:** `reports-detail.ts` (`detailToCsv`/`toCsvRow`) — числовой формат с запятой в RU-CSV (BOM+`;` уже есть). UI остаётся запятая (W6A.3), ввод принимает обе. Зафиксировать SSOT-правило: «UI запятая, CSV запятая, расчёт float».
- **Зона:** Dev2 · **Усилие:** XS · **Риск:** behavior-change (формат файла — согласовать с 1С) · **Приоритет:** P0 (баг локали, кадры выгружают часто).

### WI-55. Гонки согласования: optimistic-skip (CAS) + collect-errors (не throw на середине) — Dev2
- **Решения:** W5A.7 🔴 · W5A.29 · W5A.8 (счётчик «пропущено N») · подтверждено арх-находкой §3 (Timetta: повторное approve = no-op, single-resolver; запрет mixed-state batch).
- **Проблема:** `runResolve`/`runRevoke`/`runRecall` — read-then-write без CAS (гонка) + throw на середине батча = частичное применение без отката.
- **Что менять:** `approval.logic.ts` — перед write проверять, что статус не сменился (optimistic-lock, иначе skip); батч = collect per-item errors, идемпотентный повтор (уже APPROVED пропускаются), ответ `{approved, skipped}`. (Файл `approval.logic.test.ts` уже в git-статусе — свериться.)
- **Зона:** Dev2 · **Усилие:** M · **Риск:** behavior-change + CISO · **Приоритет:** P0 (корректность согласования).

### WI-56. Аудит revoke: revokedBy/revokedAt + resolvedBy/resolvedAt (разделить approve-семантику) — Dev2
- **Решения:** W5A.11 🔴 · W5A.12 · W5A.24 (НЕ вводить REVOKED-статус, различать через approvedAt is null + revokedBy) · подтверждено §3 (Timetta хранит историю в Activity-логе).
- **Проблема:** revoke обнуляет `approvedBy/approvedAt` → кто отозвал нигде = потеря аудита; `approvedBy` на REJECTED = двойная семантика (отклонивший).
- **Что менять:** схема записи +`revokedBy`/`revokedAt`; resolver-поля `resolvedBy`/`resolvedAt` (approve+reject) отдельно от `approvedBy` (approve-only). revoke = чистый возврат в SUBMITTED БЕЗ нового статуса (W5A.24 — 4 статуса сохраняются). UI: для REJECTED подписывать «отклонил: …».
- **Зона:** Dev2 (схема+logic) +Dev1 (подписи) · **Усилие:** M · **Риск:** two-phase (schema additive поля) · **Приоритет:** P0 (CISO/аудит). **Корректирует WI-10** (там был REVOKED-статус — снять, заменить на revokedBy).

### WI-57. submit reject-defense + полнота submit (класс CISO-005) — Dev2
- **Решения:** W5A.5 (backend валидирует, что reject-ids = полный submit сотрудника, не доверять клиенту) · W5A.6/W5A.30 [DONE: submit-REJECTED по SIGNALS] — `status[in]:DRAFT,REJECTED`.
- **Что менять:** `approval.logic.ts` reject-путь — серверная проверка полноты выборки (defense). submit-REJECTED уже закрыт (SIGNALS) — здесь только reject-defense.
- **Зона:** Dev2 · **Усилие:** S · **Риск:** additive (defense) · **Приоритет:** P0 (CISO-005 server-identity класс).

---

## P1 — ядро запроса (волна-2)

### WI-58. План пропорционально FTE (фикс завышенной util полуставочников) — Dev2
- **Решения:** W5B.13 🔴 · W6C.19 🔴 · опирается REQ-0011 (FTE%) · Timetta-канон «аллокация по доступности».
- **Проблема:** план÷поровну, а ёмкость÷FTE → полуставочник получает завышенную util.
- **Что менять:** `calc-load.ts` / распределение плана — план на сотрудника пропорционально его FTE-доле в окне (0.5 ставки → половина доли). Средняя по периодам с ёмкостью (W6C.18) + показывать «(n из N)».
- **Зона:** Dev2 · **Усилие:** M · **Риск:** behavior-change (util сдвинется — фикс) · **Приоритет:** P1.

### WI-59. Снапшот нормы/headcount закрытых периодов (заморозка согласованного) — Dev2
- **Решения:** W5B.2 🔴 · W5B.16 (версионность назначения новой записью с датой) · опирается REQ-0011.
- **Проблема:** норма/headcount пересчитываются задним числом (правка календаря/FTE) → прошлые согласованные отчёты «плывут». Timetta: закрытый период заморожен.
- **Что менять:** при согласовании периода фиксировать снапшот нормы/headcount (поле снапшота на записи/периоде) ИЛИ опираться на версионность EmployeeDepartment (W5B.16 — старую запись закрывать датой, новую открывать). MVP-минимум: версионность назначений уже даёт корректную историю — сначала проверить, достаточно ли её, прежде чем строить отдельный снапшот.
- **Зона:** Dev2 · **Усилие:** L · **Риск:** two-phase + behavior-change · **Приоритет:** P1. **MVP-граница:** если версионность REQ-0011 покрывает — снапшот не строить (keep-it-simple).

### WI-60. Сетка таймшита: норма из календаря (убрать фикс-8/40) — Dev1
- **Решения:** W5B.21 🟡 · W5B.20 (порог недобора = норма недели × FTE, не плоский) · W5B.1/3 (норма из календаря SSOT). Пересекается с WI-02 (норма SSOT) — доводка.
- **Проблема:** сетка считает норму фикс-8/40, отчёт — по календарю → на праздничной неделе разная «норма».
- **Что менять:** `grid/*` (useDailyNorm везде, см. WI-02), порог недобора недели = `Σ норма рабочих дней × FTE`, убрать плоский minHoursPerWeek.
- **Зона:** Dev1 · **Усилие:** S · **Риск:** behavior-change · **Приоритет:** P1. **Дельта к WI-02/WI-60-предка.**

### WI-61. weekStartsOn: убрать опцию «Вс», жёстко Пн (РФ) — Dev1+Dev2
- **Решения:** W5B.30 (для РФ всегда Пн, опцию Вс убрать — keep-it-simple) · W5A.22 (таймшит недельный Пн-Вс) · G4.10.
- **Проблема:** `weekStartsOn` влияет только на отрисовку, расчёты хардкод Пн → при выборе Вс расхождение. Убрать Вс проще, чем чинить расчёты.
- **Что менять:** settings — убрать опцию Вс (оставить Пн); фронт-отрисовка — снять ветку Вс. (Минимальное решение — не чинить расчёты под Вс, а удалить выбор.)
- **Зона:** Dev1 (+Dev2 settings-поле) · **Усилие:** XS · **Риск:** behavior-change (удаление опции) · **Приоритет:** P1.

### WI-62. Рабочая суббота: запись WORKDAY hours=8 (подтверждение) — Dev1
- **Решения:** W5B.5 (рабочая суббота = WORKDAY hours=8, работает уже; 5-й тип WORKING_WEEKEND — roadmap). Пересекается WI-03 (календарь SSOT).
- **Что менять:** подтвердить, что `isNonWorkingDay` (WI-03) корректно даёт рабочую субботу как рабочий день; отдельный визуальный тип — roadmap, НЕ MVP.
- **Зона:** Dev1 · **Усилие:** XS · **Риск:** — (проверка) · **Приоритет:** P1. **Дельта к WI-03.**

### WI-63. 1С-маппинг поля: personnelNumber + externalId + accountingCode — Dev2
- **Решения:** W4A.6 (personnelNumber табельный № + externalId GUID 1С) · W4A.7 (accountingCode статья/номенклатурная группа, отдельно от externalCode) · W4A.16 (upsert по externalCode, fallback по code).
- **Что менять:** +TEXT поля на employee (`personnelNumber`, `externalId`), на проекте/виде работ (`accountingCode`). Ключ матчинга для импорта/выгрузки = табельный/GUID (ФИО ненадёжно — тёзки). Связь с meta-fields (арх-находка §6 ниже — рассмотреть customFields вместо россыпи полей).
- **Зона:** Dev2 · **Усилие:** M · **Риск:** two-phase (schema additive) · **Приоритет:** P1 (основа 1С-интеграции).

### WI-64. Выгрузка в 1С: два файла (Т-13 кадры месяц + проектные часы управленка период) — Dev2
- **Решения:** W4A.2 (оба файла отдельно) · W4A.5 (CSV сейчас + XML EnterpriseData позже + XLSX-бланк Т-13) · W4A.8 (месяц для Т-13, период для управленки) · W4A.9 (только APPROVED в 1С) · W6B.2 (дата-first; сотрудник-first пресет — roadmap) · W6B.13 (русские заголовки, точные имена 1С — по запросу).
- **Что менять:** дельта к WI-15/WI-28 — два пресета выгрузки (Т-13 месячный кадровый / управленческий проектный за период), оба только APPROVED, CSV `;`+BOM+запятая (WI-54). XML EnterpriseData + XLSX — roadmap.
- **Зона:** Dev2 · **Усилие:** M · **Риск:** two-phase (после WI-50 PII + WI-04 download) · **Приоритет:** P1. **Дельта к WI-15/WI-28.**

### WI-65. Коды Т-13 расширить: +В +У +Р +НН-счётчик — Dev2
- **Решения:** W4A.3/W4A.4 (наши 6 Я/ОТ/Б/ДО/К/НН + В выходной из календаря) · W6B.16 (+У учебный, +Р декрет — Timetta подтвердила) · W6B.17 (НН + счётчик «N неизвестных типов», не тихо) · W6B.18 (явка приоритетна — заказчику на подтверждение). Запрос: справочник кодов ЗУП заказчика (W4A.3).
- **Что менять:** справочник кодов Т-13 — добавить В/У/Р; В проставлять из workdayCalendar для выходных; неизвестные → НН + счётчик в выгрузке.
- **Зона:** Dev2 · **Усилие:** S · **Риск:** additive · **Приоритет:** P1. **Дельта к WI-15.** Блокер: справочник ЗУП от заказчика.

### WI-66. «Не требует согласования» — терминальный визуал (не вечный DRAFT) — Dev1
- **Решения:** W5A.21 (записи проектов без approvalRequired навсегда «черновик» = вводит в заблуждение; факт учитывается, процесса нет) · Kimai: вне approval-flow = авто-принято.
- **Что менять:** `my-time/*` + таймшит — для записей проекта без `approvalRequired` показывать статус «Не требует согласования» (не «Черновик»). Чисто визуал, без новой логики.
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive · **Приоритет:** P1.

### WI-67. Бейдж периода «3/4 согласовано» + частичный reject «N/M» — Dev1
- **Решения:** W5A.3 (бейдж «3/4 согласовано», считать по остатку) · W5A.26 (частичный reject → «частично отклонено N/M» + баннер причины) · W5A.4 (incremental submit «досылает N черновиков» с предупреждением) · W6A.23 (тултип «Согласовано 3 из 4 записей. Остальные — черновики/на согласовании»).
- **Что менять:** агрегат-бейдж периода в «Мои периоды»/таймшит — счётчики согласовано/всего, частично-отклонено N/M + баннер причины. Связь WI-18/WI-19.
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive · **Приоритет:** P1. **Дельта к WI-18/WI-19.**

### WI-68. recall-границы + уведомления revoke/recall + причина revoke — Dev1+Dev2
- **Решения:** W5A.13 (recall всё время в SUBMITTED, без флага «открыто») · W5A.14 (частичный recall разрешён) · W5A.27 (уведомление сотруднику при revoke + поле причины revoke) · W5A.28 (уведомить руководителя «сотрудник отозвал»). Доставка — in-app/бейдж (email-канал блокирован, WI-40).
- **Что менять:** `approval.logic.ts` — поле `revokeReason` (по аналогии reject-comment); recall/revoke шлют in-app уведомление. UI — кнопки/баннеры.
- **Зона:** Dev1+Dev2 · **Усилие:** M · **Риск:** additive · **Приоритет:** P1. **Дельта к WI-10/WI-40.**

### WI-69. «Акт» — снимок APPROVED, запрет дубля период×клиент, по APPROVED-части — Dev2+Dev1
- **Решения:** W5A.16 (Акт = снимок, revoke не влияет) · W5A.17 (только APPROVED + предупреждение «N часов не вошли») · W5A.18 (запрет дубля период×клиент) · W5A.19 (по APPROVED-части, не блокировать до полного согласования). Запрос заказчику: строго целиком ИЛИ по части (W5A.17/19).
- **Что менять:** дельта к WI-19 (кнопка «Создать акт») — акт фиксирует снимок APPROVED-записей; уникальность период×клиент; предупреждение о несогласованном остатке.
- **Зона:** Dev2 (logic/снимок) +Dev1 (кнопка/предупреждение) · **Усилие:** M · **Риск:** additive · **Приоритет:** P1. **Дельта к WI-19.**

### WI-70. Util/load двойная метрика + пороги из настроек — Dev2+Dev1
- **Решения:** W4A.24/W5B.19 (util=client/fact + вторая метрика load=fact/norm рядом) · W4A.20 (util по отделам + план/факт + тренд) · W6B.30 (пороги util из настроек, дефолт 70/40) · W6C.20 (два порога свободен <70%/запас <90%) · W6B.10/W6B.11 («—» при факт-режущем drill; скрывать util в срезах одной категории). NB: без billable.
- **Что менять:** `reports-calc.ts` — вторая метрика load=fact/norm рядом с util=client/fact; пороги в settings (целевая утилизация). UI — KPI обе метрики, «—»/скрытие где неприменимо.
- **Зона:** Dev2 (метрика/порог) +Dev1 (KPI) · **Усилие:** M · **Риск:** additive · **Приоритет:** P1. **Дельта к WI-14.** Запрос заказчику: целевая утилизация (W6B.30).

### WI-71. defaultCapacityFactor — clamp [0..1.5] + смысл в доке — Dev2
- **Решения:** W5B.12 (clamp [0..1.5]) · W5B.9/W5B.10 [DONE: единый defaultCapacityFactor по SIGNALS] · W5B.11 (0.8=накладные, отпуска вычитаются сверх — зафиксировать смысл в доке).
- **Что менять:** добавить clamp [0..1.5] на capacityFactor в форме/валидации; задокументировать смысл (наш 0.8 = накладные, НЕ Timetta-ёмкость=график−отсутствия).
- **Зона:** Dev2 · **Усилие:** XS · **Риск:** additive · **Приоритет:** P1.

### WI-72. Детальный отчёт — расширение полей (см. арх-находка §2) — Dev2
> Вынесено в Часть-2 §2 (арх-ценность detail-отчёт 40+ полей). Кросс-ссылка.

---

## P2 — улучшения/состояния/форматы (волна-2)

### WI-73. Микротексты/форматы SSOT (W6-A финализация) — Dev1
- **Решения:** W6A.1 («Gap»→«Баланс» в коде) · W6A.2 (единицы «Свободно, ч»/«Загрузка, %») · W6A.4 (модуль в KPI / со знаком в ячейке) · W6A.24/W6A.27/W6A.28 (числа/проценты/null-vs-zero SSOT) · W6A.25 (даты «13–19 янв 2026» en-dash) · W6A.26 (свежесть: ≤7д относительная) · W6A.34 (шапки им.падеж + единица) · W6A.10 (пунктуация состояний) · W6A.7 («…» единым символом) · W6A.32 (порядок тулбаров) · W6A.33 (a11y aria-label).
- **Что менять:** сквозной UX-словарь — «Gap»→«Баланс» (D4.1 не реализовано, видимый разнобой), единицы/форматы/пунктуация/даты/null-vs-zero по SSOT. Делать вместе с WI-27 (UX-словарь волны-1).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive · **Приоритет:** P2. **Дельта к WI-27/WI-21.**

### WI-74. Пустые состояния/тултипы/скелетоны (W6-A гр.2/4/6) — Dev1
- **Решения:** W6A.6-W6A.13 (empty без полноэкранного в гриде, плейсхолдер-норма, тексты verbatim, deep-link «Открыть настройки», «Календарь за {year} не загружен») · W6A.20-W6A.23 (тултипы формул/метрик/бейджей) · W6A.29-W6A.31/W6A.35 (сокращения шапки+тултип, скелетон повторяет лейаут, плейсхолдер=пример формата).
- **Что менять:** дельта к WI-21 (StateView) — тексты W6, тултипы, скелетоны по лейауту.
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive · **Приоритет:** P2. **Дельта к WI-21/WI-35.**

### WI-75. Подтверждения/undo (W6-A гр.3) — Dev1
- **Решения:** W6A.14-W6A.19 (SSOT-склонения, обратимое→undo-toast/необратимое→confirm с диапазоном, глаголы на кнопках, submit→toast «Отправлено N» + уточнение если REJECTED в выборке, плейсхолдер reject «Причина отклонения (увидит сотрудник)…»).
- **Что менять:** дельта к WI-33 (undo-тост) — финализация текстов/склонений/confirm-диапазонов.
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive · **Приоритет:** P2. **Дельта к WI-33.**

### WI-76. Отчёты-детали (W6-B) — drill-до-записей + CSV=экран + норма-колонка — Dev1+Dev2
- **Решения:** W6B.1 (+Комментарий в detail-CSV) · W6B.3 (+«Норма, ч» явным числом) · W6B.4 (+РП +Срок в «Проекты») · W6B.5-W6B.7 (PREFERRED_NEXT drill, терминал=лист записей, inline-панель под строкой) · W6B.8 🟡 (CSV=экран: расширить detail-фильтры на category/workType) · W6B.9-W6B.11 (KPI под сущности) · W6B.15 (отдел/срез в имени файла) · W6B.20-W6B.22 (период мес/кв/год, тренд 12 точек, будущее для план-срезов).
- **Что менять:** `reports/use-drill.ts`+`drill-view.tsx` — inline-лист записей на терминальном уровне (computeDetail готов); фикс W6B.8 (CSV-фильтры = экранные оси); detail-CSV +Комментарий; норма-колонка. (Файлы `use-drill.ts`/`use-drill.test.ts` в git-статусе — свериться.)
- **Зона:** Dev1 (drill UI) +Dev2 (detail-фильтры/колонки) · **Усилие:** M · **Риск:** additive · **Приоритет:** P2. **Дельта к WI-14.**

### WI-77. Табель Т-13 вкладка — UI поверх готового бэка (быстрая победа) — Dev1
- **Решения:** W6B.28 (сетка сотрудник×день + CSV, бэк готов `/s/reports?groupBy=timesheet-grid`) · W6B.19 (тоггл «показать коды») · W6B.26/W6B.27 (экспорт текущего скоупа; реальный download — host-bridge, WI-04).
- **Что менять:** SIGNALS показывает экран «Табель Т-13» уже частично сделан (frontComponent в дереве) — **свериться с дельтой**, доделать тоггл кодов + CSV (текст-поповер до host-bridge).
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive · **Приоритет:** P2. **Дельта к WI-14/WI-15.** Статус: частично сделано (проверить).

### WI-78. Планирование-точные-правила (W6-C) — переименование/факт-overlay/booking — Dev1
- **Решения:** W6C.15 («План»→«Спрос/Demand») · W6C.16 (гибрид: прошлое=факт, будущее=план overlay) · W6C.7 (две полосы план/ёмкость) · W6C.1-W6C.6 (подписи раскида, привязка к числу колонок, дельта Σ vs план) · W6C.21-W6C.25 (кнопка «Подтвердить→HARD», SOFT всегда видна+флаг учёта в gap, tooltip перегруза, бронь+план разные оси) · W6C.30 («сегодня»-линия в баре).
- **Что менять:** дельта к WI-12/WI-48 — переименовать «План»→«Спрос», факт-overlay прошлых периодов, две полосы, подписи. Большинство W6-C ратифицирует W3B (часть уже сделана — SIGNALS endDate/овербукинг done).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive · **Приоритет:** P2. **Дельта к WI-12/WI-48.**

### WI-79. Масштаб: серверная агрегация тяжёлых срезов (доска/limit:500) — Dev2
- **Решения:** W4C.26 🟡 (limit:500 без hasNextPage + deptLoadCells на фронте сломаются при росте) · W4C.27 (виртуализацию отложить — 72 чел) · W4C.30 (распараллелить 12 страниц календаря) · W4C.29 (кэш справочников — запрос Dev2 про RemDOM).
- **Что менять:** доска/тяжёлые срезы — серверная агрегация с пагинацией (отчёты уже так). Распараллелить waterfall календаря. Приоритет архитектуры до роста числа проектов.
- **Зона:** Dev2 · **Усилие:** M · **Риск:** behavior-change · **Приоритет:** P2 (задел до роста).

---

## ОТЛОЖЕНО — go-ahead заказчика (НЕ в волне-2)

- **Финансы/деньги** (W4A.18/W4A.19): cost-rate на сотруднике + bill-rate на проекте, маржа, профитабилити, T&M-выручка, себестоимость в detail-отчёте. **NB-режим сейчас** [[no-billable-concept]]. Включать ТОЛЬКО по явному go заказчика. Detail-отчёт (Часть-2 §2) проектируется БЕЗ финансовых полей, с заделом под добавление.
- **RBAC/server-identity** (CISO-005): сквозной прод-блокер для W4B.* / W5A.5 / W5A.7. Ответы W4-B = целевой UX ПОСЛЕ маппинга ролей. До CISO-005 — UI disabled+tooltip, сервер 403 на аномалии. Реализация RBAC-гранул (в т.ч. «принудительное изменение состояния», арх-находка §3) — после go.
- **Дашборды по ролям** (W4A.22), **остатки отпусков** (W4A.25в — нужен ввод начислений), **мобайл день-карточка** (W4C.4б), **аудит-лог полный** (W4A.29 — MVP только флаг «изменено после согласования»), **многоуровневый workflow-маршрут** (арх §3 — MVP 1 уровень) — roadmap.

---

# ЧАСТЬ 2. АРХ-ЦЕННОСТЬ Timetta/Kimai — «Как внедрить» (6 находок)

> Заказчик: «изучи и посмотри как внедрить». Для каждой — план (объект/поле/логика/UI), additive-совместимость [[twenty-sdk-apply-gotchas]], NB-проверка, MVP-границы, приоритет.

## §1. Движок валидации таймшита (10 типов правил, конфиг на шаблоне)

**Находка:** Timetta — конфигурируемый движок, 2 уровня (Ошибка-блок / Предупреждение), кастомный текст, 10 типов правил. Наш `validateEntry`+`validateWeekUnderfill` — частный случай (хардкод).

**Как внедрить:**
- **MVP (НЕ переусложнять):** оставить хардкод-правила, НО добавить 3-4 правила-кандидата сверх текущих как явные функции в `constants/validation.ts`: «мин. число заполненных строк», «запрет пустых строк», «отклонение от нормы в %/ч» (есть только недобор), «превышение плановых трудозатрат проекта». Каждое — 2 уровня (ERROR/WARNING) + текст.
- **Задел (roadmap, НЕ MVP):** вынести правила в структуру `{ ruleType, level, threshold, text }` на уровне настроек/шаблона — конфигурируемый набор. Сейчас не строить полный движок (over-engineering на 72 чел) — только разложить хардкод в перечисляемый список с единым контрактом, чтобы потом сделать конфиг без переписывания.
- **additive:** новые правила — чистые функции, additive. Конфиг-поля (если делать) — additive TEXT/NUMBER на settings.
- **NB:** правила «актуальный центр затрат»/«выбор тарифа» из Timetta — пропустить (NB).
- **MVP-граница:** системно закрывает A4.6/UC-VAL-03, но НЕ строить движок целиком — добавить правила + единый контракт. **Приоритет: P1** (правила «мин.строк»/«превышение плана» полезны), движок-конфиг **P2/roadmap**.
- **Зона:** Dev2 (правила) +Dev1 (тексты/поповер submit). **Усилие:** M (правила) / L (движок — отложить).

## §2. Detail-отчёт — полная модель полей (UC-RPT-10 «40+ полей») [WI-72]

**Находка:** Timetta detail-отчёт = 40+ полей в 6 группах. Наш detail-CSV = 7 колонок.

**Как внедрить:**
- **Применимые поля (БЕЗ NB-финансов):** дата · орг-иерархия (проект→менеджер→подразделение) · работа+этап+роль · сотрудник+подразделение+уровень+**руководитель (SupervisorId)** · тип отсутствия · разбивка часов (HoursTotal/HoursProjects/HoursTimeOffs).
- **Пресеты:** «краткий» (текущие 7) / «полный» (расширенный набор) — связать с WI-37 (чек-лист колонок per-view).
- **SupervisorId как поле записи** — Timetta тянет руководителя в строку для группировки «по руководителю». У нас scope по дереву отделов (W4B.7) — поле supervisor дало бы прямую иерархию в отчёте. Резолв из EmployeeDepartment-истории на дату (связь WI-53).
- **«Нормализованные часы» (Normalized)** — концепт приведения к норме для util/FTE-корректировки (связь WI-58 план÷FTE). MVP: НЕ вводить отдельную колонку, держать в уме для util.
- **additive:** расширение `computeDetail` — additive колонки. Резолв иерархии — read-time (без новых полей записи, кроме опц. supervisor-кэша).
- **NB:** группа «Финансы» (Счёт/Выручка/Тариф/Себестоимость) — НЕ включать. Структуру строк оставить расширяемой под финансы после go (ОТЛОЖЕНО).
- **MVP-граница:** расширить до применимого набора пресетами; финансы и Normalized — задел. **Приоритет: P1** (закрывает UC-RPT-10 + W6B.1/3/4).
- **Зона:** Dev2 (`reports-detail.ts`). **Усилие:** M. Связь WI-50 (PII по reveal в новых колонках тоже!), WI-76, WI-37.

## §3. Workflow-движок согласования — подтверждение + задел маршрута

**Находка:** Timetta подтверждает наш дизайн (идемпотентный approve, SoD-skip автора, admin-override, запрет mixed-state batch, история в Activity). Чего нет: многоуровневый конфиг-маршрут + «принудительное изменение состояния» = отдельная гранула RBAC.

**Как внедрить:**
- **СЕЙЧАС (подтверждения — реализовать смело):** все W5A-фиксы валидны (WI-55 гонки/mixed-batch, WI-56 аудит, WI-57 reject-defense). Это прямое подтверждение — приоритет P0 у соответствующих WI.
- **Задел (НЕ MVP):** модель состояний заложить под многоуровневость — поле «маршрут/шаг» на записи/периоде, но MVP = 1 уровень (isManager). Закладка означает: не хардкодить «1 согласующий» намертво, а оставить точку расширения. Полный конфиг-маршрут (роль/человек × уровни) — roadmap (закрывает A4.17 кто согласует руководителя, W4B.2 РП vs начальник).
- **«Принудительное изменение состояния» = гранула RBAC** (не просто isManager) — связь CISO RBAC (W4B.3), ОТЛОЖЕНО до CISO-005.
- **additive:** поле «шаг/маршрут» — additive. Не вводить пока нет спроса.
- **NB:** нет финансовых аспектов.
- **MVP-граница:** реализовать подтверждённые фиксы (P0), маршрут — только архитектурная закладка, НЕ код. **Приоритет: подтверждения P0 (WI-55/56/57); маршрут P2/roadmap.**
- **Зона:** Dev2.

## §4. Тип периода «недельный по конец месяца» (решает W5C.7/W4A.8) [рекомендуемый дефолт РФ]

**Находка:** Timetta — 5 типов периода, scope=подразделение. Тип «недельный по конец месяца»: неделя на стыке месяцев разбивается на 2 таймшита (Пн-Чт авг + Пт-Вс сен) → каждый таймшит в одном месяце.

**Как внедрить:**
- **Прямое решение конфликта** «недельный таймшит vs месячный Т-13/1С» (W4A.8, W5C.7): неделя не пересекает месяц → чистая месячная агрегация для 1С без «дележа» записей вручную.
- **План:** добавить тип периода `WEEKLY_MONTH_END` в логику недель (`grid/use-week.ts` + период-агрегация). Неделя со стыком месяцев разбивается на два под-периода по границе месяца; submit/approve по под-периоду. **Рекомендую дефолтом для РФ** (месячный учёт зарплаты).
- **Per-department scope** — отвечает W5B.26 (период привязан к подразделению, не глобально) + «произвольный» тип для миграции «окна» при смене.
- **additive:** новый тип в enum периода — additive (опции enum не удаляем — [[twenty-sdk-apply-gotchas]]: drop опций падает; только добавляем). Per-department — additive поле.
- **NB:** нет.
- **MVP-граница:** реализовать ОДИН тип `WEEKLY_MONTH_END` как дефолт (разбиение по границе месяца), НЕ все 5 типов Timetta. Полумесячный/произвольный — roadmap. Per-department scope — после, если отделы реально хотят разные периоды (сейчас глобально + закладка).
- **Приоритет: P1** (решает реальный конфликт неделя↔месяц для 1С).
- **Зона:** Dev2 (логика разбиения) +Dev1 (отрисовка стыковой недели). **Усилие:** M. Связь WI-51 (date-день), WI-64 (месячная выгрузка).

## §5. Kimai exported-флаг — read-only после выгрузки в 1С (усиливает CASCADE-защиту)

**Находка:** Kimai — `exported=true` после экспорта → запись read-only. Слой защиты СВЕРХ APPROVED-lock (согласовано ≠ выгружено).

**Как внедрить:**
- **План:** +BOOLEAN `exported` (+`exportedAt`) на записи. При формировании выгрузки кадрам/1С (WI-64) — проставлять `exported=true` на вошедшие записи. Запись с `exported=true` → read-only (правка/удаление блокируется, как APPROVED-lock, но строже).
- **Усиливает CASCADE-защиту:** CASCADE→RESTRICT уже сделан (SIGNALS, CISO-015). exported-записи тем более нельзя терять каскадом — добавить в guard project/employee delete: если есть exported-записи → RESTRICT безусловно. = 3 уровня защиты прошлого учёта (APPROVED → exported → CASCADE-RESTRICT). Связь G3.16/W5C.23.
- **additive:** поля `exported`/`exportedAt` — additive BOOLEAN/DATE. Guard — расширение существующего RESTRICT.
- **NB:** нет.
- **MVP-граница:** поле + read-only guard + проставление при выгрузке. lockdown-period целиком (Kimai §) — roadmap РФ-аудита.
- **Приоритет: P1** (защита учёта, прямая связь с уже сделанным CASCADE-RESTRICT — логичное продолжение). SIGNALS уже отметил это как «backlog РФ-аудита».
- **Зона:** Dev2. **Усилие:** S-M. Связь WI-64, CISO-015.

## §6. Kimai meta-fields — кастом-поля без миграций (для 1С-кодов)

**Находка:** Kimai — произвольные key-value поля на сущностях без schema-миграций.

**Как внедрить:**
- **Применение:** будущие кастом-поля (центр затрат, табельный №, accountingCode из WI-63) без новой миграции каждый раз.
- **Twenty SDK — проверить customFields:** Twenty даёт нативные customFields. **Проверить, покрывает ли паттерн** и НЕ падает ли на apply при частом field-add ([[twenty-sdk-apply-gotchas]]: drop полей/labelIdentifier-в-том-же-sync/`name`-резерв падают). Если customFields стабильны — использовать их вместо россыпи TEXT-полей в WI-63.
- **additive:** customFields по природе additive; ключевой риск — apply-грабли при удалении (не удалять, только добавлять).
- **NB:** нет.
- **MVP-граница:** для WI-63 (personnelNumber/externalId/accountingCode) — СНАЧАЛА проверить customFields-путь; если рискованно для apply — явные поля. НЕ строить собственный key-value слой (over-engineering — Twenty уже даёт механизм).
- **Приоритет: P2** (исследование перед WI-63 — определяет способ хранения 1С-полей).
- **Зона:** Dev2 (research+решение). **Усилие:** S (research).

---

# ЧАСТЬ 3. СВОДНАЯ ТАБЛИЦА ПРИОРИТЕТОВ

## P0 — остаток/критичные (волна-2)

| WI | Что | Зона | Усилие | Статус |
|----|-----|------|--------|--------|
| **WI-50** | detail-CSV/экспорт ФИО по reveal (4-й P0, утечка ПДн) | Dev2 | S | открыт |
| **WI-51** | date→чистый день + нормализатор (двойной счёт/сдвиг суток) | Dev2 | M | открыт |
| **WI-52** | hours=0 → ERROR (держит ключ) | Dev2 | XS | открыт |
| **WI-53** | отчёты по отделу НА ДАТУ (не primary) | Dev2 | M | открыт |
| **WI-54** | CSV запятая для дробных часов (RU-локаль) | Dev2 | XS | открыт |
| **WI-55** | гонки согласования: CAS-skip + collect-errors | Dev2 | M | открыт |
| **WI-56** | аудит revoke: revokedBy/resolvedBy (корректирует WI-10) | Dev2 | M | открыт |
| **WI-57** | reject-defense полноты submit (CISO-005) | Dev2 | S | открыт |
| — | submit-REJECTED · capacityFactor единый · CASCADE→RESTRICT | Dev2 | — | **[DONE] SIGNALS** |

## P1 — ядро запроса (волна-2)

| WI | Что | Зона | Усилие |
|----|-----|------|--------|
| WI-58 | план ÷ FTE (фикс завышенной util) | Dev2 | M |
| WI-59 | снапшот нормы/headcount (или версионность REQ-0011) | Dev2 | L (MVP может S) |
| WI-60 | сетка норма из календаря (убрать фикс-8/40) | Dev1 | S |
| WI-61 | weekStartsOn: убрать Вс, жёстко Пн | Dev1+Dev2 | XS |
| WI-62 | рабочая суббота WORKDAY=8 (проверка) | Dev1 | XS |
| WI-63 | 1С-поля: personnelNumber/externalId/accountingCode | Dev2 | M |
| WI-64 | выгрузка 2 файла (Т-13 месяц + управленка период) | Dev2 | M |
| WI-65 | коды Т-13 +В/У/Р + НН-счётчик | Dev2 | S |
| WI-66 | «Не требует согласования» терминальный визуал | Dev1 | S |
| WI-67 | бейдж «3/4» + частичный reject N/M | Dev1 | S |
| WI-68 | recall-границы + revoke-причина + уведомления | Dev1+Dev2 | M |
| WI-69 | Акт: снимок APPROVED + запрет дубля + по части | Dev2+Dev1 | M |
| WI-70 | util/load двойная метрика + пороги настроек | Dev2+Dev1 | M |
| WI-71 | capacityFactor clamp [0..1.5] + смысл в доке | Dev2 | XS |
| **§1** | движок валидации: +3-4 правила (мин.строк/пустые/откл.%/превыш.плана) | Dev2 | M |
| **§2 / WI-72** | detail-отчёт расширить (без NB) + supervisor + пресеты | Dev2 | M |
| **§4** | период «недельный по конец месяца» (дефолт РФ) | Dev2+Dev1 | M |
| **§5** | exported-флаг read-only (усиливает CASCADE) | Dev2 | S-M |

## P2 — улучшения/задел (волна-2)

| WI | Что | Зона | Усилие |
|----|-----|------|--------|
| WI-73 | микротексты/форматы SSOT (W6-A) «Gap»→«Баланс» | Dev1 | M |
| WI-74 | empty/тултипы/скелетоны (W6-A) | Dev1 | M |
| WI-75 | подтверждения/undo финал (W6-A) | Dev1 | S |
| WI-76 | drill-до-записей + CSV=экран + норма-колонка | Dev1+Dev2 | M |
| WI-77 | Табель Т-13 вкладка (частично сделано — сверить) | Dev1 | S |
| WI-78 | планирование W6-C: «План»→«Спрос», факт-overlay | Dev1 | M |
| WI-79 | масштаб: серверная агрегация тяжёлых срезов | Dev2 | M |
| **§3** | workflow-маршрут (только закладка, НЕ код) | Dev2 | — |
| **§6** | meta/customFields research перед WI-63 | Dev2 | S |

## ОТЛОЖЕНО (go-ahead заказчика)
- **Финансы** (cost/bill-rate, маржа, выручка) — W4A.18/19, NB-режим. Detail-отчёт проектируется с заделом.
- **RBAC/CISO-005** (server-identity, гранулы) — W4B.*, многоуровневый workflow, «принудительное изменение».
- **Roadmap:** дашборды по ролям, остатки отпусков, мобайл день-карточка, полный аудит-лог, XML EnterpriseData, полумесячный/произвольный период, lockdown-period.

---

## РЕКОМЕНДУЕМЫЙ ПОРЯДОК (волна-2 поверх волн 1/5/3)

**Сначала — закрыть 4-й P0 + целостность данных (Dev2, последовательно — затрагивают reports/approval/schema):**
WI-50 (PII) → WI-51 (date-день) → WI-52 (0-часов) → WI-54 (CSV-запятая) → WI-53 (отдел-на-дату) → WI-55 (гонки) → WI-56 (аудит revoke) → WI-57 (reject-defense).

**Затем P1 расчётные фиксы + 1С-задел (Dev2):**
WI-58 (план÷FTE) → WI-71 (clamp) → WI-63 (1С-поля, после §6-research) → §4 (период конец-месяца) → §5 (exported) → WI-64/65 (выгрузка/коды) → §2/WI-72 (detail-расширение) → §1 (правила).

**P1 фронт (Dev1, additive):**
WI-60/61/62 (норма/неделя) → WI-66/67 (статус-визуал) → WI-68/69/70 (recall/акт/util).

**P2 полировка:** WI-73..79 + §3 (закладка) + §6 (research).

**Hot-file предупреждения волны-2:**
- `reports-detail.ts` — WI-50/54/64/72 (Dev2 последовательно).
- `reports-calc.ts` — WI-53/58/70 (Dev2; уже в git-статусе — координировать с идущей правкой).
- `approval.logic.ts` — WI-55/56/57/68/69 (Dev2 последовательно; `approval.logic.test.ts` в git-статусе).
- `use-drill.ts` — WI-76 (Dev1; уже в git-статусе).
- `constants/validation.ts` — WI-52/60/§1 (Dev2).
- `grid/use-week.ts` — WI-61/62/§4 (Dev1).

**MVP-дисциплина [[keep-it-simple]]:** §1 движок — НЕ строить целиком (только правила+контракт); §3 маршрут — только закладка; §5 exported — поле+guard, НЕ lockdown; WI-59 снапшот — сначала проверить, хватает ли версионности REQ-0011.
