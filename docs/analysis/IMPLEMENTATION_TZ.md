# Реализационный ТЗ-бэклог (work-items) по UI-решениям заказчика

**Дата:** 2026-06-21
**Автор:** арх-аналитик AITEAM
**Статус:** ТЗ к раздаче Dev (READ-ONLY свод, код не трогался)
**Источники решений:** `UI_ANSWERS.md` (A·B·C·D, 359 решений) · `TIMESHEET_CELL_CONTROLS.md` (§F, рекомендация C1) · `PLANNING_MODELS.md` · `PLANNING_DIALOG_UX.md`
**Сверка с кодом:** `apps/time/src/front-components/{grid,capacity,reports,my-time,settings,shared}`, `logic-functions`, `views`, `navigation-menu-items`

---

## Легенда

- **Зона:** Dev1 = фронт (front-components, UX), Dev2 = бэк (logic-functions, objects, calendar/calc, presigned-URL), QA = тесты.
- **Усилие:** S ≤ 0.5д · M ≈ 1-2д · L ≥ 3д.
- **Приоритет:** P0 = баг/блокер · P1 = ядро запроса заказчика · P2 = улучшение/задел.
- **Риск (twenty-sdk-apply-gotchas):** *additive* = только добавляем (безопасно) · *behavior-change* = меняем существующее (нужен регресс) · *two-phase* = сначала бэк, потом фронт (или schema → данные) · *RemDOM* = ограничение Remote DOM (нет host-DOM/drag/hover/Blob/window).

---

## P0 — Баги и блокеры (волна 1)

### WI-01. Переделка контролов ячейки (клик = ввод, контролы из ячейки в меню строки) — C1
- **Решения:** A1.24, A1.25, A1.26, A1.27 · TIMESHEET §F2 (В1-В15), рекомендация C1 · D4.6, D4.9, D4.12, D4.13
- **Что менять:**
  - `grid/hour-cell.tsx` — убрать конфликт `onClick`(=активировать+редактировать)/`onMouseDown`; клик = сразу inline-ввод; убрать плавающие кнопки чип-«8»/⇥/✎ из ячейки. Dot-маркер коммента оставить (крупнее, 6-7px).
  - `grid/row-menu.tsx` — добавить пункты: «Заполнить будни нормой», «Комментарий к [день]…» (по выбранной ячейке); переименовать на «Обнулить часы»/«Убрать строку» (A1.26).
  - `grid/cell-comment.tsx` — переключить открытие из ячейки на пункт меню строки.
- **Зона:** Dev1 · **Усилие:** M · **Риск:** behavior-change + RemDOM (убираем flaky-hover — это и есть фикс) · регресс-тесты обязательны.
- **Приоритет:** P0 (корневой UX-баг «контролы недостижимы мышью»).

### WI-02. Норма дня SSOT — убрать хардкод DAILY_NORM_HOURS из bulk-fill
- **Решения:** A1.12, A1.24, A4.8, B4.3, D4.8 · TIMESHEET §F6
- **Что менять:**
  - `grid/use-timesheet-actions.ts:95-115` (`calcFillStandardWeek`) — `DAILY_NORM_HOURS` → норма дня через `useDailyNorm(date)`. Сейчас тулбар льёт хардкод 8, чип льёт `norm` → рассинхрон.
  - `grid/use-timesheet-actions.ts:91,108` и `weekly-grid.tsx:101` — все точки fill на норму календаря.
  - `settings`: `fillTemplateHours` → алиас/убрать (B4.3), один источник `normHoursPerDay` = `useDailyNorm` fallback.
  - `useDailyNorm` уже SSOT (use-daily-norm.ts) — задача: подключить его во ВСЕ fill-пути, а не дублировать константу.
- **Зона:** Dev1 (+Dev2 если меняется поле settings) · **Усилие:** S · **Риск:** behavior-change (короткий день → 7ч вместо 8) · регресс на format.test/use-timesheet-actions.test.
- **Приоритет:** P0.

### WI-03. Производственный календарь SSOT — isWeekend → isNonWorkingDay
- **Решения:** A1.15, C1.14, D4.7 · TIMESHEET §F6, В23 (рабочие субботы)
- **Что менять:**
  - `grid/use-week.ts:20,59` — `isWeekend: i>=5` хардкод заменить на признак из календаря (рабочая суббота = рабочий день).
  - `grid/use-timesheet-actions.ts:76,102,226` (`calcCopyWithHours`, `calcFillStandardWeek`, `bulkFill`) — фильтр `target.isWeekend` → «нерабочий по календарю».
  - `reports/timesheet-grid/timesheet-grid.tsx:18,112,131` (`isWeekendIso`) — то же.
  - `logic-functions/reports-calc.ts` + `gaps.ts` — норма/недобор по календарю.
  - Нужен фронт-предикат `isNonWorkingDay(iso, calendar)` поверх `fetchCalendarYear` (его нет — см. карту: «isNonWorkingDay не найдено»).
- **Зона:** Dev2 (источник календаря/предикат) + Dev1 (потребление) · **Усилие:** M · **Риск:** two-phase (предикат → потребители) + behavior-change.
- **Приоритет:** P0 (рабочие субботы у заказчика есть → сейчас баг).

### WI-04. Скачивание файлов через backend presigned URL (CSV/XLSX/PDF)
- **Решения:** A3.21, C4.1, C4.2, C4.18, C4.6, C4.3 (XLSX+PDF)
- **Что менять:**
  - `reports/export-csv.tsx` — клиентский Blob/anchor.download недоступен в RemDOM. Заменить на запрос к бэку → presigned URL → переход по ссылке.
  - `logic-functions` — новая logic-function генерации файла (CSV `;`-разделитель, XLSX, позже PDF) + выдача URL.
- **Зона:** Dev2 (генерация+URL) + Dev1 (кнопка/ссылка) · **Усилие:** L · **Риск:** two-phase, RemDOM (главный блокер экспорта).
- **Приоритет:** P0.

### WI-05. Расчёт раскида плана по рабочим дням (фикс «часы капают в субботу»)
- **Решения:** PLANNING_MODELS B2/рек.№1 · PLANNING_DIALOG Q14/Q15, Фаза-1 п.1
- **Что менять:**
  - `capacity/calc-load.ts:189-205` (`plannedHoursInPeriod`) — делитель = Σ рабочих часов диапазона (через существующий `workHoursBetween`/`hoursByDay`), а не `endDate−startDate+1` календарных. Инвариант Σ=plannedEffort сохранить.
  - Применяется ко всем потребителям: проекты, доли отделов, `dept-plan`, брони (`bookingHoursInPeriod`).
- **Зона:** Dev2 (calc) · **Усилие:** M · **Риск:** behavior-change (числа на доске сдвинутся — это и есть исправление; согласовать с заказчиком как «теперь план бьётся с ёмкостью»).
- **Приоритет:** P0 (корень непонимания заказчиком модели планирования).

### WI-06. Bulk-fill: развести три действия в два с разными именами
- **Решения:** A1.10, A1.11, A1.13, A1.14 · TIMESHEET §F3/F5, C4
- **Что менять:**
  - `grid/toolbar.tsx:84-92` — «Заполнить 8×5» → «Заполнить неделю для всех» (все строки, норма дня, только пустые будни).
  - `grid/row-menu.tsx` — «Заполнить будни нормой» (эта строка, норма дня, пустые будни).
  - Чип ⇥ (значение ячейки) — убрать (источник «странного» поведения на 2+ строк) — закрывается в WI-01.
  - A1.11 «Очистить неделю» вынести из CopyMenu в отдельную кнопку с confirm (D4.11/D4.12 — «Очистить неделю», счётчик записей).
- **Зона:** Dev1 · **Усилие:** S · **Риск:** behavior-change · UX-copy из §F5.
- **Приоритет:** P0 (баг неоднозначности).

### WI-07. Explicit lockedByDay-guard в copy-week (APPROVED)
- **Решения:** TIMESHEET §F6 (QA UC-TS-10) · A4.24, B2.19 (CISO-011/012)
- **Что менять:** `grid/use-timesheet-actions.ts:50-78` (`calcCopyWithHours`) — добавить явный guard пустых APPROVED-ячеек (сейчас copy-week не проверяет lockedByDay для пустых).
- **Зона:** Dev1 · **Усилие:** XS · **Риск:** additive (закрывает дыру).
- **Приоритет:** P0 (compliance).

---

## P1 — Ядро запроса заказчика (волны 2-3)

### WI-08. Фильтры таймшита: мультиселект-чипы + пилюли + запоминание
- **Решения:** A1.16, A1.17, A1.18, A1.19, A1.20, A1.21 · C3.1, C3.20, C3.21
- **Что менять:** `grid/filters-bar.tsx`, `filter-chip.tsx` — мультиселект-чипы для всех осей; переиспользовать `reports/filter-pills.tsx` (пилюли с ✕ + «Сбросить всё»); фильтр «Сотрудник» только manager (A1.19); фильтры = только отображение (A1.20); запоминание набора (localStorage/settings A1.21); +ось «Группа видов работ» (A1.16).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive (переиспользование готового filter-pills).
- **Приоритет:** P1.

### WI-09. Период-навигация: datepicker по клику на заголовок + диапазон дат + «неделя»
- **Решения:** A1.6, A1.7, A1.8, A1.5 · A3.17, A3.20, C3.5, C3.18, C3.19
- **Что менять:** `grid/period-nav.tsx`, `week-header.tsx` — клик по заголовку = datepicker (выбор недели/дня), кнопка «Сегодня» остаётся; формат «13–19 янв 2026»; добавить «неделя» в период отчётов; единый период-селектор компонент (C3.18); запоминать период per-раздел (A3.19/C3.19).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive.
- **Приоритет:** P1.

### WI-10. Согласование: recall (SUBMITTED→DRAFT) + отзыв согласования (APPROVED→…)
- **Решения:** A4.3, A4.4, A4.25, A4.26, A4.20 (+REVOKED)
- **Что менять:** `logic-functions/approval.logic.ts` — переходы SUBMITTED→DRAFT (recall сотрудником, пока не открыт), APPROVED→SUBMITTED/DRAFT (отзыв manager/admin); статус REVOKED; `my-time/*` + таймшит-футер — кнопки «Отозвать отправку»/«Отозвать согласование».
- **Зона:** Dev2 (logic/статусы) + Dev1 (кнопки) · **Усилие:** L · **Риск:** two-phase + behavior-change (новые переходы статусов, RBAC-гейт A4.18/A4.19).
- **Приоритет:** P1.

### WI-11. Inline-панель плана «Планировать» (способ + С/ПО + объём + живое превью)
- **Решения:** PLANNING_DIALOG Q1-Q21 (Фаза 1) · A2.12, A2.18, A2.19, A2.20, A2.21, A2.28
- **Что менять:** новый `capacity/project-plan-panel.tsx` (useState-поповер, паттерн `cell-comment`/`row-menu`); радио способа (Равномерно дефолт; Вручную — disabled «скоро»); поля «С»/«ПО» (`type=date`, «С» теперь редактируема); объём в часах; живое превью мини-баром по рабочим дням (функция-двойник из WI-05) + Σ-сверка + мягкое предупреждение овербукинга; сохранение через готовый `usePlanEdit.save`. Объект проекта НЕ менять (фаза 1).
- **Зона:** Dev1 (+Dev2 для двойника превью) · **Усилие:** L · **Риск:** additive + RemDOM (push-down поповер, не модалка); зависит от WI-05.
- **Приоритет:** P1.

### WI-12. Capacity-доска: навигация горизонта ‹›, «Сегодня», квартал, SOFT-toggle, overbooking-warn
- **Решения:** A2.10, A2.11, A2.13, A2.14, A2.15, A2.17, A2.22, A2.23, A2.24, A2.25, A2.27 · C2.5, C2.7, C2.15, C2.16, C2.17
- **Что менять:** `capacity/board-toolbar.tsx` — ‹› навигация горизонта, «Сегодня», +квартал (A2.23), дефолт 8 недель (A2.25), SOFT-toggle (A2.10), «Развернуть/Свернуть всё» (A2.17), явный «Выйти из планирования» (A2.27); `booking-marker.tsx`/`board-legend.tsx` — подпись «предв.» на SOFT (A2.11), overbooking-warn (обводка+▲) + счётчик в легенде (A2.14/A2.15); карточка брони — «Подтвердить бронь» SOFT→HARD (A2.13/C2.5).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive.
- **Приоритет:** P1.

### WI-13. Booking quick-create + FTE% + пресейл-слой
- **Решения:** C2.1, C2.2, C2.3, C2.4, C2.8, C2.9, C2.19 · A2.12, A2.29
- **Что менять:** `capacity` — клик по ячейке сотрудник×период → форма брони с предзаполнением (RemDOM: drag недоступен); обязателен project ИЛИ company (C2.2); ввод в FTE% с пересчётом в часы (C2.3); предупреждение пересечения с отпуском (C2.4); пресейл-слой (company без project) отдельным цветом (C2.9); реестр броней +колонки Компания/Заметка/overbooking (C2.19).
- **Зона:** Dev1 (+Dev2 для FTE↔часы calc) · **Усилие:** M · **Риск:** additive.
- **Приоритет:** P1.

### WI-14. Отчёты: вкладка «Табель Т-13» + drill/фильтры + утилизация + дельта
- **Решения:** A3.1, A3.5, A3.6, A3.8, A3.9, A3.10, A3.11, A3.12, A3.13, A3.14, A3.15, A3.16, A3.24, A3.27 · C3.2, C3.3, C3.4, C3.6, C3.7, C3.11, C3.14, C3.16, C3.17
- **Что менять:** `reports/reports-dashboard.tsx` — 5-я вкладка «Табель Т-13»; `drill-view.tsx`/`use-drill.ts` — «+фильтр» по оси (мультиселект OR) рядом с drill (A3.10), точечное снятие (A3.11), крошки=навигация + пилюли=кросс-фильтры раздельно (A3.12); `kpi-cards.tsx` — дельта ▲▼% (A3.15), «—» для нормы при срезе Проект (A3.16); утилизация = client_h/fact_h (A3.13/A3.14, NB: без billable); пресеты периодов (C3.6); empty-state с действием (A3.27/D1.x).
- **Зона:** Dev1 (+Dev2 для axis «Группа видов работ»/категория) · **Усилие:** L · **Риск:** additive.
- **Приоритет:** P1.

### WI-15. Т-13: backend PDF + коды/часы toggle + право «Кадры» (ФИО)
- **Решения:** A3.25, A3.26, A3.28, C4.7, C4.8, C4.9, C4.10, C4.11, C4.16, C4.17, C4.18, C4.19, C4.22
- **Что менять:** `logic-functions` — генерация полного бланка Т-13 (реквизиты из настроек), PDF с бэка (RemDOM: window.print недоступен, см. WI-04); toggle часы/коды + только-коды для бланка (A3.25); двухстрочная ячейка код/часы (C4.10); только APPROVED в выгрузку (C4.16); право «Кадры» для экспорта с ФИО (A3.26/C4.7), иначе стабильный код; лог факта экспорта с ФИО (C4.22 / 152-ФЗ).
- **Зона:** Dev2 (PDF/право/лог) + Dev1 (toggle/кнопки) · **Усилие:** L · **Риск:** two-phase + CISO-согласование.
- **Приоритет:** P1.

### WI-16. Норма дня в settings — единый источник + валидация полей
- **Решения:** B4.3, B4.4, B4.5, B4.6, B4.7, B4.8, B4.9, B4.14, B4.15, B4.16, C1.19, C1.20, C1.21, C1.22
- **Что менять:** `settings/*` — `normHoursPerDay` = SSOT (= `useDailyNorm` fallback, см. WI-02); `defaultCapacityFactor` дефолт для отделов с локальным override (B4.4/C1.21); capacityFactor хранить долей, показывать % (B4.7); min/max + суффиксы единиц (B4.6); вложить пороги под toggle (B4.5); смысловые подписи toggle (B4.8); +BIWEEK в approvalPeriod (B4.9); autosave+error-toast+inline-ошибка (B4.14/B4.15/B4.16); inline-подсказки (?) (C1.19); «переопределено» только при отличии (C1.22).
- **Зона:** Dev1 (+Dev2 для BIWEEK/поля) · **Усилие:** M · **Риск:** additive (norma-часть зависит от WI-02).
- **Приоритет:** P1.

### WI-17. Производственный календарь: генерация года + сетка/список (справочник)
- **Решения:** C1.12, C1.13, C1.14, C1.15 · B3.7, B4.22, D4.7
- **Что менять:** справочник календаря — кнопка «Сгенерировать год» (пн-пт=8ч, сб/вс=выходные) + ручная правка праздников/переносов (C1.12); месячная сетка + список toggle (C1.13); `hours` дня = приоритет, `normHoursPerDay` = fallback (C1.15); это источник для WI-03. Дубль nav: «Производственный календарь» PAGE_LAYOUT основной, VIEW скрыть от пользователя (B3.7).
- **Зона:** Dev2 (генерация/данные) + Dev1 (сетка/список) · **Усилие:** L · **Риск:** two-phase (основа для WI-03 SSOT).
- **Приоритет:** P1.

### WI-18. Личный кабинет «Мои трудозатраты»: CTA-баннеры, статусы, клик-в-таймшит
- **Решения:** B1.1, B1.2, B1.3, B1.4, B1.5, B1.6, B1.7, B1.9, B1.10, B1.11, B1.17, B1.18, B1.19 · A3.2
- **Что менять:** `my-time/*` — две вкладки (Мои часы/Мои периоды), Согласование отдельным пунктом (B1.1); CTA в баннере → проблемная неделя (B1.2/B1.12); строка периода кликабельна → таймшит (B1.3); +статус согласования и слова «Перегруз/Недобор» (B1.5/B1.6); цвет по порогу (B1.7); ругать только за прошедшие дни (B1.9); бейдж «согласовано N из M» (B1.17); «N отклонено» + причины (B1.18); фильтр год/квартал (B1.19); «Незаполненные» = вкладка отчётов (A3.2).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive.
- **Приоритет:** P1.

### WI-19. Approval-очередь руководителя (batch per-сотрудник, reject с причиной, уведомления, акт)
- **Решения:** A4.10, A4.11, A4.12, A4.13, A4.15, A4.21, A4.22, A4.23, A4.27, A4.29, A4.30 · B1.13, B1.16
- **Что менять:** View `credos-time-approval` — экран-очередь (A4.11); approve всех SUBMITTED одного сотрудника разом (A4.10); причина reject на сотрудника (A4.12), нет частичного reject (A4.13); toast «Согласовано N, пропущено M» (A4.15); агрегат-бейдж + счётчики (A4.21); баннер REJECTED над таймшитом + в «Мои периоды» (A4.23); кнопка «Создать акт» ручная (A4.27); in-app+email уведомления (A4.29); напоминания связать с «Незаполненные» (A4.30); дайджест руководителя/вкладка «Команда» (B1.13/B1.16). *Многое уже в недавних коммитах — свериться.*
- **Зона:** Dev1 (UI) + Dev2 (logic/уведомления) · **Усилие:** L · **Риск:** behavior-change (часть готова — проверить дельту).
- **Приоритет:** P1.

### WI-20. Карточки сущностей: вкладки, name-поля, инлайн-реестры, доменные действия
- **Решения:** B2.1-B2.22 · C1.23
- **Что менять:** `objects` + views/page-layouts — +TEXT `name` проекта/отдела (заголовок=name, code=подзаголовок) (B2.12/B2.13); порядок вкладок проекта Сводка→Команда→Трудозатраты→Обзор→Бюджет→Этапы (C1.23); редкие вкладки за «…ещё» (B2.1); убрать RECORD_TABLE, оставить инлайн-relation без колонки-родителя (B2.6/B2.7/B2.8/B2.9); вкладка «Обзор» сотрудника (B2.4), «Загрузка» отдела (B2.5); front-component «Проекты, где работал» (B2.20); доменные кнопки в шапке (Submit/Создать акт) (B2.18); запрет правки после APPROVED (B2.19); метрики отдела ΣFTE (B2.14).
- **Зона:** Dev1 (front-components) + Dev2 (name-поля) · **Усилие:** L · **Риск:** two-phase (schema name-поля → views).
- **Приоритет:** P1 (крупный, дробить по карточкам).

---

## P2 — Улучшения, состояния, a11y, навигация, адаптив (волна 4)

### WI-21. Unified StateView (пусто/загрузка/ошибка/нет-доступа) по всему модулю
- **Решения:** D1.1-D1.20 · A3.27
- **Что менять:** `shared/state-view` (расширить `error-state.tsx`) — единый компонент иконка+заголовок+подсказка+опц.кнопка; скелетоны таблиц (D1.2/D1.3); ErrorState+«Повторить» везде, +onRetry в my-periods (D1.5/D1.6); 3 типа ошибок (D1.7); empty-state с действием «Добавить строку»/«Сменить период»/«Сбросить фильтры» (D1.10-D1.13); permission-denied экран (D1.15); баннер APPROVED/офлайн (D1.17/D1.19).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive (унификация).
- **Приоритет:** P2.

### WI-22. Навигация/сайдбар: группа «Согласование», «Мои трудозатраты», порядок, дубли, иконки
- **Решения:** B3.1-B3.16 · B3.5 (Записи→Таймшит)
- **Что менять:** `navigation-menu-items/*` — «Согласование» отдельной группой (B3.1); «Мои трудозатраты» над группами (B3.4); «Записи»→«Таймшит» (B3.5); скрыть дубль-VIEW календаря (B3.7); порядок справочников (B3.9); уникальные иконки (B3.10); скрыть «Настройки·данные» от не-admin (B3.13); +«Клиенты/Компании» в справочники (B3.15); почистить устаревшие комменты (B3.16).
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive.
- **Приоритет:** P2.

### WI-23. Дубли Сводка/Обзор + календарь-страница + Настройки·данные
- **Решения:** B2.2, B2.3, B3.7, B3.13, C1.20 · B4.20
- **Что менять:** развести Сводка(метрики)/Обзор(реквизиты) (B2.3); основная страница календаря vs VIEW (B3.7); «Настройки·данные» только admin (B3.13); singleton settings (B4.20).
- **Зона:** Dev1 (+Dev2 роли) · **Усилие:** S · **Риск:** additive. *Частично пересекается с WI-17/WI-22 — делать вместе.*
- **Приоритет:** P2.

### WI-24. Справочники: soft-delete, quick-create, пресеты, дерево/список, валидация FTE
- **Решения:** C1.1, C1.2, C1.4, C1.5, C1.6, C1.7, C1.8, C1.9, C1.10, C1.11, C1.24, C1.25
- **Что менять:** флаг «активен» + архивация вместо удаления (C1.2/C1.7); предупреждение перед удалением проекта (C1.8); quick-create форма (C1.5); дерево/список отделов (C1.4); пресеты «Мои/Активные» (C1.10); поиск по меткам (C1.9); валидация Σ FTE долей=100% (C1.24) и предупреждение Σ FTE сотрудника>100% (C1.25).
- **Зона:** Dev2 (поля/валидация) + Dev1 (UI) · **Усилие:** M · **Риск:** two-phase (active-поле).
- **Приоритет:** P2.

### WI-25. A11y: role=grid, aria-label, клавиатура, контраст, heatmap-ступени
- **Решения:** D2.1-D2.17
- **Что менять:** `grid/*` — `role=grid/row/gridcell`+aria-rowindex/colindex (D2.1), полный aria-label ячейки (D2.2), aria-label icon-buttons (D2.6), Home/End навигация (D2.7), Esc закрывает поповеры + focus-trap (D2.9/D2.10/D2.11), polite-объявления (D2.12); контраст textFaint до WCAG AA (D2.14); 3-4 ступени heatmap + легенда (D2.16); hit-zone ≥24px (D2.17).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive (крупнейший a11y-долг).
- **Приоритет:** P2.

### WI-26. Адаптив: sticky-колонки, h-scroll, overflow-меню, день-вид как мобайл
- **Решения:** D3.1-D3.15 · A1.4
- **Что менять:** `grid/week-header.tsx` — размеры ячейки 58→64px / 32→40px (TIMESHEET §F2); sticky 2 левых колонки + h-scroll по дням (D3.3); вторичные кнопки в overflow «⋯» (D3.11/D3.13); KPI 2×2 на узком (D3.10); день-вид как мобильный ответ (D3.5); preset-чипы для touch (D3.15). Десктоп-приоритет (D3.1/D3.14).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** RemDOM (нет @media — фикс-лейаут + h-scroll). *Размеры ячейки пересекаются с WI-01.*
- **Приоритет:** P2.

### WI-27. UX-копирайтинг: единый словарь статусов/нормы/действий
- **Решения:** D4.1-D4.22
- **Что менять:** по всему модулю — 4 статуса «Черновик/На согласовании/Согласовано/Отклонено» (D4.15), «норма дня» не «8 ч» (D4.8), «рабочие дни (по календарю)» (D4.7), confirm = само действие с счётчиком (D4.11/D4.12), словарь autosave «Сохраняю…/Сохранено✓/Ошибка» (D4.18), «Баланс/Утилизация» + тултипы (D4.1/D4.2), em-dash норма (D4.22).
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive (сквозной словарь — делать вместе с соответствующими WI).
- **Приоритет:** P2.

### WI-28. Экспорт-расширения: пресеты «для 1С/полный/краткий», имя файла, аудит, 1С API (фаза 2)
- **Решения:** C4.4, C4.5, C4.12, C4.13, C4.14, C4.15, C4.20, C4.21 · C1.16, C1.18
- **Что менять:** detail CSV +этап/комментарий (C4.4); пресеты экспорта (C4.5); подытоги по отделу/кодам (C4.12); имя файла с отделом/периодом (C4.20); «экспорт текущего вида» (C4.21); BillingLink +контрагент/статус оплаты (C1.18); сопоставление externalCode (C4.15); цель — двусторонний 1С API (C4.13/C4.14, фаза 2).
- **Зона:** Dev2 · **Усилие:** L · **Риск:** two-phase (после WI-04/WI-15).
- **Приоритет:** P2.

### WI-29. Booking roadmap: TBD-ресурс (роль-заглушка), сравнение бронь/факт, вкладка в клиенте
- **Решения:** C2.10, C2.11, C2.12, C2.13, C2.14, C2.18, C2.20, C2.21, C2.22 · A2.30
- **Что менять:** роль-заглушка (employee NOT NULL → schema-изменение, C2.11/A2.30 — roadmap); замена employee при найме (C2.12); demand с заглушкой (C2.13); «преобразовать в проектную бронь» (C2.10); вкладка «Брони» в Company (C2.20); карточка брони бронь-vs-факт (C2.21); пунктирная зона риска SOFT (C2.18).
- **Зона:** Dev2 (schema) + Dev1 · **Усилие:** L · **Риск:** two-phase (schema employee nullable — крупное).
- **Приоритет:** P2 (roadmap).

### WI-30. Планирование фаза 2-3: «Вручную по месяцам», единицы FTE/ч-нед, план×сотрудник
- **Решения:** PLANNING_MODELS A1/A2/C1/D2 · PLANNING_DIALOG Фаза 2-3 (Q12/Q13/Q10/Q11/Q18) · A2.1, A2.2
- **Что менять:** поле `planMethod` (EVEN/MANUAL) + хранение слотов период×часы (новый объект `credos-time-plan-slot` или JSON); таблица месяц×часы в панели; единица FTE%/ч-нед (`planUnit`); план×сотрудник (отдельный экран «ресурсный план»); точка прогноза «от сегодня».
- **Зона:** Dev2 (schema/calc) + Dev1 · **Усилие:** L · **Риск:** two-phase (schema) — строго после WI-05/WI-11.
- **Приоритет:** P2 (roadmap).

---

## РЕКОМЕНДУЕМЫЙ ПОРЯДОК ВОЛН

### Волна 1 — P0 фундамент (баги + SSOT + блокеры)
**WI-02** (норма SSOT) → **WI-03** (календарь SSOT) → **WI-07** (copy-week guard) → **WI-01** (контролы ячейки C1) → **WI-06** (bulk-fill развести) → **WI-05** (раскид по рабочим дням) → **WI-04** (backend download).

Логика: сначала SSOT нормы и календаря (WI-02/03) — на них опираются bulk-fill, planning, settings. WI-07 — дешёвый compliance-фикс. WI-01+WI-06 — корневой UX-баг ячейки (один заход в grid). WI-05 — корень непонимания планирования (нужен до панели плана). WI-04 — разблокирует весь экспорт.

### Волна 2 — P1 быстрые победы (additive, без schema)
**WI-08** (фильтры-чипы) · **WI-09** (datepicker/период) · **WI-12** (capacity-тулбар) · **WI-18** (ЛК баннеры/статусы) · **WI-16** (settings норма+валидация). Все переиспользуют готовое (filter-pills, useDailyNorm) — низкий риск.

### Волна 3 — P1 крупные (two-phase / schema / бэк)
**WI-17** (календарь-справочник, основа SSOT) · **WI-10** (recall/отзыв статусов) · **WI-11** (панель плана) · **WI-13** (booking quick-create) · **WI-14** (отчёты+Т-13 вкладка) · **WI-15** (Т-13 backend PDF+право Кадры) · **WI-19** (approval-очередь — свериться с готовым) · **WI-20** (карточки name-поля).

### Волна 4 — P2 полировка и roadmap
**WI-21** (StateView) · **WI-22/23** (навигация/дубли) · **WI-24** (справочники soft-delete) · **WI-25** (a11y role=grid) · **WI-26** (адаптив) · **WI-27** (UX-словарь) · **WI-28** (экспорт+1С API) · **WI-29** (booking TBD) · **WI-30** (планирование фаза 2-3).

**Hot-file предупреждение:** `grid/use-timesheet-actions.ts` и `grid/week-header.tsx` затрагиваются в WI-01/02/03/06/26 — выполнять последовательно одним потоком Dev1, не параллелить. `capacity/calc-load.ts` — WI-05/11/30 (Dev2 последовательно).

---

## Волна-2 ответов (E/F/G/Planning) — WI-31+

**Источник решений:** `UI_ANSWERS_2.md` (226 решений: E1–E4 микро/motion/onboarding/клавиши · F1–F4 плотность/тема/undo/уведомления · G1–G4 поиск/массовые/печать/локаль · Planning Q1–Q21 ратифицировано).
**Сверка с кодом (2026-06-21):** `grid/use-keyboard.ts` (минимален: только стрелки/Tab/Enter/Del — нет Shift+Enter/Ctrl+D/Home/End/`[`·`]`/clipboard) · `grid/cheatsheet.tsx` (содержит «Shift+Enter» и «↑↓←→» — но обработчика Shift+Enter НЕТ → баг E4.5; нет SSOT-массива → дрейф E4.14) · `shared/tokens.ts` (НЕТ motion-токенов, НЕТ `overBorder/warnBorder`, НЕТ density/dark) · `grid/save-indicator.tsx` (использует строковый `ease-out` → мигрировать на токен; НЕТ tint-эффекта ячейки) · `grid/approval-bar.tsx` (SUBMITTED уже янтарный `T.warn` → F2.7 частично закрыт) · `grid/validation-toast.tsx` (error/warning, БЕЗ undo/повтор) · `grid/autocomplete.tsx` (есть — база для поиска) · `shared/use-drill.ts` (память без URL → G1.15 новое) · `capacity/project-plan-panel.tsx` (385 строк, существует → Planning-Dialog в основном сделан в WI-11).

> **Важно — что из волны-2 УЖЕ покрыто/сделано в WI-01..30 (не дублировать):**
> - **E1.4/E1.10/E1.11/E1.12** (клик=ввод, ⋯ всегда видим, ячейка=число, dot-маркер) → **WI-01** (контролы C1). Статус: основное сделано/в работе.
> - **E1.5** (seed заменяет / клик выделяет всё) — паттерн F2; **частично** в WI-01, но требует явной доводки (см. WI-31).
> - **E4.1/E4.2/E4.3/E4.7** (Enter=вниз, Tab=wrap, Esc, Del=удалить) → уже в `use-keyboard.ts` (кроме Tab-wrap на след.строку — clamp сейчас). Доделать в WI-32.
> - **E3.x** (empty-states «first use vs no-results», cleared-week CTA) → пересекается с **WI-21** (StateView) и **WI-18** (ЛК CTA). Дополнить, не плодить.
> - **F2.7** (SUBMITTED янтарный) → сделано в `approval-bar.tsx`; проверить consistency по другим экранам (WI-35).
> - **F3.5** (счётчик «удалится N» в «Очистить неделю») → **WI-06**. **F4.x** (баннеры/бейджи/дайджесты) → во многом **WI-18/WI-19**.
> - **Planning Q1–Q21** → **WI-11** (панель `project-plan-panel.tsx` существует) + **WI-05** (раскид по рабочим дням) + **WI-30** (фазы 2-3). Волна-2 лишь ратифицирует — НОВОГО кода почти нет (см. WI-44 — только дельта-проверка).
> - **G2.3/G2.22** (Shift+Click диапазон строк, Shift+Arrows диапазон ячеек) пересекается с E1.3 → **WI-31/WI-32**.
> - **G3.x** (печать/PDF/Т-13) → **WI-04/WI-15/WI-28** (backend-файлы). Волна-2 уточняет коды (В/РВ/С), ориентацию, RBAC — дополнения в WI-43.
> - **G4.12** (произв.календарь) → **WI-17**; **G2.11** (норма SSOT) → **WI-02**.

### Легенда статусов WI-31+
- **новое** — кода нет, отдельный work-item.
- **дельта к WI-NN** — расширяет существующий WI волны-1 (не дублировать, добавить пункты).
- **частично** — инфраструктура есть, нужна доводка.

---

### P0 — баги волны-2

#### WI-31. Excel-паттерн ввода ячейки: seed-replace / click-select-all / красная рамка+форматы / placeholder / диапазон
- **Решения:** E1.5 (seed заменяет значение, клик = выделить всё, F2-паттерн) · E1.6 (красная рамка input + подсказка форматов при невалиде, НЕ тихий откат) · E1.7 (placeholder «8 / 8:30» в пустом input + полный список в cheatsheet) · E1.3 + G2.22 (Shift+клик/Shift+Arrows = диапазон ячеек `(row,col)`)
- **Что менять:** `grid/hour-cell.tsx` — при печати цифры заменять значение, при клике `input.select()`; невалид → красная рамка (`T.over`/`overBorder` из WI-34) + inline-подсказка форматов вместо отката (связано с E1.21/F3.9); placeholder `8 / 8:30`. `grid/use-keyboard.ts` + `weekly-grid.tsx` — selection-стейт диапазона `(row,col)` по Shift.
- **Зона:** Dev1 · **Усилие:** M · **Риск:** behavior-change + RemDOM (нет drag — диапазон только клавишами/Shift+клик) · **Приоритет:** P0 · **Статус:** частично (база в WI-01, доводка E1.5/E1.6 — новое).

#### WI-32. Клавиатура-доработка + SSOT горячих клавиш (фикс дрейфа cheatsheet↔код)
- **Решения:** E4.5 (реализовать Shift+Enter = заполнить будни строки — сейчас в cheatsheet есть, в коде нет → БАГ) · E4.6 + E1.20 (Ctrl+D/Ctrl+R заполнить вниз/вправо) · E4.4 (Home/End по строке, PageUp/Down смена недели) · E4.9 (`[`·`]` смена недели) · E4.2 (Tab-wrap на начало след.строки) · E4.8/E4.10 (контейнерный keydown, root `tabIndex=0`, автофокус) · E4.13 (cheatsheet закрывается Esc И кликом-вне) · **E4.14** (SSOT-массив клавиш → cheatsheet И обработчик из одного источника, паттерн `category-meta`/`tag-meta`) · E1.18/E1.19 (внутренний clipboard виджета — React-стейт, Cmd+V/копировать неделю)
- **Что менять:** новый `grid/keymap.ts` (SSOT: `{keys, desc, handler-id}`) → импорт в `use-keyboard.ts` и `cheatsheet.tsx`. `use-keyboard.ts` — добавить Shift+Enter, Ctrl+D/R, Home/End, PageUp/Down, `[`·`]`, Tab-wrap. Внутренний clipboard-буфер в grid-стейте.
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive (SSOT) + behavior-change (новые клавиши); **E4.5 = P0-баг** · **Приоритет:** P0 (E4.5) / P1 (остальное) · **Статус:** новое (SSOT нет; Shift+Enter — баг).

#### WI-33. Undo-тост вместо confirm для обратимого + «оставить ввод, не откатывать молча»
- **Решения:** E1.22 + F3.4 (undo-тост «Строка удалена · Отменить» 5с вместо confirm; confirm оставить только на batch «Очистить неделю») · E1.21 + F3.9 (при ошибке save — оставить значение + «Не сохранено · Повторить», НЕ молчаливый откат) · F3.1/F3.2/F3.3 (Undo последнего действия, в памяти сессии, Ctrl+Z + toast 5-6с) · F4.12/F4.13/F4.14/F4.15 (единый ToastHost: успех 3с тихо, warn 6с, error до закрытия+×; aria-live по severity; тост только для дискретных действий, не каждой ячейки) · E4.7-в (Del → undo-тост)
- **Что менять:** расширить `grid/validation-toast.tsx` → единый `shared/toast-host` с actions (кнопка «Отменить»/«Повторить»). Undo-буфер последнего действия в grid-стейте. `use-timesheet-actions.ts` — clearRow без confirm → undo-тост; ошибка PATCH → retry-тост. Ctrl+Z в `keymap` (WI-32).
- **Зона:** Dev1 (+Dev2 при retry-логике PATCH) · **Усилие:** M · **Риск:** behavior-change (убираем confirm удаления строки) · **Приоритет:** P0 (потеря ввода = баг) · **Статус:** новое (toast без undo/retry сейчас).

#### WI-34. Дрейф-цвета → токены (overBorder/warnBorder) + textFaint до AA + SUBMITTED-consistency
- **Решения:** F2.6 (токены `overBorder`/`warnBorder`, убрать хардкод `#f3c4ab`/`#f6da90` из validation-toast/reminder-banner) · F2.14 (textFaint → AA на todayCol/weekendBg/heatmap) · F2.15 (обязательная ведущая иконка у warn/error — уже есть в toast, проверить везде) · F2.7 (SUBMITTED янтарный — done в approval-bar, выверить остальные экраны)
- **Что менять:** `shared/tokens.ts` — добавить `overBorder`, `warnBorder`, поднять `textFaint` контраст. `grid/validation-toast.tsx` (строки 14-15 хардкод border), `reminder-banner` — на токены.
- **Зона:** Dev1 · **Усилие:** S · **Риск:** additive · **Приоритет:** P0 (a11y-контраст + дрейф) · **Статус:** новое.

---

### P1 — ядро волны-2

#### WI-35. Motion-токены: ease-out-quart + длительности + fade-поповеры + save-тинт + skeleton
- **Решения:** E2.4 (один токен `--ease-out-quart` `cubic-bezier(0.25,1,0.5,1)`, убрать строковый `ease`) · E2.1/E2.6 (fade opacity 150мс на поповеры; crossfade при смене режимов — НЕ slide) · E2.2 (правило: НЕ анимировать layout — только opacity/color/box-shadow/transform) · E2.3 (тинт ячейки 100-150мс при успешном save) · E2.5/E2.7 (100мс feedback / 200мс меню; box-shadow 80-100мс на active) · E2.8/E2.9 (skeleton-строки в форме таблицы, статичный без shimmer) · E2.10 (минимум анимаций → reduced-motion тумблер не нужен, prefers-reduced-motion в RemDOM недоступен) · E2.11 (анимировать только тосты)
- **Что менять:** `shared/tokens.ts` → добавить `MOTION = { ease, fast:100, menu:200, fade:150 }`. `save-indicator.tsx:32` строковый `ease-out` → токен. `hour-cell.tsx` — save-тинт. Поповеры (cheatsheet/row-menu/cell-comment/plan-panel) — fade-вход. Skeleton-строки (связать с WI-21 StateView, D1.2/D1.3).
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive · **Приоритет:** P1 · **Статус:** новое (motion-токенов нет; skeleton частично в WI-21).

#### WI-36. Плотность данных: 2-режима (compact/standard) глобально, per-user, touch=comfortable
- **Решения:** F1.1 (2 режима, не 3) · F1.2/F1.9 (глобально на модуль, реестры наследуют) · F1.3 (хранить в `credosTimeSettings` per-user, не singleton) · F1.4/F1.13 (compact меняет высоту строки+padding, font НЕ ниже 12px) · F1.5 (скрывать вторичное ✎/бейджи, показывать по фокусу строки — RemDOM: фокус надёжнее hover) · F1.6 (compact только desktop; touch принудительно 44px) · F1.7 (цель: неделя сотрудника без скролла) · F1.10 (на доске compact меняет только высоту строк, ширину период-колонок не трогать — heatmap) · F1.12 (убрать зебру в compact, оставить в standard) · F1.8 (sticky-header + пагинация, виртуализация позже)
- **Что менять:** поле `densityMode` в settings (Dev2). `shared/` — провайдер/хук плотности; `grid/week-header.tsx`, реестры, `capacity/dept-row.tsx` — высоты от режима. Тумблер в шапке/настройках.
- **Зона:** Dev1 (+Dev2 поле settings) · **Усилие:** M · **Риск:** two-phase (поле) + RemDOM (нет @media → фикс-логика touch) · **Приоритет:** P1 · **Статус:** новое (density нет нигде).

#### WI-37. Чек-лист видимых колонок per-view (особенно detail-отчёт 40+ полей)
- **Решения:** F1.11 (per-view выбор колонок, паттерн T TimesheetTemplate, отдельно от плотности) · G1.x пересечение с пресетами
- **Что менять:** `reports/` — UI выбора колонок (дропдаун-чеклист), хранить per-view в settings. Применить к detail-отчёту (40+ полей из недавнего коммита).
- **Зона:** Dev1 (+Dev2 хранение) · **Усилие:** M · **Риск:** additive · **Приоритет:** P1 · **Статус:** новое. *Пересекается с WI-28 (пресеты экспорта) — согласовать модель колонок.*

#### WI-38. Глобальный поиск-строка (проекты/сотрудники/отделы) в шапке
- **Решения:** G1.1/G1.3 (видимая строка «Поиск…» в шапке, НЕ Cmd+K — нет глобальных слушателей в Worker) · G1.2 (проекты+сотрудники+отделы; записи — позже) · G1.5 (автокомплит сотрудника по ФИО+табельному+email) · G1.6/G1.20 (проекты по коду+клиенту, мульти-поле в одном инпуте) · G1.7 (активные сверху, уволенные внизу серым) · G1.8 (сначала «начинается с», потом «содержит», без fuzzy) · G1.9 (мгновенный фильтр локально; debounce только серверный) · G1.10 (счётчик «найдено N из M» + «показать ещё») · G1.11 (недавние сверху) · G1.18 (пустой результат + «Сбросить фильтр») · G1.19 (подсветка совпадения)
- **Что менять:** новый `shared/search-box.tsx` (переиспользовать `grid/autocomplete.tsx`). Подключить в шапке модуля.
- **Зона:** Dev1 · **Усилие:** M · **Риск:** additive (autocomplete есть) · **Приоритет:** P1 · **Статус:** новое.

#### WI-39. Массовые операции в режиме «Согласование» (inline-чекбоксы + sticky-бар + bulk-действия)
- **Решения:** G2.1 (inline-чекбоксы строк — RemDOM: порталы недоступны) · G2.2 (согласование+отчёты; в своём таймшите bulk не нужен) · G2.3 (Shift+Click диапазон строк — локально) · G2.4 (sticky-бар «Выбрано N: [действие]» внизу виджета) · G2.5 (массовая смена проекта через RowMenu «Перенести в проект…») · G2.6 (массовая смена вида работ) · G2.7 (отозвать с согласования пачкой SUBMITTED→DRAFT) · G2.8 (массовый тег) · G2.9 (inline-поповер у бара — модалка к body недоступна) · G2.23 (toast «N согласованных не изменено» — CISO-012, не тихо) · G2.21 (один шаг undo для массовых)
- **Что менять:** `grid/`/approval-view — selection-стейт строк, sticky bulk-bar, inline-поповеры действий. Связать переходы статусов с WI-10.
- **Зона:** Dev1 (+Dev2 для bulk-переходов) · **Усилие:** L · **Риск:** additive + RemDOM · **Приоритет:** P1 · **Статус:** новое. *Часть approve-bulk пересекается с WI-19 (approval-очередь) — делать вместе.*

#### WI-40. Уведомления-инфраструктура: in-app центр (руководитель) + бейджи + preferences
- **Решения:** F4.1/F4.2 (in-app сейчас баннер+центр; email-дайджест когда появится канал — SDK песочница канал не даёт = блокер доставки) · F4.3 (центр уведомлений только руководителю; сотруднику контекстные баннеры) · F4.4/F4.8/F4.20 (бейдж-счётчики: «Записи», «N не заполнили» в отчётах, «N на согласовании» в меню) · F4.5/F4.6 (reminder фикс-день + за N дней до закрытия, порог настраивает менеджер) · F4.7 (два повода: не заполнил / не отправил DRAFT с часами) · F4.16 (настройка звука, по умолчанию выкл) · F4.18 (персональные preferences — чеклист типов) · F4.19 (обратный отсчёт «до закрытия N дней» в баннере) · F4.9/F4.10/F4.11 (2-уровневая эскалация дайджестом руководителю отдела, ФИО раскрывать — B1.14/CISO)
- **Что менять:** `my-time/`/approval — бейджи в меню; центр уведомлений (руководитель); preferences в settings. **Email отложить** (нет канала в песочнице — пометить блокером, готовить только in-app).
- **Зона:** Dev1 (UI) + Dev2 (reminder-логика/счётчики) · **Усилие:** L · **Риск:** additive (email = заблокировано платформой) · **Приоритет:** P1 · **Статус:** новое (частично — баннеры в WI-18). *Email-доставка = roadmap-блокер.*

#### WI-41. Onboarding/empty-states волны-2: first-use vs no-results, dismissible-подсказки, серверный флаг
- **Решения:** E3.1 (empty: «нет проектов в системе» vs «есть проекты, нет ваших записей») · E3.3 (cleared-week → «Неделя пуста · Заполнить по прошлой?») · E3.4/E3.7 (1-2 dismissible-подсказки у ключевых контролов + one-liner-баннер при первом входе; полный тур не нужен) · E3.5 (серверный флаг «видел онбординг» на пользователе Twenty — localStorage в RemDOM нет) · E3.6 (контекстная подсказка формата часов при первом фокусе, один раз) · E3.8 (unlinked → «Запросить привязку») · E3.11 («Добавить проект» фокусит AddRow + «Скопировать прошлую неделю» в пустом)
- **Что менять:** расширить **WI-21** StateView типами first-use/no-results/cleared; поле-флаг онбординга на employee (Dev2); dismissible-подсказки у контролов.
- **Зона:** Dev1 (+Dev2 серверный флаг) · **Усилие:** M · **Риск:** two-phase (флаг) · **Приоритет:** P1 · **Статус:** дельта к WI-21 (+серверный флаг новое).

---

### P2 — улучшения/локаль/печать волны-2

#### WI-42. Форматы часов и локаль: запятая/точка, HH:MM↔Decimal, превью, шаг 0.25, warn >12ч
- **Решения:** G4.5/G4.6 (запятая в UI / точка в CSV; ввод принимает обе) · G4.13/G4.14 (переключатель Decimal/HH:MM; нормализация «1:30»→«1.5») · G4.15 (шаг 0.25) · G4.16 (warn >12ч OVERTIME_WARN, блок >24ч) · G4.17 (превью «= 1.5 ч» при нестандартном вводе) · G4.7/G4.21 (UI `ДД.ММ.ГГГГ`, CSV ISO/число) · G4.9/G4.11 (диапазон дат + № недели ISO в шапке) · G4.10 (Пн жёстко) · G4.1/G4.2/G4.3/G4.4 (date-only, Duration-only, МСК, границы по локали — в основном уже так)
- **Что менять:** `grid/` — парсер ввода (обе разделители), превью распознавания, переключатель формата (хранить в settings); warn >12ч. № недели ISO в `week-header.tsx`.
- **Зона:** Dev1 (+Dev2 настройка формата) · **Усилие:** M · **Риск:** additive · **Приоритет:** P2 · **Статус:** новое (превью/HH:MM-toggle нет).

#### WI-43. Печать/PDF волны-2: коды табеля (В/РВ/С), альбомная, подытоги, RBAC-маскирование, флаг «выгружено»
- **Решения:** G3.4 (коды + В выходной, РВ работа в выходной, С сверхурочные) · G3.7 (альбомная для широких сеток) · G3.8 (ИТОГО по группам + общий + подпись) · G3.13 (полный табель только HR; для не-HR код «Б» маскировать — CISO-010, 152-ФЗ) · G3.14 (руководитель — свой отдел, HR — всё, сотрудник — свои) · G3.16 (флаг «выгружено» → read-only, аудиторский след) · G3.2/G3.6 (Т-13 полная + сетка; табель + detail) · G3.17 (copy-paste textarea = временный костыль до файлового экспорта)
- **Что менять:** дельта к **WI-15** (Т-13 backend PDF) — добавить коды-справочник, альбомную ориентацию, подытоги, RBAC-маскирование «Б», флаг exported→read-only.
- **Зона:** Dev2 (PDF/RBAC/флаг) + Dev1 (выбор кодов/ориентации) · **Усилие:** M · **Риск:** two-phase + CISO · **Приоритет:** P2 · **Статус:** дельта к WI-15.

#### WI-44. Planning-Dialog — дельта-проверка (ратификация Q1–Q21, кода почти нет)
- **Решения:** Planning Q1–Q21 (приняты целиком) · Фаза-1 п.1: фикс `plannedHoursInPeriod` (календарь→рабочие дни) + редактируемая «С» + живое превью
- **Что менять:** свериться с `capacity/project-plan-panel.tsx` (385 строк, существует) и **WI-11**: подтвердить, что закрыты Q7 («С» редактируема), Q14/Q15 (превью по рабочим дням — связано с **WI-05**), Q16 (овербукинг показывать не блокировать), Q19/Q20 (Отмена/Сохранить без `window.confirm`). Q12/Q13 («Вручную по месяцам»), Q10/Q11 (единицы FTE/ч-нед), Q18 (план×сотрудник) → **WI-30** (фазы 2-3). НОВОГО кода нет, кроме незакрытых пунктов WI-05/WI-11.
- **Зона:** Dev1+Dev2 (аудит) · **Усилие:** S · **Риск:** additive · **Приоритет:** P1 · **Статус:** в основном done (WI-11) — только дельта-проверка.

---

### Запросы к заказчику (блокируют часть WI-31+)
1. **F2.4** — бренд-палитра Credos (hex/гайд) для accent → влияет на WI-34/WI-35.
2. **G4.18/G4.20** — целевая 1С-конфигурация (разделитель + формат даты CSV) → WI-42/WI-43.
3. **F2.1** — нужна ли тёмная тема → WI-30-уровень dual-token (сейчас отложено, светлая MVP).

### Тёмная тема — ОТЛОЖЕНО (F2.1/F2.2/F2.3/F2.5/F2.16)
Заказчик: тёмная тема позже, MVP светлый. Dual-token, OKLCH-heatmap, ручной тумблер (prefers-color-scheme в RemDOM недоступен) — **не в этой волне**, после подтверждения спроса. Отдельного WI не создаём — фиксируем как roadmap-задел.

---

## РЕКОМЕНДУЕМАЯ ВОЛНА-5 (реализация волны-2 ответов)

**Сначала P0-баги (один заход в `grid/`, Dev1 последовательно — hot-files):**
**WI-32** (Shift+Enter баг + SSOT клавиш) → **WI-31** (Excel-ввод seed/select/красная-рамка) → **WI-33** (undo-тост вместо confirm + retry) → **WI-34** (токены overBorder/warnBorder + AA).

**Затем additive P1 (низкий риск, переиспользуют готовое):**
**WI-35** (motion-токены — основа для всех анимаций) → **WI-38** (поиск, autocomplete есть) → **WI-36** (плотность) → **WI-44** (дельта Planning, дешёвый аудит).

**Крупные P1/P2 (схема/бэк/two-phase):**
**WI-40** (уведомления — email блокирован платформой, готовить in-app) · **WI-39** (массовые операции — вместе с WI-19) · **WI-37** (колонки per-view) · **WI-41** (онбординг — дельта к WI-21) · **WI-42** (форматы/локаль) · **WI-43** (печать-дельта к WI-15).

**Hot-file волны-2:** `grid/use-keyboard.ts` + новый `grid/keymap.ts` — WI-31/32 (один поток). `grid/hour-cell.tsx` — WI-31/33/35 (последовательно). `shared/tokens.ts` — WI-34/35/36 (последовательно). `grid/validation-toast.tsx` — WI-33/34.
