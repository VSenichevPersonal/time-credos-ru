# Risk Register — time.credos.ru

Owner: CISO. Severity: P0 (freeze) · P1 (high) · P2 (medium) · P3 (low). Статус: OPEN · MITIGATING · ACCEPTED · CLOSED.

Контекст: внутренний инструмент, dev/staging, 15–20 пользователей. Базовый posture 🟢 LOW. Severity указаны с поправкой на dev-среду; при выходе в прод — пересмотреть вверх.

| ID | Sev | Риск | Актив | Статус | Митигизация |
|---|---|---|---|---|---|
| CISO-001 | **P1** | Реальные ПДн сотрудников в git: (а) сырые дампы-источники — `research/directum5/trudozatraty-dir5.xlsx` (13k строк), `bitrix-users/roster.csv` (72 ФИО+email), `users-bitrix.html`, `timetta/raw-odata-Users-deep.json`; (б) хардкод 42 ФИО+email в `apps/time/scripts/seed-real.mjs` (коммит 56bc320). Нарушение правила команды и 152-ФЗ. | PII сотрудников | **MITIGATING** | ✅ CISO: дампы (а) сняты с tracking (`git rm --cached`) + добавлены в `.gitignore` (секция ПДн + конвенция `**/pii/**`, `roster*.csv`). Файлы на диске целы. ⏳ Dev 2: обезличить `seed-real.mjs` (синтетика + `@example.test`, реальные грузить из gitignored-источника). ⏳ arch: решение по истории git (internal-repo → переписывание опционально). |
| CISO-002 | **P2** | `approval.logic.ts` `runResolve` (approve/reject) не проверяет: (1) роль actor = «Руководитель»; (2) **separation of duties** — actor ≠ автор записи (сотрудник может согласовать свои трудозатраты); (3) scope отдела/проекта. REST идёт под `TWENTY_APP_ACCESS_TOKEN` (сервис-токен), per-user RBAC обходится на уровне logic-function. | Целостность согласования трудозатрат | OPEN | Dev 2: добавить guard actor-role + проверку `actor != entry.employee.workspaceMemberRef` + scope по отделу руководителя. CISO даёт требования. |
| CISO-003 | P3 | `manager.role.ts` `fieldPermissions: []` — нет field-level ограничений: роль видит все поля объектов целиком (PII всё-или-ничего). | PII / least privilege | OPEN | Оценить нужность field-level (email/middleName) при появлении ролей с частичным доступом. Пока dev — ACCEPTED-кандидат. |
| CISO-005 | **P1** | `time-entry-api.logic.ts`: личность сотрудника берётся из client-supplied `params.workspaceMemberRef`, не из аутентифицированного `event.userWorkspaceId`. `delete` (L113) удаляет любую запись без проверки владельца; `upsert` создаёт/правит записи от имени любого сотрудника (impersonation); `list` читает чужие. + DEV-fallback на «первого активного». **Скоуп расширен (2026-06-21):** `approval.logic.ts` recall/revoke (WI-10) + plan PATCH (UC-PLN-13) — тот же client-supplied actor. Класс broken access control / IDOR. | Целостность+конфиденц. трудозатрат | OPEN | Server-side резолв `userWorkspaceId`→employee (исследовать SDK / маппинг-таблица); ownership-guard на delete/patch; убрать prod-fallback. Распространяется на approval recall/revoke + plan PATCH. **Эталон (2026-06-21, итер.109):** Timetta отдаёт `editAllowed`/`deleteAllowed` с сервера в каждой записи — права НЕ на клиенте. Применить паттерн к list-ответу time-entry-api. Системно связан с CISO-002 C2. **РАЗВЕДКА SDK 2026-06-21 (Dev2, итер. ciso-005-actor) — ВЕРДИКТ: серверный actor НЕДОСТУПЕН для резолва роли.** `RoutePayload` несёт только `userWorkspaceId` (server-truth для аудита `approvedBy`, подделать нельзя), но Core REST/GraphQL не имеет ни объекта `UserWorkspace`, ни фильтра `WorkspaceMember.userWorkspaceId` (0 вхождений в schema.graphql; `/rest/me`,`/rest/currentWorkspaceMember` → 400). Мост `userWorkspaceId→workspaceMember→employee` ПОСТРОИТЬ НЕЛЬЗЯ → `resolveActorRole(record, serverUser)` нереализуем без компенсирующего контрола. Минимум для закрытия: (1) собственный объект-маппинг `userWorkspaceId→workspaceMemberRef`, заполняемый при install/входе [предпочтительно, до прода обязателен]; или (2) подписанный заголовок от gateway; или (3) RLS/fieldPermissions (raw-API, RBAC-волна). Роль НЕ менял (риск сломать dev-flow: ref пуст у 42/43). [findings/CISO-005](findings/CISO-005-time-entry-idor.md). **TOFU-INTERIM (2026-06-21, іт.128):** TOFU=ПРИЕМЛЕМ при: pre-seed маппинга до прод + userMapPending enforce + DEV-fallback прибрати. DoD: resolveActor→time-entry CRUD (delete/upsert) + plan-write; pre-seed виконано. |
| CISO-004 | **P2** | ADR-0003: общий мастер-объект **Employee** (ФИО/email) делится между time / catalog / CRM-Sales. Владелец и RBAC общих мастер-данных — «Открыто» в ADR. PII сотрудников станет видна продажам/каталогу без явного разграничения. | PII / межапповый доступ | OPEN | Определить владельца + RBAC общих мастер-объектов (особенно PII Employee) ДО старта catalog-app. `[ciso-review ADR-0003 concern]`. Не блокирует time (каталог — следующая итерация). См. [reviews/ADR-REVIEW-LOG.md](reviews/ADR-REVIEW-LOG.md). |
| CISO-006 | **P2** | filter injection в logic-functions + front-layer. **Gaps:** L2 `project-team/team-rest.ts:20` fetchProjectEntries без isUuid; L3 `grid/time-rest.ts:131` fetchEntries — from/to без isIsoDate + employeeId без isUuid. | Целостность / filter injection | **MITIGATING** | ✅ Level 1 (Dev2 2026-06-22): `params-validate.ts` SSOT + три logic-function покриті (approval/time-entry-api/reports). ⏳ L2: `team-rest.ts:20` isUuid guard (1 рядок). ⏳ L3: `time-rest.ts:131` isIsoDate/isUuid guards. [findings/CISO-006](findings/CISO-006-filter-injection.md). |
| CISO-007 | **P2** | `reports.logic.ts` `/s/reports`: `byEmployee` + `reports-detail.ts:62` (`employeeName=ФИО`) раскрывали ФИО всех 42 сотрудников без role-guard. | ПДн / конфиденциальность HR-данных | **ACCEPTED** | ✅ **0446388 (2026-06-22):** `revealNames=false` fail-closed реализован. **[user-direct 2026-06-22]:** Владелец (заказчик) принял решение: `revealNames=true` по умолчанию — ФИО обязательны для работы табельного учёта. 152-ФЗ обоснование: обработка ФИО сотрудников в системе учёта рабочего времени = исполнение трудового договора (ст. 6 ч. 5 п. 5) + законный интерес работодателя; все пользователи = сотрудники Кредос-С (внутренний инструмент). Dev2: `defaultValue: true` + миграция singleton. RBAC-волна уточнит scope: ФИО только для менеджера/HR после CISO-005. [findings/CISO-007](findings/CISO-007-reports-data-disclosure.md). |
| CISO-008 | P3 | `credosTimeAbsence.note` (TEXT, nullable): провоцирует ввод медицинских ПДн (тип «больничный»). Спецкатегория 152-ФЗ ст. 10 без явного ЛНА. Нет field-level ограничений. | ПДн / медицинские данные | OPEN | arch/Dev 2: help-текст «не указывайте диагнозы»; внести в PII_INVENTORY; field restriction при появлении HR-роли. [findings/CISO-008](findings/CISO-008-absence-pii.md). |
| CISO-009 | P3 | `seed-real.mjs`: содержит реальные наименования клиентов/заказчиков (ООО/ГУП/ФГБУ) + трудозатраты по ним. Не employee-PII, но коммерческая конфиденциальная информация. При компрометации репо — утечка клиентской базы. | Коммерч. конфиденциальность | OPEN | Dev 2: синтетические названия клиентов в сиде (аналог CISO-001). Реальные → gitignored. Одновременно с обезличиванием CISO-001. [findings/CISO-009](findings/CISO-009-seed-commercial-data.md). |
| CISO-010 | **P2** | F-F экспорт CSV/табель: (1) **REQ-0006 п.4 `tabel-T13` реализован (Dev2)**: код «Б» (SICK) в CSV при `codes=true` = медПДн 152-ФЗ ст. 10; нет role-guard на `codes=true`; (2) detail-CSV (`format=csv`) без role-guard — любой auth скачивает весь табель без ФИО (revealNames=false снижает, но UUID всех сотрудников раскрыты); (3) интеграция 1С = канал ПДн без ЛНА; F-E cron: email в логах, статус коллег. | ПДн / экспорт 152-ФЗ ст. 10 | **OPEN** | ⚠️ **T13: `codes=true` без role-guard = мед.ПДн на проде.** Guard: `codes=true` только для роли HR/isManager + отдельная ЛНА. Detail-CSV: role-guard экспорта (isManager) — MITIGATING через revealNames=false. Cron: письмо только про себя, email не в логах. Аудит-лог downloads — follow-up. [findings/CISO-010](findings/CISO-010-export-pii.md). |
| CISO-011 | **P2** | APPROVED-записи не заблокированы: `op=delete/upsert` в `time-entry-api.logic.ts` не проверяют `status !== APPROVED`; прямой REST PATCH обходит logic-function. Согласованные данные можно изменить/удалить после согласования → нарушение целостности табеля. REF-CHECK vs Kimai `exported`-флаг. | Целостность данных / аудит | **MITIGATING** | ✅ Level 1 (Dev2, 2026-06-22): guard в `op=delete` (L151) + `op=upsert` update (L190) → `{ok:false,error:'cannot_modify_approved'}`. `ENTRY_STATUS` из SSOT. ⏳ Level 2: fieldPermissions (RBAC-волна). ⏳ Level 3: `exported:true` при F-F выгрузке 1С. Прямой REST PATCH всё ещё обходит — закроется RBAC-волной. [findings/CISO-011](findings/CISO-011-approved-record-mutability.md). |
| CISO-012 | **P2** | `grid/time-rest.ts` пишет напрямую в Core REST, минуя `/s/time-entry`. Серверные гарды (lock-approved CISO-011, валидация-лимит, upsert-дедуп) на этом пути не срабатывают. Twenty SDK = реактивные триггеры only, database-event вето невозможно. | Целостность данных / bypass server enforcement | **MITIGATING** | ✅ L1 Dev1 (2026-06-22): `isCellLocked()` + `notifyLocked()` UI-слой. ✅ **L2 Dev1 (ee04343):** мутации грида роутятся через `/s/time-entry` → серверные гарды path-independent для UI. factHours + дедуп = DB-event/UNIQUE. ⏳ L3: fieldPermissions/RLS (raw-API / Postman) — RBAC-волна после CISO-005. |

| CISO-013 | **P2** | `approval.logic.ts` TOCTOU race в батчах: read-then-write без CAS. (W5A.7/W5A.29, іт.110) | Целостность согласования | **MITIGATING** | ✅ L1 Dev2 (wi55, 20:26): `casApply(id, expectedFrom)` — перевірка статусу перед PATCH; якщо сменился → `skipped` (ідемпотентно). collect-errors: батч не падає на середині, `failed:[{id,error}]`. Формат відповіді: `{ok, updated, skipped, failed}`. ⏳ L2: розподілена транзакція (SDK не підтримує — NOT planned). |
| CISO-014 | **P2** | Аудит-слід: revoke обнуляв approvedBy/approvedAt; approvedBy = подвійна семантика approver vs resolver. (W5A.11/W5A.12, іт.110) | Аудит / целостность | **RESOLVED (L1)** | ✅ Dev2 (wi56, 20:26): +4 поля `resolvedBy`/`resolvedAt` (approve+reject) + `revokedBy`/`revokedAt` (revoke+recall). approvedBy тепер тільки APPROVED; reject — resolvedBy. Семантика розведена. Джерело = `event.userWorkspaceId` (server-truth). ADDITIVE, dry-run ЧИСТО. |
| CISO-015 | **P0** | **CASCADE delete project/employee уничтожает APPROVED-записи на уровне БД** — CISO-011 guard обходится полностью. FK cascade на credosTimeEntry.projectId + .employeeId = полная потеря аудита табеля. (W5C.23/24, W4A.31, итер.111-112) | Целостность / аудит | **RESOLVED (L1)** | ✅ Level 1 (Dev2, волна-5C): `onDelete: CASCADE → RESTRICT` на `credosTimeEntry.employee` + `.project` (`credos-time-entry.object.ts` L138/L162). `OnDeleteAction.RESTRICT` поддержан twenty-sdk (`RelationOnDeleteAction` enum: CASCADE/RESTRICT/SET_NULL/NO_ACTION). БД теперь ЗАПРЕЩАЕТ удаление проекта/сотрудника при наличии записей (вкл. APPROVED) — FK cascade устранён, аудит табеля/1С защищён на уровне БД. Сверка Timetta (правило 8): проект/ресурс с проводками нельзя удалить, только архив — RESTRICT форсирует именно архивирование. dry-run: relation **update** (3eee/ca74 fieldMetadata), без field-drop/objectMetadata-конфликта. ⏳ Level 2: soft-delete (archived) флаг для project/employee — UI «архивировать» вместо delete (RBAC/UX-волна). До L2 — в прод-гейт. Связь C1.7/CISO-011. |
| CISO-016 | P3 | `detail-CSV` ФИО через revealEmployeeNames — аналитик флагнул (W5C.19, итер.111-112). **ПРОВЕРЕНО:** `reports.logic.ts:306` передаёт `reveal` в `computeDetail`; `reports-detail.ts:45` дефолт `revealNames=false`, маскирует при false. Guard реализован ✅. | ПДн / экспорт | **CLOSED** | ✅ Проверено CISO: guard реализован. Аналитик-флаг = false alarm. |

## Закрытые / принятые

| ID | Sev | Решение |
|---|---|---|
| — | — | Секреты (Railway-токены, API-ключи): `.env`/`.env.*`/`**/secrets/**` в `.gitignore`, в коде только `process.env`, в CI — `secrets.*`. ✅ Утечек не найдено. |

## CISO-012 — Фронт-CRUD мимо logic-гардов (server enforcement bypass)
**ОБНОВЛЕНО 2026-06-22:** грид-путь закрыт — запись грида роутится через /s/time-entry (ee04343), серверные гарды путь-независимы для UI. Остаток: raw-API доступ (Postman) → RLS/fieldPermissions, RBAC-волна после CISO-005. Статус: MITIGATING→бо́льшая часть закрыта.
**Уровень:** MITIGATING (частично) · Выявлено: Dev1 2026-06-22
**Суть:** грид ввода пишет напрямую Core REST (`grid/time-rest.ts`), НЕ через `/s/time-entry`. Серверные гарды в `time-entry-api.logic.ts` (lock-approved T3/CISO-011, валидация ERROR #4, upsert-дедуп) на этом пути НЕ срабатывают.
**Что спасает:** factHours — database-event триггеры (любой путь). Дедуп — UNIQUE-индекс на уровне БД (любой путь). 
**Что обходится:** lock-approved (можно править согласованную через грид), валидация-лимит (только клиентская проверка validateEntry). Клиент-сайд гарды = UX, не enforcement.
**Возможности SDK для серверного enforcement (оценка Dev2, 2026-06-22):**
- **database-event триггер НЕ может ВЕТО.** Twenty SDK даёт триггеры только `created`/`updated`/`destroyed` — **реактивные (after-mutation)**. before-update / pre-hook / синхронной валидации с возможностью отклонить операцию НЕТ. `throw` в хендлере НЕ откатывает мутацию — она уже состоялась (потому `wrapEvent` в `project-fact-rollup-events.ts` и глотает ошибки: throw бесполезен как вето). Источник: docs.twenty.com `logic/logic-functions.md` (типы триггеров: httpRoute / cron / databaseEvent / serverWebhook — все, кроме httpRoute, реактивные) + текущий код триггеров. → **Путь-независимый database-event guard на entry-update нереализуем этим SDK.**
- **Компенсирующий after-контроль (revert-к-снапшоту) рассмотрен и ОТВЕРГНУТ** как осмысленный сейчас: payload `update` содержит `before`, технически можно откатить PATCH-ом. НО — окно гонки (между мутацией и revert «грязные» данные видны выгрузке 1С), хрупкий revert для `destroyed` (восстановление удалённой записи с relations), риск ложных срабатываний на легитимной смене статуса approval-флоу, и дублирование будущей RLS-логики. Не делаем — переусложнение без надёжности.

**План (RBAC-волна, после CISO-005) — два реальных пути:**
1. **(Dev1)** роутить грид-записи (`grid/time-rest.ts`) через `/s/time-entry` — тогда работает уже существующий гард `cannot_modify_approved` в `time-entry-api.logic.ts`. Самый дешёвый путь, закрывает практику.
2. **fieldPermissions / RLS** на уровне Twenty (approved = read-only для не-руководителя) — единственный по-настоящему путь-независимый enforcement; требует CISO-005 (server-side actor).

database-event guard из плана ВЫЧЕРКНУТ — SDK не поддерживает вето (см. оценку выше).
